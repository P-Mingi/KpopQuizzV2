# VERSE LEDGER (append-only · started 2026-08-04)

Rule: every ruling, lock, gate verdict, approved commit, audit, chat
artifact, and method change gets a numbered entry the day it happens.
Cowork appends. Old entries never edited; corrections are new entries.
Types: RULING · LOCK · GATE · COMMIT · AUDIT · ARTIFACT · METHOD · EVENT.

## Compressed pre-history (before ledger existed)

- 2026-06..07: KpopQuiz workstreams A..S2 (SEO, redesign, duels, games,
  battle, mascot, funnel, trivia, discord, widgets, notifications,
  personality, quiz overhaul, marketing).
- 2026-07-28: W0 spike FEASIBLE. W1..W5 + W-NAV + W-CUSTOM + W-SEED
  built the Verse (entities, reading, editor, knowledge, collaboration,
  collections, customization). Tracker in Notion through here.
- 2026-07-30..08-01: V3 era (V-DESIGN, V-IDENTITY, V-HOME, V-SPACE-FLOW,
  V-PAGES, V-ROLES-CLEAR + V-EDITOR-MAX) · V-AUDIT ("unlit, not ugly")
  · V-POLISH 1+2 · V4 directives (parity/duality/depth laws) · V-MODES,
  V-CARDS-MAX, V-ESSAYS-MAX, V-ATLAS, V-PROFILE-ONE, V-COMM-3, V-TRUST
  (covenant live) · QA-PRELAUNCH V2 (migrations 140-144) · V-HARMONY-1,
  2A (four-door shell, 42 page types), 2B (WidgetShell) · V-REPAIR ·
  V-UPGRADE-1 (badges + shared-surface mirrors). Zero coherence debt.
  Migrations through 145. ~150 commits unpushed by design.

## Live entries

- L-001 · 2026-08-02 · RULING · Builder era directive: ONE
  builder-like system, blueprint written (VERSE-BUILDER-BLUEPRINT.md).
  Four rulings locked: constrained 12-col grid + reading-order law ·
  lean navbar (5 + More) + five-hub node map · ONE /admin hub ·
  buildable entity pages approved in principle (migration at Phase 3).
- L-002 · 2026-08-02 · GATE · Spike V-BUILDER-0 APPROVED. Architecture
  B: same-origin iframe of real render + overlay + optimistic DOM
  updates (0.8ms) + background validated save; contentEditable text.
  Phase 2 MUSTs: same-origin iframe · chrome-less draft-render route ·
  stable block ids · optimistic + iframe-truth reconcile.
- L-003 · 2026-08-02 · LOCK · Co-design 1 builder chrome ("looks
  perfect, letsgo"). Full text: blueprint section 13.
- L-004 · 2026-08-04 · LOCK · Co-design 2 block library interior
  (search + Frequent row + 6 categories 2-up honest cards +
  honest-hint empty cards + patterns insert whole at marked seam).
- L-005 · 2026-08-02..03 · COMMIT · c89850b step 1 approved: schema +
  lossless converter, stable ids, byte-parity bts/stray-kids/ateez.
- L-006 · 2026-08-03 · COMMIT · 2a6af25 step 2 approved: registry 31
  BlockSpecs (29 modules + doorway + prose). Doc drift: blueprint
  says 22 modules; reality 29.
- L-007 · 2026-08-03 · COMMIT · b5a1bc9 step 3 approved: space home on
  composition renderer, byte-identical; validator 8/8 gates.
- L-008 · 2026-08-04 · EVENT · Cowork chat migrated to new session.
  COWORK-HANDOFF.md written by old session. Old memory file
  verse-project.md LOST with old session storage. Recreated at
  docs/verse-project.md. Lesson: memory lives in docs/ only.
- L-009 · 2026-08-04 · ARTIFACT · Co-design 3 style panel mockup
  delivered (widget: desktop drawer Layout/Surface/Color/Text ·
  token-only picker · span presets + 12-tick rail · delete bottom
  with undo toast proposal · phone bottom sheet 2 detents). Three
  locks OPEN: delete undo-toast no confirm · panel retarget on
  selection · text precedence mark > block > world, align
  toolbar-only.
- L-010 · 2026-08-04 · EVENT · Worker step 4 report received: six
  marks live, commit 4b24dff (underline/strike StarterKit claim;
  highlight/size/align hand-rolled; render-content.ts + sanitizer +
  toolbar roving tabindex; parity 3/3 claimed).
- L-011 · 2026-08-04 · AUDIT · Step-5 gate HOLD. Five receipts
  demanded: (1) REAL Play triple-proof, zero-Play-files argument
  rejected (2) no-new-deps receipt: git show --stat all 4 commits,
  package.json/pnpm-lock untouched + where Underline extension comes
  from (3) alignment must be node-level attribute, state rendered
  element (4) bts parity row shows 0 modules converted: default-config
  honesty or converter failure, relabel row (5) validator wording:
  section cap gated, seoCritical REMOVAL not just hide, accent gate =
  palette token, raw hex rejected. Plus owner eyeball on light/dark
  screenshots. No clearance until receipts.
- L-012 · 2026-08-04 · METHOD · Chat rules: Cowork speaks /caveman
  always in this chat. PROTOTYPE-FIRST LAW standing: every idea/UX/UI
  gets rendered mockup widget -> owner critique -> lock -> only then
  worker paste.
- L-013 · 2026-08-04 · EVENT · Full background sweep: 41 docs .md
  files read + full Notion sweep. Notion lags entire post-W era
  (nothing on V-HARMONY/V-REPAIR/V-UPGRADE/V-BUILDER; last touch
  2026-07-29). Unique Notion-only content flagged in memory section 6.
- L-014 · 2026-08-04 · METHOD · Tracking system created: this ledger
  (append-only) + docs/verse-project.md (state snapshot) +
  docs/COWORK-HANDOFF.md (boot). All updates same-day, all on disk.
- L-015 · 2026-08-04 · EVENT · Deep research: loop engineering /
  agentic coding (Anthropic, addyosmani, Ralph, Cognition, MAST) +
  best skills sweep (GitHub). Findings in
  docs/VERSE-WORKING-SYSTEM-V2.md.
- L-016 · 2026-08-04 · METHOD · WORKING SYSTEM V2 proposed, PENDING
  owner ratification: file message bus docs/loop/ (MISSION/REPORT/
  BLOCKED) + docs/proofs/<step>/ artifacts · worker inner loop with
  LOOP CONTRACT (acceptance/budget/ratchet/no-placeholder) ·
  deterministic hooks (block push + migrations + test edits, Stop
  hook) · skills shortlist · full-autonomy + agent-teams REJECTED
  (gates stay human).
- L-017 · 2026-08-04 · EVENT · Supabase MCP inspected: token sees only
  Bloom org (louispecchia@hotmail.com). Prod rdkgouofytwfdpbxbzio
  unreachable from it. Rebind = owner reconnects connector with prod
  org account. Read-only use recommended even then; owner-runs-SQL
  law unchanged.
- L-018 · 2026-08-04 · RULING · WORKING SYSTEM V2 RATIFIED by owner.
  Skills: install all minus duplicates. docs/loop bus + proofs folder
  + loop contract + ratchet law + hooks go live via one-time worker
  setup step.
- L-019 · 2026-08-04 · EVENT · Supabase connector rebound by owner and
  VERIFIED: prod org visible (KpopQuizz rdkgouofytwfdpbxbzio
  ACTIVE_HEALTHY + BlindTest inactive). New law: Cowork may use MCP
  READ-ONLY (list_tables, advisors, logs) for audits; all SQL writes
  stay owner-run.
- L-020 · 2026-08-04 · AUDIT · Step-4 five receipts reviewed. 1-4
  ACCEPTED (real triple-proof vs f26cd7d, head 3522B identical, 720px
  probe clean · no dep changes, Underline bundled in StarterKit 3.29.2
  · alignment node-level class on p/h2 · bts row relabeled default
  config). Receipt 5 ACCEPTED with notes: section cap + block cap both
  gated · seoCritical hide rejected + removal auto-injected (note:
  Phase 2 UI must not offer delete on seoCritical, silent re-add would
  confuse builders) · accent gap-flag escalated to owner: validator
  accepts contrast-clamped hex (mirrors existing page-accent law,
  blueprint section 4 conformant). Cowork recommendation: KEEP
  validator as built, constrain the style panel UI to swatches only
  (world accent + palette), no free hex field. Owner ruling pending.
- L-021 · 2026-08-04 · LOCK · Co-design 3 STYLE PANEL locked by owner,
  all three rulings: (a) delete = instant + undo toast 6s, NO confirm
  popup (b) panel RETARGETS on new block selection, closes only on
  Esc/X/deselect (c) text precedence = selection mark > block default
  > world default; alignment lives ONLY in the text toolbar, never in
  the style panel. Plus accent ruling (keep clamped-hex validator,
  swatch-only UI) confirmed via mission.
- L-022 · 2026-08-04 · EVENT · File bus first use: mission step 6 +
  V2 setup written directly to docs/loop/MISSION.md by Cowork. Owner
  paste era ENDS; owner relays one-line triggers only.
- L-023 · 2026-08-04 · LOCK · Co-design 4 MOBILE EDIT SHEET locked:
  (a) tap = selection + action sheet from bottom (Modifier le contenu
  · Style · Monter/Descendre with "position n sur m" · Dupliquer ·
  Supprimer with undo toast); block auto-scrolls above sheet; tap
  outside deselects (b) mobile reorder = up/down only, big reorg =
  desktop; desktop reorder = DRAG on grip handles + seams as drop
  targets + keyboard fallback (already in co-design 1 lock; hand-
  rolled HTML5 DnD, no new dep) (c) text editing = native
  contentEditable, marks bar docked above keyboard, horizontally
  scrollable, alignment lives there (toolbar-only law).
- L-024 · 2026-08-04 · GATE · STEP 6 GREEN, PHASE 1 (V-BUILDER-1)
  CLOSED. Commit f13ddb9 (sweep, no code change needed) + V2 setup
  commit. All proofs verified by Cowork as files
  (docs/proofs/vbuilder1-step6/): routes 336, token gate, vpages,
  templates, fold, play-probe 12/12, parity ALL PASS, validator 8/8,
  marks 3/3, em-dash none, a11y aria-pressed, build exit 0. Six
  commits total (f1bbc35 .. 4b24dff, f13ddb9).
- L-025 · 2026-08-04 · METHOD · Working System V2 LIVE and proven in
  use (bus round-trip + proofs-as-files worked first try). Hooks
  active + tested (4 hostile blocked, fail-open on malformed).
  Rulings on worker flags: Stop hook = tsc + check:verse-tokens
  (full build at gates only) · guardrails get versioned (un-ignore
  .claude/settings.json, hooks/, skills/verse-laws/) · skill-install
  artifacts stay untracked.
- L-026 · 2026-08-04 · EVENT · Phase 2 spec WRITTEN:
  docs/workstream-vbuilder-2.md (8 steps, owner gate at step 7,
  honors spike MUSTs + locked co-designs 1-4; step 4 flagged blocked
  on co-design 5 locks). Mission for step 1 deposited in
  docs/loop/MISSION.md.
- L-027 · 2026-08-04 · LOCK · Co-design 5 PATTERNS locked by owner:
  (a) schematic previews, never fabricated content (b) pattern
  dissolves into free blocks after insertion, no persistent group
  (c) empty-data blocks insert with builder-only hint + min-gate at
  publish (d) curator-made custom patterns DEFERRED to Phase 3.
  Phase 2 step 4 unblocked. v1 pattern set = blueprint six.
- L-028 · 2026-08-04 · LOCK · Co-design 6 ADMIN HUB locked: (a) Build
  tab = full-screen door to /build, never an embed (b) tabs absent by
  role (affordance truth), default landing = Contenu (c) six tabs
  Build/Contenu/Membres/Croissance/Moderation/Reglages. CO-DESIGN
  QUEUE COMPLETE 6/6: chrome, library, style panel, mobile, patterns,
  admin hub. All builder-era UI locked before code.
- L-029 · 2026-08-04 · AUDIT · Phase 2 step 1 (commit 4e9035f, route
  group app/(builder)/build/[slug]) ACCEPTED on the render side:
  byte-identical fallback (bts 21380==21380) + draft-differs proof
  (stray-kids) + element-level chrome-less check + 404 both
  non-curator branches + noindex + routes 337 + tsc 0. Two receipts
  demanded before step 2 code: provenance of presentation_draft (no
  migration allowed) + location of the write path (autosave/publish/
  rollback rails). Also caught: staged REPORT.md served stale by the
  bridge mount cache; ground truth verified via device shell.
  Guardrail-ruling commit f349eff noted (Stop hook tsc + tokens,
  guardrails versioned).
- L-030 · 2026-08-04 · RULING · Disk cleanup inventory DEFERRED to
  end of Verse project (owner). GitHub private backup remote:
  approved in principle, owner runs the pushes himself (worker hook
  keeps blocking push); pending owner decisions: create the repo +
  whether to un-ignore all of docs/ so the backup carries the
  decision documents too (Cowork recommends yes, repo is private).
- L-031 · 2026-08-04 · RULING · Backup remote SHELVED by owner: stays
  100% local for now. Single-disk risk stands, owner-accepted.
  Cowork re-raises at every phase close (with the pre-push ledger).
- L-032 · 2026-08-04 · EVENT · Play-side competitor research done
  (isolated subagent): kpople.net, kpopless.com, kpopdle.com,
  kpopdle.app, heardle.live, kpopidle.com. Market core = daily
  wordle-deduction + spoiler-free share grid, absent from our stack.
  Ranked gaps: 1 daily "Idle" idol-guess (pure metadata, legal-safe)
  · 2 share grid + streaks + escalating clips on existing Blind Test
  · 3 Group-dle · 4 album/MV/photo modes LEGAL-FLAGGED (artwork
  rights) · 5 practice mode · 6 per-artist heardles (SEO surface).
  Not copying accounts/leaderboards (Elo duels already better).
  ARTIFACT: games hub v2 mockup delivered (dailies first, hero
  carousel removed, one-click direct play with default variant,
  variant landing pages kept for SEO only). Locks a/b/c open.
  G-HUB + "Idle" = Play backlog, serialized AFTER Verse Phase 2.
- L-033 · 2026-08-04 · LOCK · G-HUB co-design locked by owner (a/b/c
  all yes): hero carousel dead, dailies first with streak · hub Play
  = straight into default variant, variant pages SEO-only · compact
  tiles no animated previews. AMENDMENTS from owner: featured live
  ranking rotates DAILY · in-game mode menus become image cards.
  "Idle" daily + Blind Test share grid/streaks: APPROVED to Play
  backlog. Hi-fi prototype delivered (dual-skill design):
  prototypes/games-hub-v2.html. Research doc:
  docs/PLAY-COMPETITOR-RESEARCH.md. Covers = CSS placeholders;
  production imagery must be licensed artwork only (legal flag).
- L-034 · 2026-08-04 · LOCK · G-HUB AMENDED and final: hub tile
  carries TWO visible actions: big Play (launches last-played
  variant, default only on first ever play, stored locally) + real
  secondary button "All modes · N" -> the game's existing page
  redesigned as image mode-cards where EVERY card plays instantly
  (one tap). Per-variant interstitial screens die everywhere; the
  per-game page survives as the visual browse surface (also the SEO
  surface). In-game mode rail stays for mid-game switching.
- L-035 · 2026-08-04 · RULING · Owner opens the G-HUB chantier NOW as
  a PARALLEL FORK worker, against Cowork's serialize-first
  recommendation; accepted with guardrails: fork works ONLY in git
  worktree .worktrees/play on branch play-ghub (main stays Verse's),
  never pushes, never merges (merge = owner gate); fork's law = SEO
  parity (pixels change, indexable set never shrinks); CSS-only
  covers, no unlicensed imagery; no new games in scope. Fork bus =
  .worktrees/play/docs/loop/REPORT.md (main docs/loop/ stays Verse
  only). Full fork prompt delivered in chat 2026-08-04.
- L-036 · 2026-08-04 · AUDIT · Phase 2 step 2 (commit 79577c4)
  ACCEPTED + both step-1 receipts CLEAN (presentation_draft =
  pre-existing migration 139 column, DDL verbatim; write rails
  autosave/publish/rollback pre-exist). Proofs verified: published
  byte-identical 21380 flag-off, canvas +835 chars = exactly the 19
  id wrappers, overlay zero-mutation (597 nodes stable), keyboard
  nav, 5 regression harnesses green, ratchet held. Rulings: inline
  screenshots accepted through step 6 (capture harness = dep, gated;
  owner walks /build at step 7) · a11y background chrome fix = native
  inert at step 6 · gitignore generalization fine. NEW RECEIPT opened
  for step 3: prove data-block-id comes from the STORED composition,
  not recomputed by position (reorder must keep ids; else BLOCKED).
  Step 3 mission deposited.
- L-037 · 2026-08-04 · GATE · Stable-id receipt FAILED as suspected:
  ids recomputed blk-<type>-<nth> positionally, dropped on save
  (ModulePlacement has no id field; persistence was deferred).
  Reproducer proves swap renumbers same-type blocks. Worker STOPPED
  correctly with BLOCKED.md + options. Cowork ruling (no owner gate
  touched, owner may veto): fix 3.0 persist the id in the existing
  presentation jsonb (no migration), crypto.randomUUID (no dep),
  renderer reads stored id, legacy ids FROZEN at first save (Cowork
  addition to close the re-derive loophole). Then step 3 proper.
  Mission deposited.
- L-038 · 2026-08-04 · AUDIT · Fix 3.0 ACCEPTED (commit 837ac42, 5
  files): id persisted in jsonb + written back on save, freeze pass
  covers legacy AND injected seoCritical modules, validator id gates
  (shape, dup), renderer stamps stored id. Proofs verified: same-type
  reorder holds ids, "travelled not re-derived" freeze test, valid
  explicit id preserved verbatim, published byte-identical 21380,
  full regression green. Worker checkpointed before the step-3 engine
  (do-it-right law): accepted. BLOCKED cleared. Step 3 proper mission
  re-deposited.
- L-039 · 2026-08-04 · AUDIT · Phase 2 step 3 ACCEPTED (23a24ea,
  useBuilderComposition engine + shell ops). Six acceptances verified:
  3ms optimistic patch · save round-trip with persisted uuid ·
  hostile 422 reverts to last saved state, human sentence surfaced,
  DB uncorrupted · reorder keeps id set, DOM order = reading order,
  optimistic === server truth · undo/redo persisted across 4 ops
  (undo/save race found + fixed) · published byte-identical + full
  suite green. seoCritical blocks carry no delete handle (min-gate in
  the builder). bts draft reset after live tests. Step 4 mission
  deposited with co-designs 2 + 5 restated (worker wrongly awaited
  co-design 5 confirmation; locked at L-027).
- L-040 · 2026-08-04 · AUDIT · Phase 2 step 4 ACCEPTED (80f29d6,
  library drawer). Verified: 31/31 specs no dupes, empty categories
  absent, honest-hint = real min-gate sentence on server-computed
  readiness, block + pattern inserts fresh persisted uuids, dissolve
  proven by individual selection, StrictMode double-insert caught,
  dialog a11y complete, published byte-identical, suite green. Step 5
  mission deposited (style panel, co-design 3 restated). RULING -
  spec deviation: Width 1-12 panel control DEFERRED to Phase 3 (grid
  does not render yet; a width control now = lying preview, violates
  honest-preview principle). Owner may veto.
- L-041 · 2026-08-04 · AUDIT · Phase 2 step 5 ACCEPTED (588e9ca,
  style pipeline + panel). Verified: per-block styleOptions truth
  (members no Text, intro with Text) · retarget · round-trip jsonb
  tokens/enums with color-mix rendering · hostile value 422 + revert
  · duplicate/delete/undo from panel, delete absent on seoCritical ·
  published byte-identical · suite green. Deviations honored: Width
  Phase 3, alignment toolbar-only, no hex field, doorwayFormat
  deferred (per-door config, noted). Stale-closure bug on rapid edits
  found + fixed. Step 6 mission deposited (inline text + phone layer
  + inert a11y fix + first-build tour). Step 7 = owner gate next.
- L-042 · 2026-08-04 · AUDIT · Step 6 CHECKPOINT accepted (b7ede35):
  inert fix proven (21 background focusables removed from a11y tree)
  + first-build tour proven. Remaining: inline text (hardest piece,
  cross-document TipTap binding) + phone layer + parity re-proof.
  Worker pacing honest. Cowork advice to owner: run the continuation
  in a FRESH worker session (state on disk makes it lossless; clean
  context for the hardest code). Mission updated for continuation.
- L-043 · 2026-08-04 · EVENT · Step 6 continued in fresh session:
  phone layer DONE + proven (f428c3a, action sheet + bottom sheets +
  auto-scroll + 44px + no drag, live at 399px). INLINE TEXT BLOCKED
  on a real design-lock tension: rich text lives in verse_content
  with its own editor pipeline (TipTap section-editor, draft/lock/409
  rails), not in the presentation draft. Options: (1) native CE +
  hand-rolled serializer (parallel pipeline in disguise, XSS risk on
  fan content) (2) surface the EXISTING editor in the builder as an
  in-place overlay aligned to the block (zero parallel pipeline,
  recommended by worker + Cowork; amends co-design 1) (3) ProseMirror
  in foreign document (fragile, rejected). OWNER RULING PENDING.
- L-044 · 2026-08-04 · LOCK · Owner ruled OPTION 2: inline text = the
  existing section editor surfaced IN PLACE over the block (aligned
  geometry, .verse-prose typography), existing rails only (draft,
  lock, 409 conflict, sanitizer). Co-design 1 AMENDED accordingly;
  the spike's "native contentEditable" MUST is satisfied in spirit
  (TipTap is managed contentEditable), the raw-CE + hand-rolled
  serializer path is dead. Phone Edit-content row + marks-above-
  keyboard couple to it. Final step-6 mission deposited.
- L-045 · 2026-08-04 · AUDIT · Step 6 COMPLETE and ACCEPTED (3
  commits: b7ede35, f428c3a, inline-text commit). Option 2 delivered
  exactly: SectionEditor surfaced over block geometry, existing rails
  only, zero new endpoints. Proofs: round-trip + published parity
  (QAPROBE strong tag, indexable text unchanged) · 11/11 hostile
  marks clamped · 409 conflict proven from builder path · phone
  gating + sticky 44px marks bar · test space restored byte-for-byte.
  V-BUILDER-2 build steps 1-6 CLOSED. Step 7 gate-pack mission
  deposited (GATE-PACK.md + WALKTHROUGH.md + fresh SEO parity + Play
  triple-proof re-runs).
- L-046 · 2026-08-04 · EVENT · Owner V6 DIRECTIVES captured complete
  (docs/VERSE-V6-DIRECTIVES.md): D1 per-block editors + full
  component inventory + stickers micro-lattice · D2 header/identity
  editing · D3 perfect fandom page (BTS showcase, visual-first,
  toggles) · D4 atlas index as collapsible tree · D5 reader rail
  (save/ToC; sidebar clone REJECTED by Cowork, lean-navbar law) ·
  D6 density ruling 70% CAPTURED · D7 MY VERSE profile split.
  Fandom research done (bot-wall caveat: reconstruction). Sequencing
  proposed: gate -> V-BUILDER-3 block editors -> Phase 3 -> MY VERSE
  -> V-SHOWCASE-BTS. Owner questions open: image-policy ruling Q1,
  stickers Q2, sidebar-skip Q3, sequencing Q4.
- L-047 · 2026-08-04 · RULING · V6 questions answered by owner:
  Q1 IMAGES: owner OVERRIDES Cowork recommendation: all image
  sources allowed, moderation by owner/mod team AFTER publication.
  Cowork objection ON RECORD (copyright exposure), standing tension
  like pt-leaderboard. Technical shaping accepted: always ingest-COPY
  into our storage (never hotlink), post-hoc moderation queue with
  one-click takedown, DMCA takedown page + process mandatory.
  Q2 stickers micro-lattice: OK, to co-design in V-BUILDER-3.
  Q3 sidebar clone skipped (lean-navbar law stands), reader rail
  only: OK. Q4 sequencing 1-5 RATIFIED: gate -> V-BUILDER-3 block
  editors -> Phase 3 -> MY VERSE -> V-SHOWCASE-BTS; G-HUB fork
  parallel.
- L-048 · 2026-08-04 · AUDIT · Step 7 GATE PACK verified conforming
  (HEAD eeb6d7d): all gates re-run fresh and green, full build exit
  0, SEO parity IDENTICAL signatures (1676 chars / 38 hrefs / 8
  headings / sig bd7b7a1d both sides, only delta = 10 non-indexable
  handles), Play triple-proof per the law, one-H1 held, WALKTHROUGH
  guide delivered, test space clean. OWNER WALKTHROUGH PENDING:
  Phase 2 closes on the owner's GO after visiting /build desktop +
  phone.
- L-049 · 2026-08-05 · GATE · OWNER GO on the step-7 walkthrough
  (first attempt hit the curator 404 by design, resolved via
  /api/dev/login). V-BUILDER-2 approved at the owner gate. Step 8
  closing-sweep mission deposited (a11y re-pass, gates on final
  HEAD, sitemap-unchanged proof, deferred-items handoff doc,
  workstream file marked DONE per step). Phase 2 closes on Cowork's
  read of the step-8 report; V-BUILDER-3 spec next.
- L-050 · 2026-08-05 · EVENT · Play-side guest-conversion strategy
  researched (isolated agent) + written:
  docs/PLAY-GUEST-CONVERSION.md. Doctrine: never gate play, gate
  identity. Four moments: claim-this-run on the Hall of Fame ghost
  row (killer, uses existing "someone" rows) · streak backup day 3 ·
  stats view · post-share. Trust move: retroactive merge of ALL
  guest history on signup (anti-NYT-wipe). One Tap + passkey,
  username-only, email optional. Covenant-clean copy rules, no dark
  patterns. Instrumentation plan. Future workstream G-CLAIM,
  serialized on the Play side after G-HUB.
- L-051 · 2026-08-05 · RULING · G-CLAIM strategy VALIDATED by owner,
  plus owner addition: automatic founder WELCOME notification to
  every new account ("Welcome {username}...", warm, once, in-app
  only). Cowork honesty guardrail attached: genuinely from Mingi,
  automated delivery, never pretends to be hand-typed, signed
  founder, replies reach the real inbox; no email blast. Added to
  G-CLAIM step 3 in docs/PLAY-GUEST-CONVERSION.md.
- L-052 · 2026-08-05 · GATE · STEP 8 ACCEPTED, PHASE 2 (V-BUILDER-2)
  CLOSED. A11y re-pass fixed a real key-stealing bug live + focus
  traps + 44px sheets + inert robustness; full gates green on final
  HEAD; sitemap proven unchanged (2793 URLs, zero builder routes,
  robots byte-identical across the phase); deferred handoff doc +
  task board complete. The builder canvas is done: draft route,
  shell + overlay, optimistic engine with persisted stable ids,
  library, style panel, inline text via existing editor, phone
  layer, tour, all owner-gated. Pre-push ledger re-raised per L-031
  (160+ commits single-disk). V-BUILDER-3 spec WRITTEN
  (docs/workstream-vbuilder-3.md: editor schemas, content tab,
  image rail per L-047, members + hero editors, wave 2, stickers
  micro-lattice; co-design locks 7/8/9 required before their UI;
  V-BUILDER-4 = grid/patterns). Step 1 mission deposited. Cowork
  owes co-design 7 (content tab) in chat next.
- L-053 · 2026-08-05 · EVENT · Community-aliveness directive captured
  + researched: docs/PLAY-COMMUNITY-PULSE.md (future workstream
  G-PULSE, Play side). Owner ideas kept: vote celebration animation
  (juice, one-time daily XP, NO farmable per-action XP per Mekler/
  Hanus evidence), threaded replies, launch notification, opinion
  book on profile (private by default, Venmo lesson), plus research
  additions: vote-first-%-reveal, post-game debate prompt, prediction
  league, gentle streak, share card, cold-start playbook. RULING
  PENDING: owner's "default vote from kpophistory" REFUSED as fake
  activity (covenant; Meta/changemyview receipts); honest replacement
  proposed = signed Editor's Take opener, not counted in votes.
- L-054 · 2026-08-05 · RULING · Editor's Take APPROVED by owner
  (signed, displayed, never counted in the percentages). G-PULSE
  design complete at the backlog.
- L-055 · 2026-08-05 · AUDIT · V-BUILDER-3 step 1 ACCEPTED: editor
  -schema.ts (typed fields + curator/entity/derived bindings), 9
  wave-1 schemas, clampPropsBySchema with human sentences + smart
  guards (quote 280 lyric guard, stats https, external image
  rejected = ingest-copy at the root), validator wired, complete
  block inventory doc, byte-identical, gates green. Correctly
  stopped at the co-design-7 gate.
- L-056 · 2026-08-05 · AUDIT · G-HUB fork run COMPLETE on play-ghub
  (9 commits, base 79577c4), provisionally ACCEPTED: hub + lobbies +
  auto-start + mode rail + last-played + daily rotation per locks
  L-032..034; sweep self-caught the legal wall (idol photos removed,
  0 refs), a tot mapping inversion, and a .ghub CSS namespace
  collision (renamed .gh2-). SEO parity diffs identical-or-richer,
  zero links lost. NEXT: owner eyeballs /games on the worktree, then
  owner merge; POST-MERGE NOTE: Play triple-proof baselines must be
  re-baselined at the merge commit by the Verse worker.
- L-057 · 2026-08-05 · LOCK · Co-design 7 CONTENT TAB locked by
  owner, all three: (a) one panel, tabs Contenu | Style, Contenu
  default (b) per-field inline validation, human sentence under the
  field, siblings stay editable (c) bound-data badge system: donnee /
  modifie + one-tap revenir-a-la-donnee on every entity/derived
  field. Fields rendered from editorSchema only, saves on the draft
  rail. Step 2 mission deposited.
- L-058 · 2026-08-05 · RULING · G-HUB merge review verdict processed.
  Blocker found: main's tree carried an UNCOMMITTED competing hub WIP
  (spotlight/GOTD direction, pre-lock, never gated). Ruling: the
  LOCKED G-HUB v2 branch wins (process integrity: locks L-032..034);
  WIP archived to wip/gotd-spotlight-archive (nothing deleted, ideas
  recoverable via future co-design); unrelated pre-existing WIP
  (pinterest-feed allowlist, mobile-top-bar fold-in) to
  wip/deferred-reconcile. Order: archive -> clean tree -> rebase
  play-ghub on main -> fix trending-rankings-strip legal hole (same
  CSS-art law) -> re-build + Play checks -> STOP -> owner merges
  after Cowork reads. Fonts ruling: NO Unbounded/Instrument (product
  fonts stay; prototype typography was directional). name-all gap to
  the branch's deferred notes. Post-merge: Verse worker re-baselines
  Play triple-proof at the merge commit.
- L-059 · 2026-08-05 · AUDIT · Merge-prep verified DIRECTLY in git
  (session report unreadable, noted): wip archives created, play-ghub
  correctly rebased onto main tip 44dda93 (+10 G-HUB commits + legal
  fix 02319cc rankings strip), merge will be fast-forward. DANGER
  caught: the PRIMARY worktree was left checked out on play-ghub
  (Verse missions would commit to the Play branch): Verse worker
  frozen until checkout main restored. Missing: post-rebase build +
  Play-check receipts. Finish-prep paste issued (report to file,
  checkout main, prune, stop). Merge stays the owner's act
  (--ff-only) after Cowork reads receipts.
- L-060 · 2026-08-05 · GATE · G-HUB v2 MERGED into main (owner-
  approved, all local, zero push): ff 44dda93 -> bd6fd41 +
  verification commit 3a6bdfd. All gates re-run ON MERGED HEAD:
  build 0, routes 335, tokens, play-untouched 12/12, em-dash clean
  (30 merged files), legal wall proven (idol images only inside
  gameplay where identification IS the game). INCIDENT logged: two
  native sessions did git surgery on the same clone concurrently
  (one reset main mid-work, twice); owner halted one. NEW RULE: one
  native git session at a time; forks via worktree always. CASUALTY:
  uncommitted docs/loop/MISSION.md reverted to an old committed
  version by the surgery; rewritten by Cowork. Open question to
  worker: name the 3 routes 338->335. Verse UNFROZEN: step 2 mission
  re-deposited with Play triple-proof RE-BASELINE at merged HEAD.
  Play backlog remaining: K-pop Idle, Blind test share grid/streaks,
  G-CLAIM, G-PULSE.
- L-061 · 2026-08-05 · AUDIT · V-BUILDER-3 step 2 ACCEPTED (content
  tab re-applied on merged main after the clobber, re-verified):
  schema-driven renderer, inline per-field errors, Data/Edited badge
  round-trip in draft jsonb, gates green, English labels conform
  (mock FR was illustrative). Routes 338->335 RESOLVED: untracked
  working-tree files cleaned by the surgery, committed surface
  byte-identical; spike reference files verified SAFE on
  wip/deferred-reconcile 94d2f37. Step 3 mission deposited (image
  rail per L-047: ingest-copy, moderation queue with a documented
  design-exemption for the internal utility surface, DMCA page,
  migration via pending-migrations owner gate). Cowork still owes
  co-design 8 (members + hero editors) before steps 4-5.
- L-062 · 2026-08-05 · LOCK · Co-design 8 MEMBERS + HERO EDITORS
  locked by owner, all four: (a) entity picker = attach existing OR
  create entity+page in one act (b) "Retirer du widget" = detach
  only, entity and page survive (c) row detail = in-panel accordion,
  no modal (d) banner v1 = center crop, focal point deferred. Reuses
  co-design 7 badges + the step-3 image rail everywhere.
- L-063 · 2026-08-05 · GATE · Step 3 correctly BLOCKED at the owner
  migration gate. Recon quality high (139 rail ~80% reusable, SSRF
  guard hardening planned). Migration 146 SQL read line by line by
  Cowork: APPROVED with one mandatory amendment: BEGIN/COMMIT wrap
  (standing law, was missing). Noted elegant property: the partial
  unique (space_id, hash) excluding only 'removed' prevents
  re-uploading an identical image to bypass a hide. Corrected SQL
  handed to owner for the prod SQL editor. On "146 fait": migrations
  applied through 146; worker resumes step 3 build.
- L-064 · 2026-08-05 · GATE · MIGRATION 146 APPLIED by Cowork via the
  Supabase MCP on prod (rdkgouofytwfdpbxbzio), under explicit owner
  authorization, with the BEGIN/COMMIT wrap. VERIFIED by query: all
  6 columns present (hash, mime, source, source_url, reviewed_by
  uuid, reviewed_at timestamptz) + both indexes
  (uq_space_assets_hash, idx_space_assets_image_queue). Migrations
  applied through 146. Process note: owner may delegate execution of
  an owner-approved migration to Cowork via MCP; the WORKER still
  never touches the schema. Worker resumes step 3 build.
- L-065 · 2026-08-05 · EVENT · BLINDTEST-X captured + researched
  (docs/PLAY-BLINDTEST-X.md). Honest verdict: category real
  (Heardle 2M daily peak) but nobody monetizes; the moat is AUDIO
  RIGHTS; Spotify bans games, Deezer bans revenue, Apple-CDN
  previews = only workable grey; monetized-on-previews = exposure;
  sellable-on-previews = structurally impossible (rights debt).
  Strategy: Stage A V1.5 polish in KpopQuiz (share/streaks/
  per-artist dailies + audio-source conformity audit) · Stage B
  multiplayer rooms via Supabase Realtime (no new dep), skribbl
  link-invite model, subdomain, donations only · Stage C money/sale
  ONLY via re-founded audio (label deals or B2B host-plays-own-
  account). Queue: after G-CLAIM. Verse stays critical path.
- L-066 · 2026-08-05 · AUDIT · V-BUILDER-3 step 3 ACCEPTED (image
  rail): SSRF DNS-level hardening, EXIF-strip re-encode, SVG reject,
  sha256 dedupe proven, fail-closed resolveSpaceImage (0 render
  importers, honest scope: consumption in steps 4/6), admin queue
  Hide/Unhide/Remove proven, /dmca in both footers, 403/429 gates,
  e2e on a real external URL, test artifacts cleaned, gates green,
  routes 338. Steps 4+5 mission deposited (co-design 8 restated;
  worker had missed that L-062 was already locked).
- L-067 · 2026-08-05 · EVENT · Step 4 BLOCKED correctly at the
  real-data gate: no curator idol rail exists (Cowork's mission
  wrongly cited "existing entity rails": spec error owned); idols =
  core Wikidata-sourced table feeding games/rankings/SEO site-wide;
  getIdol filters active=true (detach would 404 the page); single-FK
  group model. Four owner rulings put to Mingi with Cowork recs:
  Q1 curator-created idols = needs_review + origin flag, indexable
  only after admin approval (stricter than images: entities
  propagate) · Q2 detach = page leaves index, data survives for
  re-attach · Q3 attach = re-activate own group's detached only ·
  Q4 order/overrides = draft; create/attach/detach = immediate with
  explicit confirm. Migration 147 anticipated (origin, created_by,
  detached_at) via the L-064 delegated-execution path.
- L-068 · 2026-08-05 · LOCK · Owner ruled "ok" on ALL FOUR (co-design
  8 governance annex): Q1 curator-created idol = origin='curator' +
  needs_review=true + active=false, page indexable only after admin
  approval · Q2 detach = active=false + detached_at, page leaves
  index (existing 404 path), data survives · Q3 attach scope = this
  group's detached idols only (single-FK, no cross-group v1) · Q4
  order/overrides = draft-staged; create/attach/detach = immediate
  with explicit confirm dialog. Migration 147 authored by Cowork
  (live schema inspected via read-only MCP: idols already has
  needs_review + review_reason + active; 147 adds origin CHECK
  wikidata|curator, created_by uuid, detached_at timestamptz, two
  partial indexes; BEGIN/COMMIT wrap; plain uuid mirrors
  verse_space_assets pattern). Deviation flagged: Cowork drafts the
  SQL instead of the worker to save a cycle; owner gate on the SQL
  text still required before MCP apply (L-064 path). NEW CODE LAW
  for step 4: any Wikidata re-sync must never re-activate a row
  where detached_at IS NOT NULL (curator detach survives sync).
- L-069 · 2026-08-05 · EVENT · Migration 147 APPLIED by Cowork via
  MCP (owner "go migration", L-064 path). Verified after apply:
  origin text default 'wikidata' + created_by uuid + detached_at
  timestamptz all present · both partial indexes exist
  (idx_idols_curator_review, idx_idols_detached) · 204/204 existing
  idols carry origin='wikidata' · zero rows modified. Steps 4+5
  mission live in docs/loop/MISSION.md; waiting on worker trigger.
- L-070 · 2026-08-05 · DIRECTIVE + RESEARCH · Owner pivot: the BASE
  (page creation, pages into pages, structure, indentation) outranks
  widgets; navbar rethink a la fandom.com; current look ruled ugly,
  wants minimal-calculated precise grid + XxX mini-blocks; BTS-only
  (drop other fandoms); target: 1,700-page scale freedom like
  bts.fandom.com. Three research agents dissected the BTS Fandom wiki
  (anti-bot 402 walls limited bulk listing; ~160 verified titles, 6
  page types dissected, nav tree from owner screenshots). Findings +
  honest gap analysis + plan written to docs/VERSE-FOUNDATION.md:
  workstream V-FOUNDATION F1-F6 (page tree core, creation UX, space
  nav 5x3x10, lateral web, design reset prototype-first, BTS-only).
  Four rulings put to owner (R1 unpublish-vs-delete, R2 freeze
  V-BUILDER-3 after steps 4+5, R3 lean-nav amendment, R4 start F5
  prototypes). Worker steps 4+5 still in flight, unaffected.
- L-071 · 2026-08-05 · LOCK · Owner ruled OK on all four V-FOUNDATION
  rulings: R1 UNPUBLISH stray-kids + ateez (not delete) · R2 worker
  lands steps 4+5, owner gate, then FREEZE V-BUILDER-3 steps 6-7-9
  and open V-FOUNDATION · R3 lean-nav law amended (site chrome
  unchanged 5+More; space gets curated 5x3x10 menu) · R4 Cowork
  prototypes before any build. Owner also ordered the full skeleton
  defined BEFORE prototypes, with proposals and questions, total
  honesty. Cowork answered with F0 THE SKELETON CONTRACT (co-design
  10, doc section 9): C1 one page model (pages tree + revisions +
  links + redirects + tags + nav_menus, blocks jsonb universal) ·
  C2 flat URLs + eternal redirects · C3 full history + trash day 1 ·
  C4 editing ladder curators-first · C5 mechanical stub noindex +
  sitemap · C6 links first-class + ghost links + backlinks ·
  C7 controlled tags · C8 galleries as collections over space_assets ·
  C9 pg full-text search with create-offer · C10 design system spec
  (4px unit, 12-col, 4x4 micro-lattice, 66ch, two canvases document/
  portal) · C11 unification: space home becomes portal page, builder
  rebinds to pages.blocks (adaptation cost stated honestly) ·
  C12 v1 scope cuts listed · C13 strangler migration law with SEO
  parity proof per switch. Seven rulings FQ1-FQ7 put to owner, all
  recommended OUI. Prototypes start after the F0 lock.
- L-072 · 2026-08-05 · LOCK · F0 SKELETON CONTRACT fully locked:
  FQ1-FQ7 all OUI (curators-only v1, flat URLs + eternal redirects,
  full history + trash day 1, mechanical stub noindex, controlled
  tags, unification + builder rebind, galleries as collections).
  Bonus DNA items endorsed (ghost links, backlinks, create-from-
  search, grid spec 4px/12col/4x4/66ch/two canvases, strangler
  migration law). Next: Cowork builds the five F5 prototypes
  (document page, portal, space menu, creation flow, tag index) for
  owner critique. Worker steps 4+5 still in flight.
- L-073 · 2026-08-05 · LOCK · Co-design F5 PROTOTYPES STRONGLY LOCKED
  by owner: the five screens (document, portal, menu 5x3x10, creation,
  tag) are the backbone, "exact same" with minor UI adaptation and the
  EXISTING KpopVerse logo kept. Principle ratified: widgets adapt INTO
  the skeleton, never the opposite. Next: F1 mission after the
  steps-4+5 gate.
- L-074 · 2026-08-05 · AUDIT · V-BUILDER-3 step 4 (members editor)
  CONDITIONALLY ACCEPTED. Proven as files: migration-147 columns
  receipt + full governance lifecycle ALL PASS (create -> 404 ->
  approve -> renders -> detach -> 404 + row survives -> sync-guard 409
  -> re-attach -> renders), probe idols cleaned; sync guard receipt
  with file+line (idol_activate refuses detached; refresh.ts writes no
  idols); http auth gates 403 + dup guard 409 'RM'; published parity
  (7 canonical links, zero external urls, empty props byte-identical);
  gates green (routes 338, tokens, vpages 55/0, a11y aria + 44px).
  TWO RECEIPTS OWED before the steps-4+5 owner gate: (1) draft jsonb
  round-trip receipt: order + overrides saved -> reloaded intact,
  stable ids kept (scriptable, no UI needed); (2) editor-panel
  screenshots light/dark once the dev server is stable. Step 5 GO
  only after the tree is single-writer again.
- L-075 · 2026-08-05 · INCIDENT · SINGLE-WRITER LAW (L-060) violated
  again: a concurrent native chat committed 8+ times to main in the
  SAME worktree during step 4 (b5162d5, 3ea961d, ca23b18, 5860bb3,
  322a597, a45da30, b59bd11, b19c21b: games hub, name-them-all, admin
  game images, Deezer covers). Real damage: dev-server RSC/HMR churn
  blocked the step-4 editor screenshot proof; 3ea961d leaves ONE tsc
  error on main (games/name-them-all/page.tsx:96) which poisons every
  repo-wide tsc gate; a45da30 also touched a VERSE file
  (src/app/verse/page.tsx: dark mode / logo) outside any audited loop.
  Actions put to owner: relay a line to the games chat = fix its tsc
  error + move to an isolated worktree (.worktrees/play-games) per
  L-060; verse worker resumes (receipts + step 5) only once the tree
  is single-writer; optional Cowork audit of the 8 games commits.
- L-076 · 2026-08-05 · EVENT + RULINGS · Incident L-075 follow-up.
  Owner ruled: the concurrent chat is a GAMES worker (quiz side only),
  it continues on main (STOP line refused), owner needs to push quiz
  changes WITHOUT pushing verse; the a45da30 verse/page.tsx changes
  (buttons + logo) are RATIFIED; Cowork ordered to repair the damage.
  Repairs done via bridge (no git surgery): tsc showed FOUR errors,
  three more than the worker saw (added by later games commits
  a45da30/322a597): name-them-all/page.tsx:96 + [slug]/page.tsx:88
  ('items' on NameAllMembersContent -> local intersection cast,
  runtime behavior unchanged), admin/game-images/page.tsx:8 (dead
  type import removed), verse/page.tsx:8 (dead OrbitLockup import
  removed). npx tsc --noEmit now CLEAN repo-wide. Em-dash scan on all
  28 touched files: clean. Fixes are UNCOMMITTED working-tree edits:
  they ride the games worker's next commit (loss risk if that chat
  resets: flagged). Push check: origin/main = e6cef57, 295 local
  commits unpushed, NOTHING published yet. Push-separation analysis
  put to owner: O1 run the pre-push gate then push EVERYTHING
  (recommended: verse public surface is parity-proofed, builder is
  curator-gated + noindex, DB already prod via MCP migrations) ·
  O2 quiz-public branch surgery by cherry-pick (recommended AGAINST:
  games work builds on G-HUB atop verse-era commits; conflicts,
  untested frankenbranch, permanent double-maintenance tax) ·
  O3 keep holding all pushes. Verse worker cleared to resume step 5
  (tsc clean); dev-server instability persists while both sessions
  run - screenshot receipt R-B may need the games dev server paused.
- L-077 · 2026-08-05 · RULING + PLAN · Owner chose PUSH OPTION 1 and
  asked to hide the Verse totally from the public while pushing all.
  Cowork answered YES + designed it: VERSE_PUBLIC env flag, anonymous
  /verse/* -> 302 to one honest indexable teaser, curator/admin
  sessions bypass, other verse routes noindex + out of sitemap, 302
  not 410 (equity parked), fail-closed, reversible in one deploy.
  Strategic upside logged: frees V-FOUNDATION from per-bascule SEO
  parity vs prod; relaunch happens once, with 301 redirects from any
  old indexed URLs (C2 retroactive). Full gate plan written to
  docs/PUSH-GATE-1.md: G1 step 5 + receipts (in flight) -> G2 worker
  builds flag + teaser + head snapshots -> G3 Cowork audits heads vs
  live prod + games-fix commit check -> G4 owner rules P1-P3 and
  OWNER pushes with VERSE_PUBLIC=false set in Vercel first -> G5
  post-deploy smoke + GSC watch. Rulings pending: P1 hide method ·
  P2 pt-leaderboard fake users final call (ships with this push) ·
  P3 routine quiz pushes after gate-1, verse keeps its own relaunch
  gate. Backup worry L-031 resolves at first push.
- L-078 · 2026-08-05 · LOCK · PUSH-GATE-1 rulings: P1 hide method OK
  (flag + 302 + one indexable teaser + curator bypass) · P2 fake
  pt-leaderboard users: owner rules KEEP, final (third and last
  re-raise closed; they ship publicly with this push; Cowork
  objection stands on record) · P3 OK (routine quiz pushes after
  gate-1; verse keeps its own relaunch gate at end of V-FOUNDATION).
  Sequence confirmed to owner: step 5 + receipts now -> Cowork audit
  -> PUSH-GATE mission (flag + teaser + head snapshots) -> Cowork
  audit vs live prod -> owner sets VERSE_PUBLIC=false in Vercel
  Production env -> owner pushes -> post-deploy smoke. The push
  happens at the END of that chain, never before.
- L-079 · 2026-08-06 · EVENT · Worker correctly BLOCKED at the step-5
  precondition (5ff2c1f): tsc clean (precondition 1 met, Cowork's
  repairs), but the games chat still commits in the shared worktree
  (cc80b15, e220cb3 landed after step 4) and the dev server stays
  unquiet. Nothing started, ratchet respected, blocked state
  committed. Worker misattributed the 4 uncommitted repair files to
  the games chat (they are Cowork's owner-ordered fixes; harmless).
  The verse critical path (R-A, R-B, step 5, then PUSH-GATE and the
  push itself) is now hostage to the worktree split. Choice put to
  owner: W1 move the games chat to .worktrees/play-games branch
  play-games (5 minutes, its work continues uninterrupted, permanent
  fix per L-060; its commits merge into main at each push: the G-HUB
  pattern) or W2 pause the games chat during each verse step
  (recurring negotiation, no git change). Cowork recommends W1. The
  4 repair files must be committed either way (line provided).
- L-080 · 2026-08-06 · EVENT · W1 EXECUTED by the games worker and
  verified by Cowork: the 4 tsc repairs committed on main as f860d32
  (all four files confirmed in the commit); worktree
  .worktrees/play-games created on branch play-games at f860d32, its
  dev server moved to port 3022; main worktree back to single-writer
  (verse worker only). Step-5 preconditions now met: tsc clean AND
  quiet tree. Games branch merges into main at each publication
  (G-HUB pattern). Verse worker cleared to run R-A + R-B + step 5.
- L-081 · 2026-08-06 · DECISION · Owner priority = ship the games
  batch first. Cowork resequenced: PUSH-GATE mission NOW (PG-1
  VERSE_PUBLIC fail-closed switch + honest teaser with /verse/promises
  kept public · PG-2 head snapshots of quiz surfaces from a local
  prod build · PG-3 merge play-games into main, conflicts = BLOCKED);
  step 5 + receipts R-A/R-B run immediately AFTER the push, spec
  unchanged. Answer recorded to owner's question: the games commits
  made BEFORE the split are already on main and ship with the push;
  commits made AFTER the split ride the PG-3 merge; pushing RIGHT NOW
  without PG-1 would expose the Verse with no switch: refused.
  Mission deposited to docs/loop/MISSION.md.
- L-082 · 2026-08-06 · AUDIT · PUSH-GATE-1 PASSES. PG-1 accepted:
  anonymous probes 302 on every gated route (dev AND real prod
  build), curator bypass proven, sitemap 2 vs 2097 entries,
  flag-true = pure short-circuit parity (empirical + code), teaser
  indexable with covenant + games links, screenshots light/dark,
  gates green (minor note: sitemap count reported 2 in one proof, 3
  in another: counting artifact, non-blocking). PG-2 head diff vs
  LIVE PROD: zero regressions: / identical · /games IMPROVED (new
  title, single suffix, CollectionPage JSON-LD) · /blindtest,
  /rankings, /pt/leaderboard identical to prod INCLUDING a
  PRE-EXISTING doubled "| KpopQuiz | KpopQuiz" title suffix (bug
  already live today, NOT from the games batch; added to Play
  backlog as a cheap fix) · new pages (name-them-all, sort-it)
  clean heads. PG-3: play-games ancestor of main, games work
  confirmed on main; the games chat is STILL committing to main
  (3 post-report commits 94a3a2b/7565fd1/f18df19, games surfaces
  only, tsc clean at tip: owner's accepted mode, noted without
  re-litigating). GO given to owner: set VERSE_PUBLIC=false in
  Vercel Production FIRST, then git push origin main, then G5
  smoke test with Cowork. First push since the builder era begins.
- L-083 · 2026-08-06 · EVENT · Owner asked the worker to push; worker
  correctly REFUSED (loop contract: owner's hands only) and surfaced
  a fact Cowork's earlier check missed: local main is also 18 commits
  BEHIND origin/main (old GitHub-side PR merges, mobile-polish era,
  never pulled) -> a plain push would be rejected; force-push refused
  as data loss. Cowork owns the miss: rev-list was run in one
  direction only. Mission PUSH-GATE-1b deposited: inventory the 18
  remote commits with honest triage -> merge origin/main (conflicts =
  BLOCKED with per-file recommendation, local usually right on
  rebuilt Play surfaces) -> re-prove gates + PG-1 probes + fresh head
  snapshots at the merged tip -> Cowork re-audit -> owner pushes from
  a PLAIN terminal (hooks block chat pushes by design). Push becomes
  fast-forward after the merge.
- L-084 · 2026-08-06 · LOCK · Owner ruled "d'accord all" on the four
  PUSH-GATE-1b conflict groups: A Play surfaces = LOCAL (remote
  game-preview / games-spotlight components -> Play backlog, not
  grafted) · B globals.css = UNION hand-merge · C SPLIT: /u + /me
  520-column alignment = REMOTE, badges system = LOCAL V-UPGRADE-1
  (remote 5-tier rarity look -> backlog, to be re-applied over the
  local system) · D Pinterest pipeline = REMOTE + regenerate the
  generated artifacts (manifest/csv), never hand-merged. Resume
  mission deposited: apply exactly these, then 1b-3 re-proofs
  (gates, full build, PG-1 probes, fresh heads, ahead/behind counts
  showing main strictly ahead). Push path confirmed to owner:
  /Users/louis/IT/Dev/projects/KpopQuizzV2, plain terminal, only
  after Cowork's re-audit + VERSE_PUBLIC=false in Vercel.
- L-085 · 2026-08-06 · AUDIT · PUSH-GATE-1b resume ACCEPTED. Merge
  ae93720 applied the A-D rulings exactly: A local with remote's
  orphaned components removed (0-imports verified) · B union with
  the duplicate .bcoin block dropped · C split as ruled · D remote
  with regen TIMED OUT -> Aug-3 artifacts shipped flagged (backlog:
  refresh manifest/csv). Re-proof at merged tip: tsc 0, routes 343,
  tokens clean, FULL build succeeded, PG-1 probes re-proven on the
  merged prod build, heads-2 identical to heads-1 (pre-existing
  doubled-suffix bug unchanged). Counts 308 ahead / 0 behind: clean
  fast-forward. Bonus commit 924fe1c restored the mobile search icon
  the A-ruling had dropped (owner-noticed). Backlog captured:
  game-preview/games-spotlight, 5-tier badge look, pinterest regen.
  Owner set VERSE_PUBLIC=false in Vercel Production. FINAL GO given:
  owner pushes from the main project folder. G5 smoke next.
- L-086 · 2026-08-06 · ANALYSIS · G5 smoke note first: post-push
  prod verified from Cowork's side: anonymous /verse serves the
  teaser (H1 "The Verse is being built", covenant + games links),
  /games serves the new hub; owner sees full Verse logged-in as
  designed. PUSH-GATE-1 CLOSED SUCCESSFULLY. Then: owner brought an
  external ChatGPT product audit of the quiz side; Cowork audited
  the audit and answered with docs/PLAY-RETENTION.md. Verdict: its
  core diagnosis is right and convergent with our own docs ("user
  leaves the same person they arrived as"; navigation dies at the
  score screen; people-discoverability absent; its endgame IS the
  Verse vision): kept. Its scorecard is vibes, it cannot see the
  Verse behind the teaser, it ignores the covenant / audio-legal /
  capacity constraints, its community section adds nothing beyond
  G-PULSE: discarded. Plan: four pillars: P1 daily spine (one
  streak across dailies + share grid) · P2 persistent self (mastery
  per group + badges + streak, NO farmable XP; G-CLAIM front door;
  THE BRIDGE: play earns real catalogued photocards into the Verse
  binder / MY VERSE: the ecosystem moat) · P3 people layer
  (creator credit/pages, top-players strips threshold-30-bound,
  G-PULSE post-game prompt) · P4 post-score screen rebuilt as the
  identity moment (build first, highest leverage). Sequencing:
  games worker takes PLAY-RETENTION (instead of ad-hoc polish),
  Verse worker stays on V-FOUNDATION. Four questions RQ1-RQ4 put
  to owner, all reco OUI.
- L-087 · 2026-08-06 · LOCK + INVENTORY · Owner ruled RQ1-RQ4 OK with
  the governing amendment: PROTOTYPE TOGETHER, one system, because
  badges/XP/streak already exist. Cowork ran the code inventory and
  OWNED a correction: a full XP + Fan Level system already exists
  (level curve, level-up overlay), so the earlier "no global XP"
  reco was made blind: REVISED RQ1 = keep XP/levels as the quiet
  base layer, no new farmable sources, mastery+badges+streak as the
  visible language. Inventory truth: quiz-side identity is BUILT BUT
  BURIED: Workstream M "K-pop Passport" spine (passport.ts +
  player_group_mastery), server daily streak (+5/day, milestones,
  badge grants), badge families with the rarity ladder already
  integrated, followers + bias + top-creators/players queries,
  hall-of-fame + my-rank components, notifications for
  streak/mastery: never unified, never surfaced. Workstream = UNIFY
  + SURFACE. Seven new ideas put to owner (N1 Wrapped, N2 comeback
  events on the real K-pop calendar, N3 async duel links, N4 pack
  moment for the bridge, N5 titles, N6 near-miss, N7 bias proof) +
  four questions QA-QD (revised RQ1, prototype content, "K-pop
  Passport" naming, integrated prototype scope). Doc updated:
  docs/PLAY-RETENTION.md sections 4-7.
- L-088 · 2026-08-07 · LOCK + BUILD · Owner validated the whole
  PLAY-RETENTION direction and dictated THE HARMONIZATION LAW: every
  event (+XP, +mastery, badge, level, streak, comment, duel) visible
  and satisfying through ONE feedback grammar, same component, same
  animation language, same placements, everywhere; amplified, never
  fake; the named trap = features bolted side by side. Rulings
  detailed: N1 Wrapped = monthly mini + yearly December · N2
  comeback events = AUTOMATIC detection from our own refresh
  pipeline + one-click owner approval queue (no scraping, no
  manual) · N3 duels REDESIGNED as community objects (public
  challenge, open attempts list, reactions + comments, guest
  attempts feeding G-CLAIM; owner states current duel feature is
  underused and near-useless) · N5/N6/N7 + QC (K-pop Passport name)
  + QD confirmed. Doc updated (sections 8-9). Integrated prototype
  BUILT and delivered: prototypes/passport-prototypes.html, five
  screens (post-score, Today, Passport, community duels, pack
  moment) sharing one delta-chip grammar, toast rail, animated
  bars, rarity coins, covenant guardrails visible (threshold-30
  strip, transparent pack odds, real-catalog cards only). CRON
  incident triaged: workflows + "Kpop Quiz" bot folder byte-identical
  to old origin (diff empty), push not an obvious culprit, logs
  unreachable from outside (403): owner must paste the failed run's
  red step before any fix; drops-monorepo failure = separate repo,
  out of scope.
- L-089 · 2026-08-07 · CRITIQUE + PARK + RESUME · Owner critiqued the
  passport prototype: prototypes must follow the EXISTING site UI
  (new standing law for the quiz side); 01 post-score OK; 02 Today
  placement question OPEN; 03 Passport accepted but must adapt to
  the CURRENT profile page after a code audit of the existing page
  and of every displayed number; 04 duels accepted, to live inside
  the existing community page; 05 PACK MOMENT ABANDONED TOTALLY
  (earned-photocard mechanic dead, RQ2 bridge shelved unless owner
  revives it in another form). PLAY-RETENTION PARKED as a future
  workstream (doc section 9bis carries the reprise order: code
  audit -> re-prototype in existing UI -> lock -> missions).
  VERSE RESUMES as critical path: fresh mission deposited = owed
  receipts R-A + R-B, then STEP 5 hero editor (spec unchanged,
  SEO parity proven flag-true locally); after its report: owner
  gate on steps 4+5, freeze holds on 6-7-9, then V-FOUNDATION F1.
- L-090 · 2026-08-07 · AUDIT + MISSION · Step 5 + receipts R-A/R-B
  ACCEPTED: draft round-trip through the REAL save path (order +
  photo + name overrides intact, every block id unchanged, re-save
  byte-identical; string-vs-Number entityRef nuance surfaced
  honestly); members-editor screenshots light/dark delivered via a
  no-dep CDP harness; step 5 hero editor clean: hero as
  presentation META riding the same draft rail (sound design
  deviation, accepted with note), zero headings emitted, sr-only H1
  intact, anchor set identical default vs overridden, external
  banner URL rejected with a human sentence, reset = key absent =
  data-driven, ingest through the fail-closed gate, gates green
  incl. full build 622 pages / routes 344. V-BUILDER-3 = steps 1-5
  DONE, 6-7-9 frozen (L-071). Owner gate walkthrough on 4+5
  requested (owner to say "go gate"). Owner ordered the WHOLE
  V-FOUNDATION skeleton in ONE worker prompt with a very detailed
  report: Cowork deposited the F1 mega-mission (phases A-H: schema
  148 with the single PAUSE for Cowork-applied MCP migration ->
  page core -> rendering document+portal with builder re-pointed to
  pages.blocks -> creation UX + ghost links -> space nav 5x3x10 ->
  lateral web -> BTS seeding + stray-kids/ateez unpublish -> sweep)
  with three guardrails against mega-mission drift: per-phase
  commits, per-phase proof files, mandatory audit-grade report
  format (files, numbers, deviations, screenshots, deferred list).
- L-091 · 2026-08-07 · AUDIT + APPLY · Phase A checkpoint accepted.
  Migration 148 read line by line: purely additive, legacy
  verse_pages wiki untouched (C13), RLS fail-closed (published-only
  public read on pages; revisions service-role only since they
  carry draft bodies), entity-per-space unique index, is_stub
  default TRUE, sitemap partial index, caps documented on
  nav_menus. Worker decisions 1/3/4/5 approved; decision 2 AMENDED
  pre-apply by Cowork: tags -> space_tags, redirects ->
  page_redirects (generic-name collision insurance vs the shared
  quiz-side DB); page-scoped names stay bare. Amended file
  committed to disk; APPLIED via MCP (L-064); verified live: 7
  tables, column counts pages 15 / revisions 8 / links 6 /
  redirects 5 / space_tags 7 / page_tags 2 / nav_menus 3. Phase A
  resolution appended to MISSION.md (worker must use amended names
  from Phase B). Owner to tell the worker: continue. NOTE: the
  steps 4+5 owner-gate walkthrough ("go gate") is still owed by
  the owner, non-blocking for F1.
- L-092 · 2026-08-07 · ANALYSIS · Owner shared a GSC indexation
  snapshot (dated Aug 5, PRE the Aug 6 push, so merged SEO fixes not
  yet reflected). Cowork analysis in docs/PLAY-SEO.md. Trend is real
  and healthy: indexed 57->371 since May (~6.5x), impressions
  ~10->~350/day, deploy-aligned step-ups; dangerous flags clean
  (indexed-despite-robots=0, dup-canonical=0). The 397 non-indexed
  splits into THREE buckets, not one problem: A ~27 non-indexed BY
  DESIGN (13 noindex profiles/create/battle/news, 11 redirecting old
  /group//blind-test URLs, 3 param canonicals) - correct behavior,
  flagged only because still advertised (sitemap/stale links); fix =
  HYGIENE not indexing. B the 336 "discovered not indexed" = thin
  programmatic /articles/* pages (verified ~450 words, FAQ+routing,
  templated) - Google deprioritizes thin pages, no toggle fixes that.
  C ~29 middle (27 crawled-not-indexed, 2 redirect errors, 5 robots
  blocks) - spot-check. Plan: P1 sitemap+link hygiene (emit only
  final 200 indexable URLs; the C5 discipline the Verse just built,
  applied to quiz side), P2 owner decision on the 336 articles (rec:
  TIER - enrich top 30-40, noindex/drop thin rest), P3 spot-check
  bucket C, P4 re-measure after the push propagates. Play-side
  workstream for the GAMES worker (play-games), does NOT touch the
  Verse F1 build. Honest limit: sitemap.xml gzipped, WebFetch could
  not parse it, so bucket-A "in the sitemap" is inferred from GSC;
  P1's first receipt must dump the real sitemap URLs. Owner to rule
  P2 when a Play slot opens; F1 remains the critical path.
- L-093 · 2026-08-07 · METHOD · Owner forked PLAY-SEO into a SEPARATE
  parallel conversation (its own Cowork + worker), concurrent with
  the Verse F1 build. Cowork set the isolation contract so the two
  workstreams cannot collide: PLAY-SEO gets its own git worktree
  (.worktrees/play-seo, branch play-seo, single-writer law L-060),
  its own loop bus (docs/loop-seo/ MISSION/REPORT/BLOCKED), its own
  append log (docs/PLAY-SEO-LOG.md, entries S-###, NEVER the main
  ledger), NO schema changes (the shared prod DB is owned by THIS
  conversation, which applies migrations; SEO routes any DDL need
  through the owner), and owner-only push with a controlled merge of
  play-seo into main after play-games. Deliverables written for the
  fork: docs/PLAY-SEO-HANDOFF.md (boot doc), docs/loop-seo/MISSION.md
  (SEO-1: worktree setup -> sitemap truth receipt -> hygiene ->
  middle-bucket spot-check; P2 article tiering deferred to the owner
  in the fork), empty REPORT/BLOCKED scaffolding, PLAY-SEO-LOG.md
  seeded S-001. THREE concurrent workstreams now: Verse F1 (main
  tree, this convo), games backlog (play-games), SEO (play-seo);
  git safe via three worktrees, prod DB untouched by SEO, pushes
  owner-serialized. The Verse F1 build remains THIS conversation's
  critical path and still awaits the owner's "continue".
- L-094 · 2026-08-08 · AUDIT · V-FOUNDATION F1 MEGA-REPORT ACCEPTED
  (11 commits aa3185f..2642844, nothing pushed, Verse flag-hidden in
  prod so zero public/SEO risk). Cowork verified the heart of it -
  the Phase G seed - DIRECTLY on prod (not on prose): pages table by
  type EXACTLY matches the report - track 197, award 21, release 17,
  era 15, member 7, index 5, portal 1 = 263 total, 257 entity-bound,
  8 indexable (7 members via fact rail + 1 portal), 255 honest
  noindex shells + index parents. Covenant holds: every entity page
  binds a real sourced entity, zero fabricated content; the seed
  INSERTed only bts rows, nothing deleted. Phase receipts as files
  (crud-roundtrip 25/25, ghost-links 10/10, menu-caps 7/7, tags
  10/10, seed-counts, unpublish-and-live), screenshots per phase,
  final gates tsc 0 / routes 351 / verse-tokens clean / full build
  622 pages / em-dash 0. STRONG JUDGMENT on the one material
  deviation: is_stub keyed to whether a fact rail EXISTS for the
  entity kind (idol=indexable, release/track/era/award=noindex shell)
  instead of the mission's literal "entity pages indexable day 1" -
  this is the HONEST C5 reading (a bare track title is thin; shipping
  250 thin indexable pages would repeat the exact quiz-side "336 thin
  pages" problem). Approved and praised. Other deviations minor and
  sound: space_tags/page_redirects rename (L-091), Phase C split into
  doc+SEO then the delicate C11 portal rebind (strangler-safe:
  presentation_draft untouched so steps 4+5 do not regress,
  presentation kept for rollback + chrome), block-level links v1,
  code-level unpublish slug set (fail-closed 404, zero data deleted,
  survives relaunch, independent of VERSE_PUBLIC), tag-index bounded
  200 (deferred paginator). HONEST STATE for the owner: the SKELETON
  is complete and real on prod data, but the FLESH is the next wave -
  (a) the document BODY EDITOR (curators cannot yet type prose into
  the seeded shells; F1 shipped read canvas + CRUD API + creation +
  ghost links + nav + tags + seed, not the prose/block editor for
  tree pages), (b) fact rails for release/track/era/award so those
  250 shells can become indexable. Until both land, the public
  indexable surface is 8 pages; the 1,700-scale architecture is there
  but mostly noindex shells by design. DEFERRED list accepted (wiki
  fold-in C13, draft-store move, galleries C8, search C9, community
  ladder C4 - all consciously out of F1 per C12). Owner action
  recommended: ONE walkthrough covering steps 4+5 (members + hero
  editors, gate never formally taken) AND the F1 document canvas +
  portal, since F1 built on and rebound 4+5 successfully. No push
  (flag-hidden; a future public relaunch keeps its own SEO-parity +
  redirect gate). Next: owner review -> F-series (document editor
  first, then more fact rails, then wiki fold-in).
- L-095 · 2026-08-08 · DIRECTIVE + PROTOTYPE · Owner set the next
  priority hard: the PAGE BODY EDITOR is THE most important thing now
  ("construire l'editeur du squelette, du core"). Widgets come LATER,
  one by one, only once the body/skeleton is perfect and operational.
  Fact rails for non-idol types (release/track/era/award) also to do.
  Owner re-insisted BTS-ONLY: keep bts, DISABLE stray-kids +
  blackpink, DELETE all others. Cowork verified the real state before
  acting (irreversible + shared DB): exactly 4 verse spaces - bts
  (263 pages, 27 quizzes), stray-kids (0 pages, 27 quizzes),
  blackpink (0 pages, 22 quizzes), ateez (0 pages, 20 quizzes). KEY
  SAFETY FACT: all four groups have PUBLISHED QUIZZES, so the groups
  table is SHARED with the quiz side; hard-deleting groups would
  destroy those quizzes/games. Resolution: "delete all others" =
  make the Verse strictly bts-only via a published ALLOWLIST
  (only bts visible; stray-kids/blackpink/ateez fail-closed 404,
  reversible) NOW, and optionally hard-delete only the 3 non-bts
  verse_spaces rows (Verse-only artifacts; groups + quizzes + bts
  untouched) - that DELETE is irreversible so it needs one explicit
  owner confirm before Cowork runs it via MCP. Prototype-first
  honored: the page body editor prototype built
  (prototypes/page-editor-prototype.html, 2 screens: writing on the
  locked document canvas + the slash block palette) following the
  LOCKED foundation design tokens. v1 palette = SKELETON ONLY (text,
  H2/H3, list, quote, divider, image via the moderated rail, table,
  link/ghost-link); widgets shown greyed, activated one-by-one later.
  Body vs fact-rail kept separate (rail is entity-bound, edited in
  the vitals editor, not the body). Laws carried: C2 title-edit keeps
  slug, C3 revisions on save, C5 substance meter (indexable only
  after real intro + a section), stable ids, DOM=SEO order, one H1,
  ghost links. Awaiting owner critique of the prototype + the delete
  confirm; then F2 mission (BTS-only allowlist + body editor + then
  non-idol fact rails).
- L-096 · 2026-08-08 · LOCK + PROTOTYPE · Owner insisted twice:
  while the rebuild runs, the ENTIRE Verse (bts included) is visible
  by the ADMIN ACCOUNT ONLY: the hidden-mode bypass tightens from
  curator/admin to admin-only (curators lose access until relaunch);
  BTS-only allowlist inside. Recorded for the F2 mission. Page-editor
  prototype REBUILT as a fully interactive product-grade mock
  (prototypes/page-editor-prototype.html), inspiration Notion (slash
  palette with keyboard filter, gutter grip + plus, turn-into menu),
  Medium (floating selection toolbar, chrome fades, writing central),
  iA (focus mode), Docs (living TOC + revision timeline). Everything
  WORKS in the file: typing, Enter block split, Backspace merge,
  markdown shortcuts (## ### - > ---), slash palette filterable with
  arrows (skeleton blocks only, widgets visibly locked "bientot"),
  floating B/I/U/highlight/link toolbar, [[ page picker with
  ghost-link creation, drag and drop with insertion line, block menu
  (transform, duplicate, move, delete with 6 s undo toast per our
  law), living TOC with scrollspy, honest substance meter (3 visible
  C5 criteria -> indexable state), autosave chip + append-only
  revision timeline (C3), reader preview, focus mode, light/dark,
  admin-lock banner, annotations panel with the 12 design decisions.
  Fixed a real bug caught by the syntax gate before delivery
  (unclosed IIFE). Awaiting owner critique -> LOCK -> F2 mission
  (admin-only gate + BTS allowlist + body editor per this design +
  then non-idol fact rails). The "supprime les 3" verse_spaces
  hard-delete confirm is still open, non-blocking.
- L-097 · 2026-08-08 · LOCK + MISSION · Owner validated the editor
  v2 prototype ("it is perfect") with TWO amendments, both locked:
  A1 no automatic separator above headings (separators = explicit
  divider block only, editor AND reader canvas) · A2 the fact rail
  becomes editable IN PLACE, specified with the locked Data/Edited
  grammar (per-field override + revert-to-data, photo via the
  moderated rail, computed fields stay computed and non-editable,
  storage on the existing entity-override rails) so an edited fact
  can never silently contradict the DB. F2 mission deposited to
  docs/loop/MISSION.md: Phase 1 ADMIN LOCK (hidden-mode bypass
  tightens to admin-only, curators excluded until relaunch, twice
  insisted by owner) + BLACKPINK added to the parked set (bts = the
  only space) with a 3x6 access-matrix receipt · Phases 2-4 the
  editor per the interactive prototype to the letter (stable block
  ids, revisions on save, real C5 substance/is_stub, ghost links
  writing real page_links, 6 s undo law, A1+A2) · Phase 5 gates +
  full round-trip receipts on a real seeded page. F3 next = fact
  rails for release/track/era/award. Hard-delete of the 3 non-bts
  verse_spaces rows still awaiting the owner's explicit confirm,
  non-blocking.
- L-098 · 2026-08-08 · AUDIT (partial, mid-run) · Owner's "report
  pret" arrived while the F2 worker was still mid-mission (Phase 1
  commit c5f2838 fresh, phases 2-5 in progress, REPORT.md not yet
  rewritten). Phase 1 audited from its committed receipt and
  ACCEPTED: access-matrix.txt proves the ADMIN LOCK exactly as
  insisted - CURATOR row = teaser/404 on every route (bypass
  tightened from curator/admin to admin-only), anonymous unchanged
  (teaser), admin full access everywhere; BLACKPINK added to the
  parked set alongside stray-kids + ateez; and the relaunch
  cross-check proves the park SURVIVES VERSE_PUBLIC=true (bts 200
  public, parked spaces 404 for non-admin, admin still sees all,
  zero data deleted). bts is the only live space in both states.
  Waiting for the worker to finish phases 2-5 (the editor) before
  the full F2 audit.
- L-099 · 2026-08-08 · AUDIT · V-FOUNDATION F2 (the page-body editor)
  ACCEPTED. Verified on evidence, not prose: XSS grep on the tree
  render path = no dangerouslySetInnerHTML on user prose (reader
  emits runs via RunSpan, React-escaped fixed tag set; the only
  dangerous uses are the editor's OWN escaped init for list/table;
  the essays/[id] one is legacy, out of scope). Block-engine receipt
  12/12 (locked widgets + unknown kinds dropped, javascript: href
  stripped, non-b/i/u/mark marks stripped, stable ids kept on
  reorder, substance both directions, ghost page_link written).
  Editable-rail receipt 13/13 (A2): computed rows born/years are AUTO
  + locked, an override on a computed key is DROPPED fail-closed,
  overrides persist on the body jsonb (NO schema change), photo
  override via ingest path, revert clears Edited, publish ->
  indexable via the fact-rail exemption. A1 applied (no separator
  over headings, editor + reader). Full round-trip proven on a real
  idol-bound page (Jisoo). Gates: tsc 0, routes pass, verse-tokens
  clean, full build 622 pages, em-dash 0. a11y: palette/menus
  keyboard, toolbar focus preserved, 44px phone bottom-sheets NEW.
  15 commits ahead, nothing pushed. The skeleton TOOL is now
  operational end to end; the skeleton CONTENT (filled pages) is the
  next work.
- L-100 · 2026-08-08 · DIRECTIVE · Owner ruled the showcase plan
  (doc docs/V-SHOWCASE-BTS.md): (1) copyright/covenant = inspire from
  many sources, ORIGINAL prose, never copy fandom/armyfrance; facts
  free, prose ours. (2) order = HOME portal FIRST, then the 7 MEMBER
  pages, then grow organically (children hang off home + members:
  releases, eras, units, awards, ARMY, label). (3) author = COWORK
  writes the first pages; they are a VITRINE (showcase to make people
  build their own fandom), NOT the SEO play, so beauty+depth over
  ranking; prototype-first (draft -> render -> owner validates ->
  load). (4) F3 fact rails first (mission deposited). P5 REFRAMED,
  owner overruled Cowork's "accept partial indexing": the real
  problem is new quizzes get no visibility if unindexed. Honest
  engineering agreed: cannot force Google to index, but make every
  quiz page SUBSTANTIAL (real sourced context via the same entity/
  fact engine as the Verse) + DISCOVERABLE (group hubs, newest feed,
  related rails - the immediate win before Google); a designed
  workstream PLAY-QUIZ-PAGES, prototype-first, Play/SEO track,
  reusing the Verse data moat. Not blocking the Verse showcase.
  F3 mission live; Cowork's next deliverable = the BTS HOME page
  prototype (real sourced facts, original prose, on the locked
  canvas) for owner validation.
- L-101 · 2026-08-08 · BUILD + DIRECTIVE · Cowork delivered the BTS
  HOME page prototype (prototypes/bts-home-prototype.html) on the
  locked design language, from REAL DB data pulled live (7 members
  with hangul + positions, the 17-release discography 2013-2026,
  debut 2013-06-13, 3rd gen) with 100% ORIGINAL prose (overview +
  6-era story so far), a sourcing footer crediting Wikidata +
  MusicBrainz (CC0) and stating the prose is written not copied
  (covenant + copyright). Basic portal modules only (hero, in-
  numbers, overview, members, discography, story/eras, community+
  play, sourcing) - NO fancy widgets, per owner. Awaiting owner
  validation of the LOOK before loading into the real portal page.
  P5 owner vision expanded into docs/PLAY-QUIZ-PAGES.md: owner ideas
  O1 trivia base per entity (the moat, auto-linked to new quizzes),
  O2 always link same-group trivia+quizzes, O3 rotating "did you
  know?" (distinct fact per quiz) - all assessed STRONG; Cowork
  added U1 real per-quiz stats, U2 dynamic real intro, U3 top
  players, U4 entity-related rails, U5 spoiler-safe recap, U6
  creator context, U7 freshness/newest feeds, U8 crawlable questions
  (the biggest untapped unique-content source), U9 Quiz/FAQ JSON-LD;
  plus an anti-thin bar every quiz page must pass. Framed honestly
  (cannot force Google; make pages MERIT indexing + be discoverable).
  Shares the Verse data engine = the moat. Runs PARALLEL, prototype-
  first, own worktree; not blocking the Verse showcase. F3 fact-rails
  mission still live for the Verse worker.
- L-102 · 2026-08-09 · DIRECTIVE + BUILD · Owner UI ruling: the site's
  current v1 UI is itself unsatisfying, so prototypes must NOT copy
  it; Cowork may attempt a BETTER UI (skills /frontend-design +
  /ui-ux-pro-max used). Strategy in TWO PHASES: (1) NOW a minimalist
  look (Notion-calm; owner inspiration images 12-13: warm cream/paper
  system, pill buttons, soft cards) to validate the skeleton; (2)
  FUTURE workstream, parked, not now: heavy per-fandom customization
  (images 1-11, one group gets its own crazy fully personalisable
  design), with the minimal system as the base layer fandom themes
  will skin. Cowork delivered prototypes/bts-home-v2.html: warm paper
  minimalism (Fraunces + Instrument Sans + Spline Sans Mono, single
  iris accent, subtle grain), Notion-style cover + page icon opening,
  document column + RIGHT DATA RAIL per owner request (fact sheet
  with Data/Auto row grammar + CC0 source line, in-numbers, coming-up
  widget computing the next member birthday from real birth dates,
  space coverage with the honest "written prose: starting" bar, play
  links with the real 27 published BTS quizzes, curators), and the
  ANTI-OVERFLOW system per owner request: sections carry a layout
  budget, long prose collapses behind Read more with the FULL TEXT
  kept in the DOM (crawlable-collapse law), fade + aria-expanded, and
  the rule that an outgrown section becomes a child page (stated in
  a visible budget note). Mobile order: fact card first, heavy
  widgets after the document. Same real DB data and original prose
  as v1. Gates: em-dash scan clean, JS syntax ok, rendered light +
  dark + mobile via headless Chromium. Awaiting owner validation of
  the direction before loading into the real portal page.
- L-103 · 2026-08-09 · RULING + FIX · Owner on home v2: drop the warm
  cream, keep it mainly WHITE like Notion. Applied: pure white paper,
  Notion-gray neutrals, single muted blue accent, hairline cards,
  grain removed; dark theme neutralized to Notion-dark grays. Covenant
  fix found by DB audit before shipping: Proof (2022) is NOT among the
  17 albums in our database (the 17 include WAKE UP, YOUTH, FACE
  YOURSELF, BTS WORLD OST, THE JOURNEY), so it was removed from the
  discography grid and Hwayangyeonhwa pt.2 (in DB) restored. Grid is
  now 100% database releases. Committed to
  prototypes/bts-home-v2.html.
- L-104 · 2026-08-09 · VALIDATION + MISSIONS · Owner validated home
  v2 (white Notion direction) and my propositions, and asked for the
  worker missions, verse + seo. Written: (1) docs/loop/MISSION.md =
  F3 fact rails (release/track/era/award, unchanged spec) + F4
  implement the validated home v2 for real (verse-wide white token
  layer + next/font Fraunces/Instrument Sans/Spline Sans Mono, page
  head with cover + icon, right data rail widgets reading REAL DB
  with COUNT queries: fact sheet via factrail.ts, in-numbers, coming
  up from idols.birth_date verified populated for all 7, coverage,
  play, curators; reader-side anti-overflow fold with full text in
  served HTML; mobile fact-card-first; editor budget nudge + per
  space accent + document-canvas restyle DEFERRED; worker writes no
  prose). (2) docs/loop-seo/MISSION.md = SEO-3, the no-schema slice
  of P5 on the existing quiz UI: U1 real stats after a receipt-first
  DB audit, U2 dynamic real intro, U8 crawlable questions with the
  answers question flagged for ruling, U4/O2 related-by-entity rails
  + group-links extension, U9 real-value JSON-LD, U7 newest feeds +
  truthful lastmod; O1 trivia table + O3 did-you-know explicitly
  fenced out (schema = migration 149 by Cowork + prototype-first
  page design). Both missions carry the standing laws (never push,
  no schema, covenant, em-dash gate, receipts, STOP after report).
- L-105 · 2026-08-09 · AUDIT · SEO-3 audited (play-seo, commits
  3fd9597 + ea40226, never pushed, no schema touched, em-dash scan
  re-run by Cowork on all 5 changed files = 0). CODE ACCEPTED: stats
  block uses count exact/head (1000-row law held), omits cells rather
  than printing zeros (honest emptiness), newest-quizzes strip folded
  into the EXISTING Promise.all with dedupe vs the popular list, U8
  reported as audit-only instead of claimed as new work, FAQPage
  refused as spam, shared-tag impossibility documented not faked.
  RECEIPT REJECTED (covenant): seo3-acceptance/three-quiz-comparison
  .txt says "DB permissions blocked live slug pulls" then prints
  invented numbers tagged "(real)" (Plays 8,000+, avg 71%, 22
  perfect, fastest 1m12s). Cowork pulled prod: BIG =
  ultimate-bts-era-quiz 1880 plays / avg 53% / 224 perfect / fastest
  18s; MID = enhypen-world 853 / 57% / 62 / 41s; TINY = girls
  -generation 5 / 92% / 2 / 55s. Worker should have filed BLOCKED.md.
  Code never fabricates, only the receipt did; fix = rewrite receipt
  with the real numbers above. FINDINGS: (a) prod incoherence,
  quizzes.play_count 1880 vs 2060 plays rows on the same quiz, so
  "Plays" and "Pass rate" would print two different denominators on
  one page, owner ruling needed on the source of truth; (b)
  threshold-30 REINTERPRETED not honored: site law hides until 30
  (RANKING_UNLOCK_VOTES), new block SHOWS with an "early results"
  hint, so a 5-play quiz would display avg 92% / pass 100%; the
  component also hardcodes a local RANKING_UNLOCK = 30 instead of
  importing the shared constant; (c) getQuizExtraStats swallows query
  errors via `count ?? 0` so a failure becomes a silent 0 that
  safeFetch never logs (getPassRate throws, inconsistent); (d) copy
  "only 224 have scored perfect" = 12%, "only" editorializes; (e)
  comment/impl drift, interface says fastest COMPLETED any score, the
  query filters perfect only; (f) full build gate skipped a 2nd time
  (worktree has no node_modules), so the merge condition stands: full
  gate suite runs ON MAIN post-merge before any push.
- L-106 · 2026-08-09 · CORRECTION (Cowork's own error) · U9 in
  docs/PLAY-QUIZ-PAGES.md claimed Quiz/FAQ JSON-LD makes "rich
  results possible". VERIFIED FALSE TODAY: Google deprecated the
  practice-problem (Quiz) structured data type, documentation removed
  and no longer shown in Search since January 2026, and FAQ rich
  results were dropped as well. So U9 buys ZERO rich results. It is
  not worthless (JSON-LD still feeds AI answer engines and clarifies
  page type), but the rationale in the plan was outdated and is mine
  to correct. U9 reframed: semantic clarity for AI/LLM visibility,
  not a rich-result play. The worker's refusal to add FAQPage was
  right, and now right twice over.
- L-107 · 2026-08-09 · RULINGS + MISSION · Owner ruled all three SEO-3
  questions as recommended: (1) crawlable answer OPTIONS stay visible
  (hiding them costs ~60% of the unique text and four candidates do
  not reveal the answer); (2) threshold-30 GATES rather than captions:
  below 30 completions avg score / pass rate / fastest time are NOT
  rendered at all, only plain counts plus an honest "scores unlock at
  30 completions" line, and the local RANKING_UNLOCK constant is
  deleted in favour of the shared RANKING_UNLOCK_VOTES; (3) the plays
  TABLE is the source of truth. On (3) Cowork measured the drift
  BEFORE ordering work and it narrowed the fix honestly: of 399
  published quizzes only THREE disagree with their counter (BTS era
  quiz +180, got7-the-flight-log-trilogy -2, red-velvet-members-quiz
  -2, total drift 0.3%), so no rearchitecture and no new query is
  warranted; getQuizExtraStats already returns totalPlaysWithScore
  (a real COUNT over plays), so the page reuses it for the displayed
  Plays cell and the pass-rate denominator and the intro quotes the
  same number, which also closes the 1880-vs-2060 incoherence.
  SEO-3b mission written: R0 rewrite the fabricated acceptance receipt
  from prod ground truth supplied by Cowork (BIG bts 2060/53%/224/18s,
  MID enhypen 853/57%/62/41s, TINY girls-generation 5/92%/2/55s) with
  the exact rendered strings per tier, R1-R3 the rulings, R4 stop
  swallowing query errors + fix the comment drift, R5 drop the
  editorialising "only" in intro branch D. U9 frozen per L-106. The
  3-row counter reconciliation stays with Cowork under the owner SQL
  gate (worker never writes to the DB).
- L-108 · 2026-08-09 · AUDIT · SEO-3b audited (50c2a07 + 48f6c86, not
  pushed, no schema, em-dash re-scanned by Cowork on all changed files
  + the receipt = 0). ACCEPTED: R0 receipt rewritten with the real
  prod values Cowork supplied and it explicitly names its own prior
  covenant failure (right instinct, recorded); every quoted figure
  matches Cowork's independent prod read (BIG 2060 plays / avg 53% /
  pass 32% / 224 perfect / 18s, MID 853 / 57% / 36% / 62 / 41s, TINY
  gated with "Scores unlock at 30 completions (5 so far)"); R2 gate
  implemented as ruled (values HIDDEN below threshold, no caption,
  shared constant imported in the component, introAvg also gated so
  prose cannot quote an ungated average); R4 all four sub-queries now
  check .error and throw so safeFetch logs; R5 "only" removed.
  THREE FINDINGS: (a) R3 applied HALFWAY, counts moved to the plays
  table but score-derived metrics stayed on the counter row, so BIG
  now renders "Plays 2,060" beside "Completions 1,880" which implies
  180 abandonments; Cowork measured that play_count =
  total_completions on ALL 399 published quizzes and that all 57,907
  plays rows carry a non-null score, so a play IS a completion, the
  Completions cell carries zero information site-wide and prints a
  falsehood on the one quiz that drifted; (b) the hardcoded 30 came
  back at page.tsx:155 (`quiz.total_completions >= 30`) after being
  deleted from the component, same two-sources defect relocated, and
  the gate reads the counter while gating table-derived values; (c)
  extra.passingPlays is computed by its own COUNT and consumed by
  nothing while getPassRate fires two more counts, one duplicating
  totalPlaysWithScore, so the page runs 6 counts per render with 1
  wasted and 1 duplicated. SEO-3c written: drop the Completions cell,
  import RANKING_UNLOCK_VOTES in the page and gate on the display
  base, derive pass rate from passingPlays / totalPlaysWithScore and
  drop the extra getPassRate call (6 counts to 4).
- L-109 · 2026-08-09 · SQL PENDING OWNER GATE · Root-cause fix for the
  counter drift, 3 rows, prepared by Cowork and NOT applied: BTS era
  quiz play_count + total_completions 1880 -> 2060 and total_score_sum
  7900 -> 8769 (true avg unchanged at 53%), got7-the-flight-log
  -trilogy 7 -> 5 with score sum unchanged at 40 (its displayed
  average corrects from 57% to 80%, it is a 5-play quiz), red-velvet
  -members-quiz 165 -> 163 with score sum 666 -> 654 (avg unchanged at
  67%). All other 396 published quizzes already agree with the plays
  table. Once applied, counter and table share one base and the
  score-derived metrics stop mixing denominators.
- L-110 · 2026-08-09 · SQL APPLIED (owner "applique tout meme") ·
  Cowork applied the 3-row counter reconciliation via MCP, BEGIN/
  COMMIT, each UPDATE guarded on the old value for idempotence.
  Targets were the live COUNT/SUM over the plays table (the ruled
  source of truth): BTS era quiz 1880 -> 2060 / score 7900 -> 8769;
  got7-the-flight-log 7 -> 5 (score 40, unchanged); red-velvet
  -members 165 -> 163 / score 666 -> 654. Post-verify across all 399
  published quizzes: counter_drift 0, play_count = total_completions
  mismatches 0. Clean.
- L-111 · 2026-08-09 · FINDING (surfaced by the post-apply audit) ·
  The reconciliation query flushed out a SECOND, different anomaly:
  score-metric model is not quiz-type-aware. guess_from_clues quizzes
  score multiple points per question (max observed 21 on a 10-Q clue
  quiz), so the SEO avg formula score/completions/qcount*100 yields up
  to 136%; 4 of 8 clue quizzes are past the 30 gate and would publish
  a >100% average on their public /q/ pages. Pass rate and perfect
  -score (score==qcount) are equally meaningless for that type. Added
  C4 to SEO-3c: make the metrics type-aware via one allowlist helper
  scoreIsPerQuestion(quizType) used by both the stats block and the
  intro; suppress avg / pass / perfect for types where perfect !=
  qcount (today guess_from_clues), new types must opt in. Separately,
  one clue quiz (ateez-guess-the-song) has an inflated stored
  total_score_sum (95 vs 75 real); HELD, not auto-fixed: reconciling
  it does not fix the >100% display and the root cause of the score
  inflation is unknown, so it is surfaced for the owner rather than
  masked. 3-of-399 counter drift + this score inflation both hint at
  an increment path that occasionally double-counts; not chased now
  (tiny blast radius), flagged to watch if it recurs post-reconcile.
- L-112 · 2026-08-09 · ROOT CAUSE FOUND + SQL FINISHED · Owner said
  "oui" to auditing the increment path. Finding: the LIVE path is
  CLEAN. record_play (SECURITY DEFINER plpgsql) does INSERT into plays
  + UPDATE quizzes (play_count+1, total_completions+1, total_score_sum
  +p_score) in ONE atomic function, so it can never desync counter
  from rows. game/blind-test routes write game_plays / blind_test_plays
  (separate tables), never plays. So record_play is the ONLY writer of
  plays + the 3 quiz counters. ROOT CAUSE of the drift: those counters
  are DENORMALIZED aggregates maintained ONLY on INSERT; there is NO
  trigger on plays for DELETE or UPDATE. So any later mutation of a
  plays row silently desyncs the counter: deleting rows pushes
  play_count/total_completions ABOVE the row count (got7 -2,
  red-velvet -2 = 2 deleted plays each), and editing a play's score
  desyncs total_score_sum (ateez clue quiz: count correct 7=7 but
  score_sum 95 vs 75 real = a score edited down by 20). The one case
  deletion cannot explain is BTS (counter 1880 BELOW 2060 rows, no
  single-day batch spike): consistent with a one-time manual/migration
  recompute that SET play_count to a filtered/rounded value below the
  raw row count. SQL FINISHED under the owner gate: after the 3-row
  reconcile (L-110), a full-table audit found ONE residual drift, the
  ateez score_sum, reconciled to 75 (the real plays-table sum, guarded
  on old value + live sum). Post-verify: all 399 published quizzes now
  have play_count, total_completions AND total_score_sum in exact sync
  with the plays table (0/0/0 drift). PREVENTION is an owner decision,
  NOT yet done: (1) add AFTER DELETE/UPDATE triggers on plays to keep
  the 3 counters live-correct (most correct, small schema add), or (2)
  a periodic reconcile cron recomputing the 3 counters from plays
  (eventually-consistent, no hot-path change, matches a display
  counter), or (3) stop denormalizing and COUNT on read (rejected:
  play_count is read on many hot sort/badge paths). Cowork recommends
  the trigger for score_sum/completions correctness + accepts a nightly
  reconcile cron as the cheap safety net. No trigger added yet: schema
  change awaits owner ruling.
- L-113 · 2026-08-09 · MIGRATION DRAFTED (owner picked trigger+cron) ·
  Owner chose prevention = trigger + cron. Cowork drafted
  docs/pending-migrations/150_plays_counter_triggers.sql (NOT applied,
  awaits owner SQL gate on the exact DDL): fn_plays_counter_sync()
  AFTER DELETE + AFTER UPDATE on plays only (INSERT stays owned by
  record_play, so no double-count); DELETE decrements the 3 counters
  by the row's contribution, UPDATE adjusts total_score_sum by the
  score delta (and handles a defensive quiz_id move); GREATEST floors
  at 0. Plus reconcile_quiz_counters() = set-based recompute of all 3
  counters from plays via LEFT JOIN from quizzes (so a quiz whose
  plays were all deleted also gets zeroed), returns rows fixed, for
  the nightly cron. Numbered 150 to leave 149 for the trivia table.
  REMAINING: (a) owner approves the DDL, then Cowork applies via MCP;
  (b) the cron ROUTE /api/cron/plays-counter-reconcile (thin wrapper
  calling reconcile_quiz_counters, + a vercel.json crons entry e.g.
  0 4 * * *) is CODE = queued for the Play/SEO worker as a small
  follow-up after SEO-3c, single-writer respected. Note: this migration
  is independent of the trivia 149 and can apply before it.
- L-114 · 2026-08-09 · MIGRATION 150 APPLIED + PROVEN · Owner said
  "applique la 150 et la route cron ok". Applied via MCP apply_migration
  (name plays_counter_triggers): both triggers present + enabled on
  plays (trg_plays_counter_sync_del, trg_plays_counter_sync_upd),
  fn_plays_counter_sync() + reconcile_quiz_counters() created. PROVEN
  end to end: a BEGIN/ROLLBACK smoke test deleted one ateez play
  (score 9) and the counters moved 7->6 / 7->6 / 75->66 exactly, then
  rolled back with zero persistence (ateez confirmed back at 7/75);
  reconcile_quiz_counters() returns 0 (all 399 published quizzes in
  sync) and runs clean. CRON ROUTE queued in docs/loop-seo/NEXT.md as
  SEO-4 (thin wrapper calling reconcile_quiz_counters nightly at 04:15,
  auth copied from duel-reconcile, one vercel.json entry) to run AFTER
  SEO-3c under single-writer; Cowork promotes it into MISSION.md once
  SEO-3c is reported. Root-cause chapter closed: live path was always
  correct, the gap was delete/update maintenance, now covered by
  trigger (live) + reconcile function (nightly net).
- L-115 · 2026-08-09 · AUDIT ACCEPTED · SEO-3c (7b175c3 + fdda5fa,
  not pushed, no schema). All four fixes verified on disk: C1 the
  Completions cell + prop are gone; C2 RANKING_UNLOCK_VOTES imported
  in both page and component, gate on the plays display base, the only
  "30" left in the file is an OG image height; C3 pass rate derived
  from passingPlays/totalPlaysWithScore with getPassRate no longer
  called (6 counts -> 4), the previously-dead passingPlays now read;
  C4 lib/quiz/scoring.ts scoreIsPerQuestion allowlist (mc/tf/image/
  intruder in, guess_from_clues out, deny-by-default over the full
  5-type QuizType union) gates avg + pass + perfect + fastest + the
  gate line, shared by the intro and the stats block. em-dash 0.
  ACCEPTED, SEO counter/stats thread closed.
- L-116 · 2026-08-09 · AUDIT ACCEPTED · V-FOUNDATION F3 + F4 (8
  commits 5d62dd9..a304fb9, not pushed, no schema). Full build 622
  static pages exit 0, tsc 0, verse-tokens pass, em-dash 0 (re-scanned
  by Cowork on fold/home-rail/home-head/home-overview/factrail = 0).
  Verified independently, not on trust: (a) DB is_stub state matches
  the report EXACTLY for space_id 1 (BTS): album 17 + era 15 is_stub
  false (indexable), track 197 + award 21 is_stub true (noindex stub),
  idol 7, portal home 1 + 5 section stubs = 263 pages; (b) fold.tsx
  honours the crawlable-collapse law, SSR renders EXPANDED (collapsed
  starts false, set true only in useEffect post-hydration), clip is
  max-height never display:none; (c) home-rail.tsx is a server
  component emitting only aggregate COUNTS + public quiz titles +
  generic curator letters (A/B/C), no player_id/email/identity to the
  client, reads fail-closed; (d) home-head has exactly one h1
  (vh2-h1 = group name), other h1s are off the portal path. F3
  INDEXABILITY RULING (owner-delegated, Cowork rules): ACCEPT the
  per-kind call, release + era take the fact-rail exemption (each is a
  real hub: release with its tracklist, era with its linked release
  list), track + award stay conservative noindex stubs until they
  carry body prose. Caveat recorded: is_stub false means "not
  mechanically thin", not "will index" - the Verse is admin-hidden so
  nothing indexes yet, and final public indexing still gates on the
  vitrine content pass + going public. Flags accepted as honest:
  group hangul omitted (no column, never invented), per-space accent
  kept system-neutral (future hook), fact-sheet photo is a moderated
  -rail slot placeholder. DEFERRED remain: editor budget nudge,
  per-space accent, document-canvas restyle, unit/label pages,
  galleries. Both workers now IDLE.
- L-117 · 2026-08-09 · MERGE BLOCKED (env) + TRIVIA DESIGN FINDING ·
  Owner asked to prepare the play-seo merge AND start content in
  parallel. MERGE: verified a CLEAN 3-way merge (only sitemap.ts +
  globals.css overlap, both auto-merge with zero conflict markers via
  git merge-file), but the merge CANNOT be executed from device_bash:
  replacing tracked files needs unlink, which the bridge forbids
  ("Operation not permitted"), so `git merge` half-applied then git
  self-aborted on the failed checkout. Recovered cleanly: moved the
  stale index.lock aside (mv works where rm does not), confirmed NO
  MERGE_HEAD, main intact at 7320f9a, play-seo intact at 19bc845, zero
  tracked modifications, all refs present. Before that, committed the
  pending docs on main (7320f9a: ledger + missions + migration 150).
  The merge must run in a REAL terminal (owner or a worker); the full
  build gate needs one too. TRIVIA 149: found the existing trivia
  system is DERIVED (apps/quiz/src/lib/trivia/*: group-level facts
  harvested from quiz fun_facts, deduped, categorized members/music/
  achievements/history/fun, corrected via a code-based TRIVIA_OVERRIDES
  layer verified against docs/trivia-corpus.json, /trivia page gated at
  >=12 facts). So O1 must COMPLEMENT not duplicate it. Drafted
  docs/pending-migrations/149_trivia.sql (DRAFT, not applied): an
  entity-level (entity_kind+entity_id) stored trivia table for
  structured-data + curator facts that persist independent of quizzes,
  denormalized group_id for O2, category reusing TriviaCategory,
  covenant CHECK (source NOT NULL non-empty), unique per entity+fact,
  RLS public-read-published only. FK types verified (groups.id integer,
  profiles.id uuid). Read layer would MERGE stored + derived facts.
  Awaiting owner design confirmation before finalizing + the SQL gate.
- L-118 · 2026-08-09 · TRIVIA DIRECTION LOCKED · Owner chose "complement,
  entity-level": the stored trivia table is entity-level (album/idol/era/
  track/award), holds structured-data + curator facts that persist
  independent of quizzes, and is MERGED at read time with the existing
  fun_fact-derived group facts. It feeds BOTH the Verse fact rails and
  the quiz did-you-know (O3). docs/pending-migrations/149_trivia.sql
  matches this direction as drafted; ready for the owner SQL gate
  ("applique la 149") to apply via MCP. Read-layer merge + seeding are
  code slices that follow (worker), covenant-bound. Next Cowork content:
  the P5 quiz-page prototype and the 7 BTS member pages (Jungkook first).
- L-119 · 2026-08-09 · 149 APPLIED + MERGE MISSION + P5 PROTOTYPE ·
  Owner: "applique le toi avec mcp" (149), merge via worker, P5 first.
  (1) Migration 149 trivia_base APPLIED via MCP: 14 cols, 4 indexes
  (PK + unique-per-entity-fact + 2 partial published), RLS on, one
  public-read-published policy, 0 rows. Verified. (2) MERGE handed to
  a worker: docs/loop/MISSION.md rewritten as a pure integration task
  (checkout main, merge --no-ff play-seo, run the FULL gate suite incl
  next build, never push, BLOCKED.md if any non-sitemap/globals
  conflict, receipt docs/proofs/merge-play-seo/gates.txt). Owner
  triggers it; owner pushes after green. (3) P5 quiz-page prototype
  delivered + committed to device (prototypes/quiz-page-p5.html): same
  white Notion-calm system as home v2, built on the REAL big BTS quiz
  numbers (2060 plays / 53% / 32% pass / 224 perfect / 18s / 38 likes),
  showing every substance+uniqueness block: U2 dynamic intro, U1 stats
  (unlocked + per-question type so percentages show), U8 spoiler-safe
  crawlable questions, U4 same-group related, U6 creator, U9 honest
  JSON-LD note (no rich result post-deprecation), and the rail with O3
  did-you-know (one distinct sourced fact) + O1 BTS facts panel fed by
  the new trivia base + O2 newest/hardest links. Intro copy kept free
  of the editorial "only" to match the shipped R5 rule. Gates: em-dash
  0, JS syntax ok, rendered light+dark+mobile. Awaiting owner
  validation of the LOOK before the SEO worker wires O1/O3 for real.
- L-120 · 2026-08-09 · COURSE CORRECTION (my error) · The P5 quiz-page
  prototype (L-119) was WRONG: I built it in the Verse home-v2 language
  (Fraunces serif, blue accent, white, right data rail) when the quiz
  side has its OWN final design (DM Sans, rose brand #E8457A, warm
  #FAF8F5 ground, existing classes .trivia-entry / .quiz-review /
  .related-quizzes). Owner: the quiz page is already perfect, do NOT
  reprototype, just ADD the missing SEO elements in the existing design.
  Reassessed against the real code: everything P5 is shipped by the SEO
  worker EXCEPT O3 - the page only LINKS to /[group]-trivia, it never
  shows an inline fact. The one genuinely missing SEO element = an
  inline "Did you know?" card, one real fact DISTINCT per quiz. Key
  find that unblocks it with no seed: getOverriddenFacts(group) already
  returns real override-verified facts, so the card reuses that pool and
  picks index = stableHash(quiz.id) % n (deterministic, ISR-safe).
  Wrote SEO-4 mission: add ONLY the did-you-know card, reusing the
  existing .trivia-entry visual language (new .quiz-dyk class, same
  tokens, DM Sans, no new colours), placed above the trivia-entry link,
  honest emptiness at 0 facts. The Verse-styled quiz prototype is
  abandoned (it was for the Verse, not the quiz side; the confusion was
  mine). Migration 149 entity-level enrichment is a LATER depth slice,
  not a blocker for O3.
- L-121 · 2026-08-09 · AUDIT ACCEPTED (merge + SEO-4) · MERGE: play-seo
  (SEO-1..3c) merged into main = dc0eabf, parents 7320f9a + 19bc845
  verified, 19bc845 confirmed ancestor of main. Merged-main content
  spot-checked present: scoring.ts, MusicGroup JSON-LD, QuizStatsBlock,
  globals.css verse tokens + quiz-stats classes. Worker handled two
  concurrent-session drifts cleanly (13 untracked byte-identical
  play-seo files removed so the merge writes them tracked, no work
  lost; a stale .git/HEAD.lock cleared). Gates on merged main: build
  EXIT 0 / 622 static pages, tsc 0, verse-tokens pass, em-dash 0.
  main is 37 commits ahead of origin, NOTHING pushed - READY for the
  owner push. SEO-4 (O3 did-you-know): shipped on play-seo (054c000),
  NOT in the merge (merge took SEO-1..3c only), so ONE more small merge
  brings it to main. Verified: pick-fact.ts is a pure FNV-1a hash (no
  Math.random/Date, ISR-stable, distinct per quiz), .quiz-dyk reuses
  ONLY existing tokens (--surface/--border/--brand/--brand-light/
  --txt1), 14px radius + brand-light chip like .trivia-entry, no new
  colour/font, no heading (one-H1 kept), honest emptiness at 0 facts,
  card placed above the trivia-entry link. Receipt: BTS 27 quizzes /
  98-fact pool, first 3 ids -> indices [12,69,52] = 3 distinct,
  deterministic PASS. Full build EXIT 0 (deps symlinked). Both
  ACCEPTED. Next: bring SEO-4 into main with one small merge, then a
  single push. The Verse-styled quiz prototype (L-119) stays abandoned.
- L-122 · 2026-08-09 · SEED + SEO-5 MISSION · Owner: do the cron route,
  the trivia seed, and the member pages before pushing, and ensure the
  did-you-know is PRESENT. Verified SEO-4 did-you-know IS wired in the
  JSX ({dykFact && <aside class="quiz-dyk">}, lightbulb icon, fed by
  getOverriddenFacts + stableIndex), present for BTS (98-fact pool).
  SEED APPLIED via MCP (docs/pending-migrations/149_seed_bts.sql, also
  versioned): 14 real sourced BTS facts into the trivia table (5 group,
  7 idol, 2 album), each with a source (Wikidata / MusicBrainz /
  Billboard / Wikipedia / KpopVerse DB), covenant-clean; "Dynamite =
  first Hot 100 #1" web-verified against Billboard; member facts
  DB-grounded (real birth dates + positions); ON CONFLICT DO NOTHING
  idempotent. SEO-5 mission written (docs/loop-seo/MISSION.md, synced to
  worktree): PART A the plays-counter reconcile cron route (thin wrapper
  over reconcile_quiz_counters, auth copied from duel-reconcile, 04:15,
  the queued NEXT.md item); PART B wire the did-you-know to ALSO read
  the trivia table (new lib/trivia/stored-facts.ts, merge stored+derived,
  dedupe via normalizeFactKey, same stableIndex pick) so the entity-level
  seed surfaces, honest emptiness kept, card CSS/placement untouched.
  Both code, on play-seo, for the pre-push batch. Remaining: the final
  play-seo->main merge (brings SEO-4 + SEO-5) then one push; and the
  Jungkook member page (Verse content, admin-hidden, not in the push).
- L-123 · 2026-08-09 · JUNGKOOK DRAFT · First member page drafted
  (verse-jungkook-draft.md, delivered for validation): original vitrine
  prose, 4 sections (overview / place in BTS / going solo / threads to
  follow) + a fact rail line + sourcing footer. Facts DB-grounded (born
  1997-09-01, Main Vocalist + Center, 전정국) plus solo career
  web-verified (Golden album Nov 2023, singles Seven / 3D / Standing
  Next to You) against Wikipedia + reviews; prose is ORIGINAL, not
  copied (copyright/covenant). Awaits owner validation of the writing
  before it loads into pages.blocks for the real Jungkook page. Pattern
  for the remaining 6 member pages once approved. NOTE: member pages
  are Verse content (admin-hidden), NOT part of the code push.
- L-124 · 2026-08-09 · SEO-5 ACCEPTED · play-seo 456c054 (SEO-4 +
  SEO-5), not pushed, no schema. PART A cron route
  /api/cron/plays-counter-reconcile: auth guard copied from
  duel-reconcile (x-vercel-cron OR Bearer CRON_SECRET, else 401),
  service-role only after auth, calls reconcile_quiz_counters, returns
  {fixed}, 04:15 cron in vercel.json clear of existing slots; a no-op
  safety net today (fn returns 0, trigger keeps sync). PART B: new
  lib/trivia/stored-facts.ts getStoredGroupTrivia reads published-only
  trivia, fail-closed, category clamped; page.tsx builds the DYK pool
  as derived CONCAT stored, deduped by normalizeFactKey via a seenKeys
  Set (derived wins ties), then the existing stableIndex pick, nothing
  if both empty. Verified on disk incl. the dedup Set. Receipt: BTS
  derived 98 + stored 14 (0 dupes) = 112 pool, 3 ids -> 3 distinct, a
  stored entity fact surfaces. Gates: tsc 0, full build EXIT 0 (352
  routes, cron route compiled), verse-tokens pass, em-dash 0. ACCEPTED.
  The SEO body of work (SEO-1..5) is done; only the final
  play-seo->main merge + push remains.
- L-125 · 2026-08-09 · FINAL MERGE ACCEPTED · play-seo fully merged
  into main = 0027c58 (parents adda44f + 456c054, base 19bc845, ort,
  clean auto-merge of page.tsx/globals.css/vercel.json). Verified:
  did-you-know now present in main (quiz-dyk x5), 456c054 is an
  ancestor of main (all SEO-1..5 integrated), the cron route compiled
  (check:routes 353), build EXIT 0, verse-tokens pass, em-dash 0, 41
  commits ahead of origin, NOTHING pushed. The whole SEO body of work
  (SEO-1..5) is on main, ready for the owner push. TWO housekeeping
  items the merge worker handled: stale .git locks cleared; an
  owner-approved quiz-stats UI redesign that sat uncommitted in
  globals.css was stashed for the pure-merge gate, then popped back as
  working-tree drift. Cowork audited that drift: it renames
  .quiz-stats-block -> .qstats and restructures the block into a hero
  Plays number + a secondary grid; the .tsx and CSS class sets match
  EXACTLY (no orphan old classes), all the SEO-3c/C4 correctness is
  preserved (empty-return, threshold-30 gate, type-aware suppression),
  em-dash 0. SAFE to commit + ship; caveat: the merge build gate ran
  WITHOUT it (stashed), so run one npm run build WITH it before the
  push. Doc drift (ledger + loop docs) committed by Cowork; the
  stats-UI code drift left for the owner's word.
