# V-SPACE-FLOW + V-TEXT + V-TEMPLATES - the reading product's final form

## Claude Code Implementation Prompt

---

V3 step 3. Read first: VERSE-ROADMAP-V3.md (V-SPACE-FLOW, V-TEXT sections),
VERSE-PAGES-UNIVERSE.md Part 3 (the canonical order 3a + structure templates
3b are LOCKED owner decisions, do not re-litigate), and
VERSE-APPLICATION-BLUEPRINT.md sections 7-8 (ideas 1-3 and 7 attach here).
Builds on V-DESIGN + V-IDENTITY.

Hard rules: NO em dashes. Commit per step, do NOT push. No new deps.
Dual-skill design on every surface. Three breakpoints, light + dark. Play
world byte-untouched. SEO parity law + contrast guardrail in force. ONE
migration maximum IF truly needed (expected candidate: space-scoped quiz
linkage); if any second schema need appears, STOP. Min-gate everything.

## Step 0 - identity debt

Deeper violet token for small text on light mode (AA 4.5:1, same hue
family); brand violet #7c5cfc stays for wordmark/hero/large display. Apply
to the small links flagged in the V-IDENTITY audit. Commit.

## Step 1 - the canonical order becomes the default space home

Locked order (universe doc 3a): hero -> INTRO -> vitals strip -> members ->
story-so-far -> latest/landmark releases -> now (era/countdown/live) -> go
deeper explore grid -> collections teaser -> community -> games (LAST) ->
related footer.
- INTRO: reuse the existing overview section as the seo_critical intro
  block rendered in position 2 (2-4 sentences, curator-written, "Start
  writing" nudge when empty for members, hidden for visitors). No new
  storage.
- VITALS: one quiet line (debut year, members, agency, fandom name +
  colors) from entity data. Not boxes.
- GO DEEPER: explore grid of real destinations only (min-gated per target)
  + "Surprise me" (random page within the space).
- GAMES DE-EMPHASIZED: the games strip leaves the top; becomes one compact
  module in position 11 + a nav link. Existing curator reorder freedom
  (W-CUSTOM) still applies on top of this new default.
- Commit. Screenshot the new default on bts (no preset) for the report.

## Step 2 - V-TEXT: long text without fandom fatigue

- FOLDING: every prose section (lore, era stories, about) renders preview
  (first ~2 paragraphs) + "Read more" expansion. FULL text stays in the
  served HTML (SEO unharmed, fold is presentation only, verify with JS
  off).
- Auto-TOC (exists, W3.5) activates past 3 sections + sticky on desktop.
- EDITOR HINTS: section length guidance in the editor ("readers drop
  around 300 words; consider splitting"), never blocking.
- Per-section curator choice: inline / folded / split to sub-page (the
  sub-page option lands with V-PAGES; ship the toggle disabled for it
  now with a "coming" state ONLY if that state is honest, else omit).
- Commit.

## Step 3 - V-TEMPLATES: structure templates + space creation flow

- The four bundles (locked, universe doc 3b): STARTER (recommended, say
  so) / COMPLETE / ENCYCLOPEDIA / EMPTY. Pure config bundles over the
  W-CUSTOM system (tab set + module set + enabled kinds placeholder +
  draft scaffolds as DRAFTS never published stubs).
- Template picker at space creation (admin/owner-facing today, curator-
  facing at recruitment) + switchable later from the studio (switching
  = config merge, never content loss; prove it).
- Commit.

## Step 4 - in-space quiz creator (the funnel running backward)

- Curators create quizzes FROM the space: existing creation engine,
  Verse-chromed entry point, group pre-selected and locked, published to
  the main games catalog AND the space's shelf, credited "by {curator} of
  {space}".
- Schema check FIRST: if quizzes need a space linkage column, this is THE
  one migration: spec it, STOP, owner runs. If existing group linkage
  suffices (quiz group_id -> space by slug), NO migration: justify which.
- The space's games module shows its own quizzes first.
- Commit.

## Step 5 - the living modules (blueprint ideas 1-3, 7)

Each min-gated, each a W-CUSTOM registry module, each honest:
- COMEBACK MODE: curator arms a comeback window (start/end, max 45 days):
  event skin (hero countdown), pinned comeback hub link (page arrives
  with V-PAGES; until then pins an era/discussion), release-day live
  discussion thread. Auto-expires. Extends liveNow machinery.
- THIS DAY IN {GROUP} HISTORY: computed from dates we hold (debut,
  releases, awards, tour stops). Hides on empty days. Zero new data.
- COMPLETENESS METER: public coverage score on the space home ("Eras 4/6
  written · Discography 92% noted"), computed from the quality engine
  (W3K.10). Min-gated past a floor so new spaces are not shamed.
- ANNIVERSARY AUTO-MOMENTS: debut/birthday days get a quiet hero
  celebration (accent moment, "8 years today"), curator-disableable,
  reduced-motion respected.
- Commit per module.

## Step 6 - closing: STOP for owner review

Screenshot matrix: the new default order on a launch space + one showcase,
a comeback-armed space, this-day + completeness live, 3 breakpoints x
light/dark. Then after approval: dual-skill closing audit, SEO parity
re-proof (fold test with JS off included), Play byte-diff, full build,
em-dash grep, check:routes. Commit.

## Permitted loops (per LOOP-CHARTER)

BUILD-VERIFY-FIX per step · GOAL tsc + full build + check:routes + step
tests green · MAX 8 · STOP IF second migration / design ambiguity / any
"coming soon" state that would be dishonest.

## Verify

- [ ] Default order matches 3a exactly on a no-config space; W-CUSTOM
      reorder still works on top
- [ ] Folded sections: full text in served HTML with JS off; SEO parity
      re-proven
- [ ] Templates: all four apply + switch without content loss (proof)
- [ ] In-space quiz: created from space, appears in catalog + shelf,
      credited; migration decision justified either way
- [ ] Comeback mode arms/expires; no fake liveness anywhere
- [ ] This-day/completeness/anniversary all real-data, all min-gated
- [ ] Small-text violet passes AA on light; guardrail sweep on new
      surfaces
- [ ] tsc, full build, check:routes green; zero em dashes; no new deps;
      Play byte-untouched; static/ISR intact

/caveman report per step; Step 6 matrix is the owner gate.
