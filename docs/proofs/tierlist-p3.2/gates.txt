# SEO gates - TIERLIST phase 3.2 (2026-09-14), server :3021
=== check:docs-secrets ===
PASS
Docs secret gate: scanned 678 tracked files under /Users/louis/IT/Dev/projects/KpopQuizzV2/docs/.
Docs secret gate passed: no credential-shaped value in any tracked doc.

=== check:routes ===
PASS
Route allowlist guard passed: 377 page routes reachable.
  - skipped /avatar-studio (Avatar studio is frozen: hidden on purpose, code and assets kept.)
  - skipped /blind-test (SEO consolidation: 301s to /blindtest.)
  - skipped /blind-test/sample (SEO consolidation: 301s to /blindtest.)

=== check:indexability ===
NONZERO EXIT

Either the sitemap advertises a page it should not (noindex / missing title / non-self-canonical),
or an indexable page fell out of the sitemap. Fix the page's metadata OR the sitemap inclusion in src/app/sitemap.ts.


=== check:metadata-dupes ===
NONZERO EXIT

Every indexable URL needs a unique title and a unique description.
Fix the template so it varies by construction, or stop advertising the duplicate URL in the sitemap.


=== check:orphans ===
NONZERO EXIT
  x /q/bts-true-or-false-bet-you-cant-get-100
  x /q/katseye-quiz

Fix by linking them from a real surface people use (e.g. /groups, the A-Z directory), not by minting a page to hold the link.


--- READING (phase 3.2) ---
docs-secrets + routes PASS. indexability + orphans + metadata-dupes NONZERO, but every offender is
pre-existing and unrelated to this phase:
  orphans/indexability: /katseye-trivia, /q/katseye-quiz, /q/bts-true-or-false-bet-you-cant-get-100
    - the recurring katseye/bts quizzes that flip in and out of the local sitemap by seed-data state
    (present in phase 3, absent in 3.1, back now); data-dependent, NOT caused by this change.
  metadata-dupes: the verse-inflation /verse/* dupes.
CRITICAL for this phase: /verse is NOT orphaned (0 hits) and /personality is NOT orphaned (0 hits),
so removing the desktop world toggle and the personality home banner stranded nothing - the footer
Fandoms link keeps /verse reachable, and personality keeps its nav/games/sitemap presence. NO
tier-list URL appears in any failing gate. This phase adds no NEW orphan and no new dupe.
