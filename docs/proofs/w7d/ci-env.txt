=== W7d PART 1: what CI has vs what the gates need ===
workflows present:
  birthdays.yml
  daily-quiz.yml
  indexnow.yml
  leaderboard.yml
  news.yml
  setup.yml
  youtube.yml
secrets referenced by ALL existing workflows:
  secrets.DISCORD_TOKEN
  secrets.GUILD_ID
  secrets.INDEXNOW_TOKEN
  secrets.QUIZ_SUPABASE_ANON_KEY
  secrets.QUIZ_SUPABASE_URL
env keys the app needs to build+serve (names only):
  ADMIN_PASSWORD
  ADMIN_USER_IDS
  CRON_SECRET
  DEV_LOGIN_CONTRIBUTOR_EMAIL
  DEV_LOGIN_EMAIL
  DEV_LOGIN_ENABLED
  DISCORD_FLEX_WEBHOOK_URL
  NEXT_PUBLIC_SITE_NAME
  NEXT_PUBLIC_SITE_URL
  NEXT_PUBLIC_SUPABASE_ANON_KEY
  NEXT_PUBLIC_SUPABASE_URL
  SONGS_IS_CURATED
  SUPABASE_SERVICE_ROLE_KEY
  VERSE_PUBLIC
  YOUTUBE_API_KEY

MISSING for CI: SUPABASE_SERVICE_ROLE_KEY (sitemap.ts uses createServiceRoleClient;
without it the sitemap emits static-only and check:orphans grades a crippled sitemap).
