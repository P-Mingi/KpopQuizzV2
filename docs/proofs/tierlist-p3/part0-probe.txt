# PHASE 3 PART 0 probe - is migration 146 applied? (2026-09-14)
# Read-only probe of the LIVE quiz DB via the public REST endpoint (anon key).
# The owner applied 146 between the blocked run (2026-09-13) and this run.

GET /rest/v1/tier_lists?select=<all 14 columns>&limit=1        -> HTTP 200 []
GET /rest/v1/tier_list_assets?select=<all 7 columns>&limit=1   -> HTTP 200 []
storage bucket tier-list-assets                                -> exists, public read

VERDICT: migration 146 is APPLIED. Both tables exist with the full 146 column set
(tier_lists: id, slug, creator_id, anon_id, subject_group_id, subject_kind, title,
tiers, placements, visibility, views, likes, created_at, updated_at; tier_list_assets:
id, owner_id, anon_id, name, image_url, status, created_at), RLS public-read works
(anon returns 200), and the public bucket is present. PART 0 passes -> proceeded to
build PART A/B/C live. No DDL applied by the worker.
