V-FOUNDATION F4.3 - the home v2 RIGHT DATA RAIL, six real widgets on /verse/bts.
Server components (components/verse/tree/home-rail.tsx); counts via HEAD COUNT queries
(1000-row law, never fetchAllRows for counting); fail-closed (a throwing count -> 0/hidden).
Every value below was read LIVE from the DB and rendered in the browser (verified).

1. FACT SHEET  (buildGroupFactRail, A2 Data / Auto grammar + moderated photo slot)
   Debut       Jun 13, 2013      [Data]   <- groups.inception_date
   Label       Columbia Records  [Data]   <- groups.record_label
   Generation  3rd Gen           [Data]   <- groups.generation
   Origin      South Korea       [Data]   <- groups.origin_country
   Fandom      ARMY              [Data]   <- groups.fandom_name
   Members     7                 [Auto]   <- count(idols active, group 1)
   Active      13 years          [Auto]   <- computed(now - inception_date)
   source line: "Wikidata · MusicBrainz · CC0."

2. IN NUMBERS  (all Auto, counted live)
   Releases 17   <- count(albums, group 1)
   Tracks  197   <- count(album_tracks in the group's album ids)
   Eras     15   <- count(eras, group 1)
   Awards   21   <- count(awards, group 1)

3. COMING UP  (Auto)
   "Jungkook's birthday · in 23 days" (Sep 1)  <- min upcoming of idols.birth_date
   (today 2026-08-09; Jungkook 1997-09-01). NULL birth date -> that member skipped
   (honest emptiness); no members with a date -> the widget renders nothing.

4. THIS SPACE  (Auto)
   Pages 263            <- count(pages space 1, published)
   Members covered 7/7  <- count(pages entity_kind idol) / counts.members
   Releases covered 17/17 <- count(pages entity_kind album) / counts.albums
   Filled in 15%        <- non-stub 40 / published 263 (the F3 backfill made release+era non-stub)

5. PLAY  (Data)
   3 quiz links -> /quiz/{id} (quizzes group 1, published, top 3 by play_count)
   source line: "27 published BTS quizzes on the Play side." <- count(quizzes group 1, published)

6. CURATORS
   "1 curator tends this space"  <- count(space_members group 1, role curator, active)

LAYOUT: verse/[slug]/page.tsx now renders head + a two-column body (.vh2-layout):
the document column (still the unified Composition, reconciled in F4.5) + the data
rail (.vh2-rail, sticky). Screenshots home-rail-{light,dark}.png (both clean; Data
badges green, Auto badges gold, Fraunces numbers, coverage bars).

FLAGS / DEFERRED
- The rail loader uses the service-role client for server-side aggregate COUNTS only
  (numbers + public quiz titles + a curator COUNT); no private identity/fields reach
  the client (privacy law: redact from props). Reads are fail-closed.
- The document column is still the existing Composition (with its own section styles);
  the doc-column reconciliation + the mobile order (fact card right after the head) is F4.5.
- Fact-sheet photo is a moderated-rail slot placeholder (no group photo field yet).

GATES: tsc 0. verse-tokens: pass. em-dash: clean.
