C1-002 | header-sheet (P10 state; A0 HeaderPictureSheet, .ux-drop) | 1440,390 | light,dark | sheet opens with the drop zone at rest: 1.5px dashed --edge border, no fill (#hsheet .drop) | sheet opens with the drop zone in its hover look (pink border rgb(232, 69, 122), --surface fill): the sheet moves focus into the file input and `.ux-drop:hover, .ux-drop:focus-within` paints it. Keep the focus move (a11y); drop :focus-within from that rule (keyboard focus already gets the outline through :has(input:focus-visible)) or start focus on the sheet title / close button | docs/design/ux-dashboard-v1/v11/checks/pixel/header-sheet/1440-light-side.webp (numbers: header-sheet/verdict.json, landmark "drop zone")
C2-001 | R024 (flag off: /api/quizzes/count) | n/a | n/a | every endpoint added since main (450f053) answers 404 with the flag off; flag off = today's site | GET /api/quizzes/count answers 200 {"count":425} with the flag off (phase 1 badge endpoint, not gated by UX_V1, no caller left in apps/quiz); gate it with UX_V1 (404 flag off) or delete it | docs/design/ux-dashboard-v1/v11/checks/backend/proofs/flag-off-endpoints-dev-4202.txt
