# P7 Ranked

Branch `ux11/p7-ranked` (engine pass done: 56eb011, draft PR #44; 211 vitest tests). Dev port 3037. Owned paths: the "P7" globs in `v11/OWNERSHIP.json` + `v11/reports/P7.md`, `v11/reports/P7/**`, `v11/requests/P7.md`.
Pass 2 (this brief): continue on your branch, first `git merge origin/feat/ux-v1-v11` (A0's foundation is there), then build the UI.
States: `ranked`, `btend-ranked`.
Spec: 17.6 (wins), 15.4, 17.10, 17.11, 16.7 ranked.
Scope: the ranked page at /blindtest/ranked (flag off: `notFound()`, the URL 404s today and must keep doing so; flag on: the page) and the ranked results panel in `components/ranked/ux-v1/` that P6's blindtest results render in ranked mode (coordinate through your report and P6's; do not edit P6 files), plus a ladder endpoint under `api/ranked/`. Everything reads the real engine and fails soft: while `v11-p7-ranked.sql` is not applied the page shows a truthful "Ranked is not live yet" state. The populated layout (season, tier, division, runs, ladder) is built to the prototype and proven with render tests that feed engine outputs (say clearly what they prove); the live pixel check of the populated state is "NOT verified until the migration is applied".
New URL: /blindtest/ranked is noindex and out of the sitemap until the owner decides.
