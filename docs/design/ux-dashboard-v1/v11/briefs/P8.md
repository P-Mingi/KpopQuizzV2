# P8 Community

Branch `ux11/p8-community`, dev port 3038. Owned paths: the "P8" globs in `v11/OWNERSHIP.json` + `v11/reports/P8.md`, `v11/reports/P8/**`, `v11/requests/P8.md`.
States: `community`, `post-challenge`, `post-blog`, `post-debate`, `editor`.
Spec: 17.7 (bordered posts and rail panels, heart + count comment likes, names with identity flair, a bit more pink), 17.8 flair, 17.10, 17.11, 16.7 community, 13.x (feed, post modes, editor, thread page) and 12.x where 16/17 point.
Scope: /community (new route; the middleware knows it only with the flag on, so flag off keeps today's 301 to home) with feed, rail panels, post view per type (threads, blogs, debates, challenges), editor (4 modes), replies, likes, debates, challenges, happening now, badge watch. Author names through A0's PersonName (name_accent, name_font, bias). `/verse` stays as it is (legacy chrome, separate product).
Backend: the existing community tables (verse_threads, verse_discussions, verse_essays, daily_debates / debate_votes, challenges / challenge_attempts, activity_events / activity_cheers, likes, follows) through the existing endpoints (same payloads; `api/community/**` is yours but existing routes keep their contracts; new behaviour = new route under `api/ux-v1/p8/`). Existing components in `components/community/` (happening-now, daily-debate, badge-showcase...) are reused, not edited. Anything with no table: pending migration, fail soft. Posting, replying, liking and voting are proven by guardWrites payloads, never performed.
SEO: /community and its post URLs are new: noindex, out of the sitemap, until the owner decides (they would duplicate /verse content).
