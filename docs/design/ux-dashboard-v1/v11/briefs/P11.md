# P11 Notifications + search + bell

Branch `ux11/p11-notifications`, dev port 3041. Owned paths: the "P11" globs in `v11/OWNERSHIP.json` + `v11/reports/P11.md`, `v11/reports/P11/**`, `v11/requests/P11.md`.
States: `notifications`, `search`, `bell`.
Spec: 15.x notifications (v9), 16.5 navigation (search and bell triggers), 16.7, 17.10 (sheet closing), 17.11.
Scope: the /notifications page, the bell panel (latest 6), the search overlay results. A0 owns the nav and the overlay/panel chrome and defines slots in `components/layout/ux-v1/slots.tsx` (`SearchResults({ query, onNavigate })`, `BellPanel({ unread, onClose })`). You export components with exactly those props from `components/search/ux-v1/search-results.tsx` and `components/notifications/ux-v1/bell-panel.tsx`, then file a request in `v11/requests/P11.md` for A0 to swap the slot exports; do not edit A0's files. The notifications center is EXISTS: re-skin, never fork its read or mark-read calls.
Backend: notifications from the existing store (creator_notifications) + mark-read through the existing endpoint (proven by guardWrites payloads, never performed); search over groups, quizzes (including the quizzes of a matched group) and songs, through the existing search endpoint or a new one under `api/ux-v1/p11/` (never change the existing payload). No endpoint returns another user's email, auth id or prefs.
SEO: /search and /notifications keep their robots rules (both are noindex today) and titles.
