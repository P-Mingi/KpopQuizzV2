# C3 QA + report

1. e2e: run every `apps/quiz/e2e/ux-v1/*.spec.ts` except `parity.spec.ts` against the shared flag-on build, projects ux-1440 and ux-390, light and dark, guest and signed in (setup project). Record pass / fail / skip per spec; a failure goes to the spec's owner.
2. Accessibility: axe (A0's `runAxe`, axe-core comes with eslint-config-next) on every state: 0 serious or critical. A keyboard walk of every page (reach and operate every control, visible focus, sheets trap focus and close with X, Escape and backdrop, focus returns). Screen reader semantics for the game (live region announces results) and the sheets (role, name, describedby). Extra specs go in `apps/quiz/e2e/ux-v1/qa-*.spec.ts`.
3. SEO: for every public URL type (at least /, /pt, /quizzes and ?page=2, ?group=bts, 3 /q/<slug> (classic, true/false, image), 3 hubs incl. one near-empty, one -trivia page, /groups, /blindtest and 2 modes, /pt/blindtest, /leaderboard, /pt/leaderboard, /u/testtest, /create, /search, /articles and one article, /trending, /new, /most-liked, /stats, /data/pulse), compare the flag-on build with the live site (kpopquiz.org = main) and with a flag-off build of the integration branch: title, meta description, H1, intro, FAQ text, canonical, JSON-LD (parsed), hreflang, robots, the link set (every href of the flag-off / live server HTML present with the flag on), server rendering without JS. Also sitemap.xml and robots.txt: identical flag on vs off (new pages are noindex and out of the sitemap). Anything removed = an issue to the owner; additions are listed and checked against DESIGN-SPEC 16.10.
4. Performance: Lighthouse is not installed and adding it is a new dependency, so do NOT install or npx it; measure with Playwright on mobile emulation (390, 4x CPU throttle) for the home, a quiz page, a group hub and /blindtest: LCP, CLS, total JS transferred, image natural width vs rendered width for `public/idols` photos (right size, `sizes` set). Say in the report that the Lighthouse scores (perf >= 85, SEO 100, a11y >= 95) are pending the owner's OK to run Lighthouse.
5. Write `docs/design/ux-dashboard-v1/v11/REPORT.md`: one section per page (status DONE / OPEN, reference vs implementation thumbnails from C1's evidence, pixel verdict from C1, wiring verdict from C2, e2e / a11y / SEO / perf numbers), open issues with their owner, pending migrations and owner decisions (copy from `v11/RUN-STATE.md`), what is NOT verified and why. Read C1's and C2's files as they land; finish the report after they finish.
