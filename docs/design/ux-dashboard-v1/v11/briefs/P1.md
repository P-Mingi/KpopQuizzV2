# P1 Home (spawned 2026-09-25, in flight; kept here for respawn)

Branch `ux11/p1-home`, dev port 3031. Owned paths: the "P1" globs in `v11/OWNERSHIP.json` + `v11/reports/P1.md`, `v11/reports/P1/**`, `v11/requests/P1.md`.
States: `home`, `home-guest`.
Spec: 17.2, 17.10, 17.11 (live ticker + centred live-site header, pink quiz of the day), 16.7 home, 16.10, 14.x dashboard rows where 16/17 point.
Scope: live ticker + centred header (greeting when signed in; the real fan H1 + CTAs for guests; today's home H1 text and intro stay, owner-locked for SEO), quiz of the day as ONE calm row on a pink gradient (label + countdown, title, meta line, Play; no group tag, no question preview), continue, groups rail, trending, all time best, new, blindtest daily band, community rows. Static/ISR with client islands for user state (no cookies on the server render).
Backend: QOTD from quizzes.is_quiz_of_the_day + qotd_log (title, type, level, question count, real average, countdown). Ticker = components/home/activity-ticker.tsx and its two endpoints, imported, never edited (its random floor on the online count is an open owner decision, keep it). Header greeting from the profile via a client island. QOTD rotation stopped on 2026-06-30: root cause by code reading + read-only queries; fix the code behind an env switch that defaults off (`QOTD_ROTATION_FIX`), or a pending migration; the home must not show a stale QOTD with a lying label.
SEO / flag-off proof: `/` and `/pt` (the `/pt` page itself is unowned and stays as is).
