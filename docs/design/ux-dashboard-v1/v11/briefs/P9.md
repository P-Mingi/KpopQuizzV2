# P9 Leaderboard

Branch `ux11/p9-leaderboard`, dev port 3039. Owned paths: the "P9" globs in `v11/OWNERSHIP.json` + `v11/reports/P9.md`, `v11/reports/P9/**`, `v11/requests/P9.md`.
States: `leaderboard`.
Spec: 17.3 (validated; global pink and border rules), 17.10, 17.11, 16.7 leaderboard, 13.x leaderboard where 16/17 point.
Scope: tabs, podium, rows, pinned "you" row (A0's PinnedRow), "how points work". Names with A0's PersonName.
Backend: fandom war, players, ranked and creators from real aggregates (existing components in `components/community/` such as fandom-war-map, top-creators-tabs, your-standing are reused, not edited); the ranked tab shows the truthful not-live state while P7's migration is pending; the pinned row reads the signed-in user through a client island (the page stays static/ISR).
SEO lock: /leaderboard title, meta, H1, intro and canonical unchanged; `/pt/leaderboard` (unowned) unchanged. `/blindtest/leaderboard` is P6's.
