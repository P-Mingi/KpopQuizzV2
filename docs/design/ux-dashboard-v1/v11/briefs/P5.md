# P5 Create

Branch `ux11/p5-create`, dev port 3035. Owned paths: the "P5" globs in `v11/OWNERSHIP.json` + `v11/reports/P5.md`, `v11/reports/P5/**`, `v11/requests/P5.md`.
States: `create-1`, `create-2`, `create-3`, `signin` (A0's sign-in sheet opened at Publish).
Spec: 17.9 (validated: no change), 16.6 sign-in sheet, 16.7 create, 16.10, 14.x creation where 16/17 point.
Scope: the create funnel, re-skin only. `create-funnel.tsx` and `question-list-editor.tsx` are EXISTS components: re-skin, never rewrite their state, validation or save calls.
Backend: sign-in happens at Publish and keeps the draft (the existing create-draft mechanism); after auth the draft publishes through the existing endpoints (quiz save = `PUT /api/quiz/[id]`, `title-check` is a GET). Use A0's `useSignIn()` with an action id and `takePendingAction(id)` to resume. Never actually create or publish a quiz: prove it with guardWrites payloads.
SEO lock: `/create` keeps its title, meta, H1, intro and robots. Proof on `/create`.
