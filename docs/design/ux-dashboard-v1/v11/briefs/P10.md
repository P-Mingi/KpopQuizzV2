# P10 Passport + settings (spawned 2026-09-25, in flight; kept here for respawn)

Branch `ux11/p10-passport`, dev port 3040. Owned paths: the "P10" globs in `v11/OWNERSHIP.json` + `v11/reports/P10.md`, `v11/reports/P10/**`, `v11/requests/P10.md`.
States: `passport`, `passport-badges`, `settings`, `header-sheet`.
Spec: 17.8, 17.10, 17.11 (SVG badge medallions, sheet closing), 16.7 passport + settings, 16.10, 12.x/15.x passport where 16/17 point.
The /me write problem: viewing /me (or /profile) signed in grants badge tiers and writes passport snapshots. Never load them signed in. The flag-on passport keeps exactly the same server behaviour (same calls, same writes, same order); only the render changes. Verify through `/u/testtest` as a guest (after confirming its GET writes nothing) and render tests fed with the props the server builds; mark signed-in /me checks "NOT verified (owner decision pending)". /settings may be loaded signed in only if its GET writes nothing.
Scope: passport (band, change header, name flair, pinned badge medallion next to the name, badge medallions with live counts, tabs), settings (Your look, notifications, appearance, account), header upload + link routes. Auth and identity untouched; settings round-trip = zero diff in profiles for unchanged fields.
Backend: Your look writes name_accent, name_font, bias, profile_theme, pinned_badge_id through the EXISTING /api/auth/update-profile (validation from lib/passport-flair.ts / passport-themes.ts), proven by guardWrites payloads. Header upload route (storage bucket, JPG/PNG/WebP, 5 MB, re-encode + crop 1500x300 with sharp, store header_url) and link route (https only, no private or link-local IPs after DNS, no redirects to them, size/type limits, timeouts, copy to storage, never hot-link); the bucket does not exist: both routes fail soft (503 + UI message), bucket + policy as a pending migration note; vitest for the SSRF guard and file checks. Badges: badgeRarity(), badge_definitions names, public/badges art, icon fallback (A0's BadgeMedal).
