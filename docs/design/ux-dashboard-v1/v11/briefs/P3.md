# P3 Groups + group hub

Branch `ux11/p3-groups`, dev port 3033. Owned paths: the "P3" globs in `v11/OWNERSHIP.json` + `v11/reports/P3.md`, `v11/reports/P3/**`, `v11/requests/P3.md`. (`/g/[slug]` is the blind-test game and belongs to P6, not you.)
States: `groups`, `hub-blackpink`, `hub-ateez`, `hub-empty`.
Spec: 17.3 (validated; photos from `public/idols/<Group>.jpg` 736 to 1200px via next/image with correct `sizes`, never the 480px copies; 33 groups have a photo, the rest use the typographic cover), 17.10, 17.11, 16.7 groups + hub, 16.10, 15.x group hub template and 12.x hub where 16/17 point.
Scope: /groups index; the `/<slug>-quiz` hub (split hero, facts, quiz cards, about, trivia link, FAQ, community, fans also play, empty group state). The `-trivia` pages keep today's content inside the shell.
Backend: counts from published quizzes (`quizzes where status='published'`), never `groups.quiz_count` (stale: BTS 31 vs 27 published). Visible groups = 90 (the 91st row is the quarantine group).
SEO lock: the hub's H1, intro, existing FAQ text and existing JSON-LD stay verbatim. 16.10 allows adding FAQPage + BreadcrumbList JSON-LD on hubs where they are missing (the FAQPage must match visible text exactly). Do NOT add noindex to groups with few quizzes: the worker prompt asks for "noindex until 3 quizzes" but today's pages have no robots rule, so it is an SEO diff outside 16.10; write it in your report as an owner decision. Proof on `/groups`, `/blackpink-quiz`, `/ateez-quiz`, one near-empty group hub, one `-trivia` page.
