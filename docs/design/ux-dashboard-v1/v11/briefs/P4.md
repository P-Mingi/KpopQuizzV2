# P4 Quiz (spawned 2026-09-25, in flight; kept here for respawn)

Branch `ux11/p4-quiz`, dev port 3034. Owned paths: the "P4" globs in `v11/OWNERSHIP.json` (not `components/quiz/ux-v1/ux-quiz-card.tsx`, A0's) + `v11/reports/P4.md`, `v11/reports/P4/**`, `v11/requests/P4.md`.
States: `quiz`, `play`, `play-answered`, `play-qotd`, `end`, `end-guest`, `share` (A0's ShareSheet wired to results).
Spec: 17.4, 17.10, 17.11 (flush cards, yellow "Did you know" bulb), 16.7 quiz page, 16.10, 14.x quiz page / in-game / results where 16/17 point.
Scope: /q/[slug] page (header card, right column, hall of fame, your best, made by); in-game (timer ring, answer states, relaxed mode, challenge chip, quit confirm via ConfirmSheet, focus shell via useShellMode); results (photocard, stats box, primary action by score, share numbers, comments). quiz-player.tsx and friends are EXISTS: re-skin, never fork scoring or save calls.
Backend: relaxed mode flag on the play record (pending migration, fail soft), excluded from the hall of fame and quiz_time_stats; results numbers from the finished run; comments + heart likes through the existing endpoints (same payloads); challenge runs read the challenge and report win or lose. Saving flows proven by guardWrites payloads.
SEO lock (owner): on /q/[slug] the H1, the intro paragraph, the FAQ text and the JSON-LD do not change; SSR/ISR kept. Before/after SEO diff in the PR on 3 quizzes (image, true/false, classic).
