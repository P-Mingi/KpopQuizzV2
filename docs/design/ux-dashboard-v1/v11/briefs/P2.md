# P2 Quizzes

Branch `ux11/p2-quizzes`, dev port 3032. Owned paths: the "P2" globs in `v11/OWNERSHIP.json` + `v11/reports/P2.md`, `v11/reports/P2/**`, `v11/requests/P2.md`.
States: `quizzes`.
Spec: 17.3 (validated; global pink and border rules only), 17.10, 17.11 (flush cards), 16.7 quizzes, 7 (browse, ONE page), 16.10.
Scope: /quizzes browse: sort, Type / Level / Group dropdowns + chips (A0's UxDropdown, Chip), grid (A0's UxQuizGrid + UxQuizCard, 3 or 4 columns per the prototype; mobile list rows), empty state, pagination with real `?page=N` hrefs. The popular-today / this-week / this-month subpages under /quizzes keep their content and SEO; re-skin them only with A0's grid if it is a pure re-skin.
Backend: filtering and sorting on the server with real counts (published quizzes only). Today the sort param is `?tab=` (not `?sort=`) and `/api/quizzes` has no difficulty filter: add what you need in `lib/ux-v1/p2/` or a new endpoint under `api/ux-v1/p2/`, never change `/api/quizzes` or its payload. `/api/quizzes/count` (A0's) can feed a total.
SEO lock: the canonical / indexability logic in `quizzes/page.tsx` (facets canonical to `/quizzes`, `?group=` canonical to `/<slug>-quiz`, page > 1 self-canonical when indexable), the H1, the intro, the FAQ text and both JSON-LD blocks stay byte-identical in meaning and text. Every filter and page link is a real `<a href>`. Proof on `/quizzes`, `/quizzes?page=2`, `/quizzes?group=bts`, a `?tab=` URL, and `/pt/quizzes` (unowned, unchanged).
