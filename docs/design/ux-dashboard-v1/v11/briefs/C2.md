# C2 Backend checker

Walk every row of `docs/design/ux-dashboard-v1/WIRING-MAP.md` (the old tables + the v10 and v11 tables), corrected by `WIRING-MAP.verified.md`. For each row, on the shared flag-on build: trigger the control as the test user and as a guest where relevant; prove the network call (endpoint, method, status for reads, payload for writes via `guardWrites`); prove the database side with READ-ONLY SQL limited to the test user's rows (service role through supabase-js `select`, never insert/update/delete/rpc that writes); and prove that with the flag off the control does nothing new (flag-off build on your own port: the new endpoints answer 404, no new requests).
Must-prove list (worker prompt section 7, adapted to the no-write rule): the QOTD shown is the current stored QOTD with its real average (and the truthful label while rotation is stopped); ticker lines are real recent activity; filters and counts on /quizzes and /groups; blindtest playlists = 79 groups; daily one try; ranked math (P7's table tests, 20+ cases) + season/tier/division mapping + limits + token replay rejected (unit level, the API fails soft with 503 until the migration); comment like toggles once per user; debate vote once; flair saved (payload) and shown on posts, comments and the hall of fame (read); header upload and link: type, size, SSRF block (unit tests + the 503 fail-soft); badge rarity mapping; sign-in returns to the action; the streak rule (any quiz or blindtest per the spec, vs the live rule); notifications mark read (payload).
Run the data guard (`pnpm --filter quiz data-guard`, read only, prints counts only) before and after: no other user's row changed, counts only grew.
Evidence: `v11/checks/backend/<row id>.md` per row (control, call, payload or response, SQL and result, verdict: pass / fail / NOT verified and why), and a summary table in `v11/checks/backend/README.md`. File failures to the row's owner.
