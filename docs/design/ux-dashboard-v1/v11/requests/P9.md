# Requests from P9

## 1. (Low priority) A shared accordion item

- What: a server-safe `UxAccordion` / `AccItem({ title, children, open? })` in `components/ux-v1/`: native
  `<details class="ux-acc">` + `<summary>` (min-height 56, padding 12/0, 16/600, chevron 20px muted that turns
  180deg when open) + `<div class="ux-ab">` (15/1.65 muted, max 62ch, `b` in ink), the hairline top and bottom with
  no double line between two items: the prototype's `.acc` / `.acc summary` / `.acc .ab` rules.
- Why: P6 (blindtest FAQ), P7 (How ranked works) and P9 (How points work) each carry a copy of the same CSS
  (`.p7-acc`, `.p9-acc`, P6's own). One component keeps them identical (and crawlable: native `<details>`, content
  in the HTML).
- Prototype state: `leaderboard` (How points work), `ranked` (How ranked works), `blindtest` (FAQ).
- Not blocking P9: `.p9-acc` in `styles/ux-v1/p9.css` matches the prototype today.
