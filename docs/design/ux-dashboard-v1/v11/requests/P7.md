# Requests from P7

## 1. Shell: keep the `/search` link (link-set rule, COMMON done-when 5)

- What: the flag-on shell drops the only `<a href="/search">` of every page. Measured on production builds of
  this branch (flag off = base `origin/feat/ux-v1-v11` f054cd3, flag on = `ux11/p7-ranked`), server HTML link
  sets: `/blindtest` 121 -> 137 (lost `/search`), `/blindtest/classic` 22 -> 37 (lost `/search`), `/quizzes`
  80 -> 90 (lost `/search`). The legacy header links to `/search`; the v11 nav opens the search overlay from a
  button, so the link is gone from every flag-on page.
- Why: COMMON done-when 5 now says a lost internal link is a blocker. `/search` is a real page (in the route
  allowlist); not caused by P7 (P7 renders only on `/blindtest/ranked`), but it fails the rule for every page
  agent's URLs.
- Proposal: make the nav search control an `<a href="/search">` that opens the overlay on a plain click
  (keeps "/" and the icon-button look), or add `/search` to the footer.
- Prototype state: any (nav).
