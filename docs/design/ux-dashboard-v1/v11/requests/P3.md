# P3 requests to A0

## 1. Shell: keep a real `<a href="/search">` (link-set rule, COMMON done-when 5)

- What: the flag-off chrome links `/search` twice (legacy top nav search icon and the footer); the v11 shell
  has no `<a href="/search">` at all (the nav search is a `<button>` that opens the overlay). The link-set diff of
  every P3 URL (`/groups`, `/<slug>-quiz`, `/<slug>-trivia`) therefore lists `/search` as the only lost link, and it
  is the same on every page of the run (P10's `seo-diff.txt` already shows it: "only in A 1: /search").
- Why: the ORCH rule of f054cd3 makes a lost internal link a blocker, and it is a shell link (A0's paths), so P3
  cannot add it without copying a shared component.
- Suggested fix (either): make the nav search control an `<a href="/search">` that opens the overlay on a plain
  click (`preventDefault`, modifier clicks keep the page), or add a "Search" link to the footer (Explore column).
- Prototype state: every state (nav `.sbtn`, `home`); no visual change needed for the first option.
