# P11 Notifications + search + bell report (UX v11.2)

## Progress

- Done (branch `ux11/p11-notifications`): /notifications v11 page (flag on) on the live center's calls; `BellPanel`
  and `SearchResults` with exactly the slot props; new read-only `GET /api/ux-v1/p11/search`; `p11.css`; prototype
  style + box capture (`P11/proto-styles.json`); 31 unit tests; `e2e/ux-v1/p11.spec.ts` (22 cases per width) green at
  1440 and 390, light and dark, against the flag-on dev server with the A0 swap applied LOCALLY (uncommitted);
  request file `v11/requests/P11.md` (A0: swap the two slot exports, Mark all read in the bell header).
- Next: /search page v11 skin (owned path, link set kept), flag-off builds + diff, SEO + link-set diff, lint,
  report images, PR.
- Blockers: none. The bell and overlay content go live only when A0 swaps the slot exports (after this PR merges).
