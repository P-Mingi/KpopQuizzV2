# P4 Quiz report (UX v11.2)

## Progress

- Done (b0dd2f8, WIP): flag-on /q/[slug] render (server, ISR kept) with the prototype layout; flag-off JSX
  unchanged (breadcrumb items and the Quiz JSON-LD object are now shared consts, same output); run engine
  (the EXISTS player's rules + save payload) in lib/ux-v1/p4/engine.ts; game, results, comments, report
  sheet, share + challenge, Continue store; flag-gated API (relaxed mark, standing, challenge create /
  read / attempt); pending migrations v11-p4-relaxed-runs.sql and v11-p4-rank-for-score.sql.
- Next: tsc + lint clean, render and fix against the reference at 1440 / 390 light / dark, unit tests
  for the engine, e2e/ux-v1/p4.spec.ts with guardWrites, axe, flag-off diff + SEO diff, screenshots,
  report, PR into feat/ux-v1-v11.
- Blockers: none yet. Owner decision pending for all P4 saving flows (production writes, RUN-STATE 1):
  every write is proven by the payloads guardWrites records, never sent.
