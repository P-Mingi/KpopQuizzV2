# P4 Quiz report (UX v11.2)

## Progress

- Done: flag-on /q/[slug] (server rendered, ISR kept), quiz game, results, share + challenge, Continue,
  relaxed mode (fail soft), flag-gated API, two pending migrations, engine parity tests, screenshots vs
  reference, SEO diff (none), flag-off diff (identical), this report. Final e2e: 44 passed, 1 skipped by
  design, 0 failed (1440 + 390, light + dark). PR into feat/ux-v1-v11:
  https://github.com/P-Mingi/KpopQuizzV2/pull/46
- Next: ORCH review and merge (C1 / C2 / C3 checks). Owner: apply the two pending migrations (the relaxed
  control and the guest rank line switch on by themselves), and the production-write decision for the
  saving flows.
- Blockers: none in code. Every saving flow is proven by the payloads guardWrites records, never sent
  (RUN-STATE owner decision 1).

## 1. What I built

| Area | Where |
|---|---|
| /q/[slug] flag-on render, server component (route `revalidate` and `generateStaticParams` unchanged), prototype `#quiz` in the 720 column: crumb, cover, H1, meta line, author + Follow, Start + share, timer rule + "Play without a timer", Hall of fame + Your best / Beat it / Challenge a friend, Did you know + trivia link, About box, In this quiz (3) + every question in a native `<details>`, More {group} quizzes (text cards) + article links, Made by + Report, sticky Start on phones | `app/(site)/q/[slug]/page.tsx` (flag branch), `app/(site)/q/[slug]/ux-page.tsx`, `components/quiz/ux-v1/quiz-page.tsx` |
| Flag off: the legacy JSX is untouched; the breadcrumb items and the Quiz JSON-LD object became shared consts (same values, same key order) so both renders emit the same objects; the legacy hall-of-fame read is skipped only when the flag is on | `app/(site)/q/[slug]/page.tsx` |
| Quiz run: client provider around the server page (Start, `?resume=1`, `?c=` challenge, sign-in continuations), focus shell while playing (`useShellMode('focus')`), timer, answers, save, relaxed mark, challenge attempt, share sheets, quit confirm (A0 `ConfirmSheet`) | `components/quiz/ux-v1/quiz-run.tsx`, `run-context.tsx` |
| Game (`#play`): game bar (quit, title or "Beat {user}: 7/8" chip, segmented progress, "N in a row" from 3, score, sound), timer ring 76 with the number alone (warn 8, danger 5 + pulse, frozen green / red after the answer), question h1 focused on each question, answers with 1-4 keys (A-D on touch), Correct / Your pick labels, "Did you know" (yellow bulb), Next + Enter (focused after the answer), live-region announcements; image, true/false, clue and intruder variants | `components/quiz/ux-v1/game.tsx` |
| Results (`#end`): hidden H1 "8/8 on {quiz}", photocard (count-up, `getResultLabel` stamp, mascot on the seam, confetti on a pass, "Play no. N"), XP + streak line / guest "Save your 8/8 and start a streak. Sign in to save", You / Average / Time (A0 `UxStatsRow`), rank line or the challenge win / lose line, primary by score (Share at or above the quiz average, Play again below), Like pill, Keep playing (next quiz + one row + the group blindtest), comments folded, Report | `components/quiz/ux-v1/results.tsx`, `comments.tsx`, `report.tsx` |
| Share: A0 `ShareSheet` for the quiz page ("Share this quiz"), your best ("Challenge a friend") and the result ("Share your score": score, beat %, rank from the run; Story image = 1080 x 1920 PNG drawn in the browser; More apps shares it as a file where supported; challenge link block) | `quiz-run.tsx`, `story.ts` |
| Engine: the EXISTS player's answer rules, reducer and save payload in one pure module, + RESUME | `lib/ux-v1/p4/engine.ts` |
| Challenges on the existing `battles` / `battle_results` tables, signed links | `lib/ux-v1/p4/challenge.ts`, `challenge-server.ts`, `app/api/ux-v1/p4/challenge/**` |
| Continue playing store (read by P1) | `lib/ux-v1/p4/continue.ts` |
| Server reads (hall of fame minus relaxed runs, creator counters, published count of the group, group playlist, relaxed probe), all `unstable_cache` at the stats TTL | `lib/ux-v1/p4/queries.ts` |
| Standing (read only; signed in: `get_quiz_rank`; guest score: `get_quiz_rank_for_score` once applied) | `app/api/ux-v1/p4/standing/route.ts` |
| Relaxed-run mark (own row, 15 min window, 503 `not_live` until the column exists) | `app/api/ux-v1/p4/relaxed/route.ts` |
| Stylesheet (never imported; A0's flag-on route serves it) | `styles/ux-v1/p4.css` |
| Tests: 25 unit (engine parity with quiz-player.tsx, payload, challenges, formats, no em / en dash), e2e | `lib/ux-v1/p4/engine.test.ts`, `e2e/ux-v1/p4.spec.ts` |

Every v11 API route answers 404 with the flag off. No new dependency. No new public URL.

### Decisions and deviations (for ORCH / owner)

1. Breadcrumb stays "Home / {Group} Quiz / {title}" (its BreadcrumbList JSON-LD is locked); the prototype's
   "Quizzes / BTS / {short title}" would change it. One line, the title ellipsizes.
2. Crawlable content kept inside the prototype layout: the stats-block numbers moved to the meta line
   (plays, average), the hall-of-fame header (perfect scores, fastest) and one muted line in the About box
   (pass rate, likes, reactions, comments); the full question list is a native `<details>` under "In this
   quiz"; "Read more about {group}" links sit under the text cards (groups with articles only).
3. Hall of fame = today's board (best row per signed-in player, guest rows shown as "someone"), top 5,
   minus relaxed runs once the column is live. On popular quizzes the top 5 are often guests; showing
   signed-in players only is an owner call.
4. Relaxed runs: saved through the existing endpoint with the same keys and `time_taken_seconds: null`
   (so `quiz_time_stats` and "fastest" ignore them), then marked by the new route. Challenge runs always
   use the timer.
5. Challenge links are `/q/<slug>?c=<id>.<sig>` (canonical unchanged). The HMAC signature (server secret:
   `UX_P4_CHALLENGE_SECRET`, else derived from the service role key; never sent to a client) is what makes
   a `battles` row a challenge, because that table accepts public inserts (migration 073). A guest
   challenger shows as "your friend".
6. "Play again" replays the same quiz (prototype), not `/quizzes` (legacy).
7. Stamp words come from `getResultLabel()` (a perfect run is "올킬! PERFECT"; the prototype sample used
   "대박!"), trailing "!" dropped as in 14.4.
8. Touch devices show A-D chips and hide the Enter hint (16.7 "A-D on touch", 16.9 "key hints hidden on
   touch"); the 390 reference PNGs were captured without `(hover: none)` and show 1-4.
9. Not shown because no store exists (no dead controls): hearts and replies on quiz comments (no
   quiz-comment like table; P8 may define one); the legacy Discord results line and Brag button (the
   share sheet has Discord). The level-up overlay and the streak-backup nudge are the EXISTS components.
10. The group's blindtest row reads the playlist list once an hour (`unstable_cache`); on a cold cache the
    page's 5 s fetch budget can fall back to the generic "K-pop blindtest" row for that hour (fail soft).

## 2. Screenshots (prototype | implementation)

Each image: the pinned prototype state (reference PNG from `v11/checks/reference/`) on the left, the
implementation on the right, same width and theme (1440 images are halved). Real data replaces the
prototype samples (quiz "Ultimate BTS era quiz" by mina, 2,376 plays; the test user's own best 1/8; the
current quiz of the day "Stray Kids District 9" for `play-qotd`). The results images use a stubbed save
response (percentile 96, +60 XP) because a real save would write production; every other number is real.
Signed-in states (`quiz`, `end`, `share`) use the parity test user's session (read only).

| State | 1440 light | 1440 dark | 390 light | 390 dark |
|---|---|---|---|---|
| quiz | ![](P4/quiz-1440-light.webp) | ![](P4/quiz-1440-dark.webp) | ![](P4/quiz-390-light.webp) | ![](P4/quiz-390-dark.webp) |
| play | ![](P4/play-1440-light.webp) | ![](P4/play-1440-dark.webp) | ![](P4/play-390-light.webp) | ![](P4/play-390-dark.webp) |
| play-answered | ![](P4/play-answered-1440-light.webp) | ![](P4/play-answered-1440-dark.webp) | ![](P4/play-answered-390-light.webp) | ![](P4/play-answered-390-dark.webp) |
| play-qotd | ![](P4/play-qotd-1440-light.webp) | ![](P4/play-qotd-1440-dark.webp) | ![](P4/play-qotd-390-light.webp) | ![](P4/play-qotd-390-dark.webp) |
| end | ![](P4/end-1440-light.webp) | ![](P4/end-1440-dark.webp) | ![](P4/end-390-light.webp) | ![](P4/end-390-dark.webp) |
| end-guest | ![](P4/end-guest-1440-light.webp) | ![](P4/end-guest-1440-dark.webp) | ![](P4/end-guest-390-light.webp) | ![](P4/end-guest-390-dark.webp) |
| share | ![](P4/share-1440-light.webp) | ![](P4/share-1440-dark.webp) | ![](P4/share-390-light.webp) | ![](P4/share-390-dark.webp) |

Before (flag off, today) | after (flag on) | prototype, 1440 light:
![](P4/before-after-prototype-quiz-1440-light.webp)
![](P4/before-after-prototype-play-answered-1440-light.webp)
![](P4/before-after-prototype-end-1440-light.webp)

What differs from the reference, and why (layout lines up to the pixel otherwise, see 3.2):
- real content: cover photo, titles, names, counts, question text; the hall of fame of this quiz is five
  guest runs ("someone"), the test user's best is 1/8, "#36 of 41 players" counts signed-in players;
- guest results have no rank line until `v11-p4-rank-for-score.sql` is applied (the next section moves up
  by one line);
- 390: A-D answer chips and no Enter hint on touch (decision 8); the reference was captured with hover;
- the gbar title is the full quiz title with an ellipsis (there is no short title in the data);
- share 390: the challenge label wraps differently and "Copy link" is not focused on open (A0's
  ShareSheet, requests/P4.md item 5).

## 3. Proofs

### 3.1 Checks

```
(re-run on 23daccb, the final code)
tsc --noEmit (apps/quiz)                      0 errors
eslint (eslint-config-next core-web-vitals + typescript) on the 25 P4 .ts / .tsx files: 0 problems
vitest run src/lib/ux-v1/p4                   25 passed
em / en dash in P4 sources (a unit test)      none; none in any branch file or commit message either
ux11-owner-guard --range origin/feat/ux-v1-v11..HEAD   P4 ok (66 files)
git merge-tree origin/feat/ux-v1-v11 (2abb108) HEAD    clean (integration moved by docs only)
```

### 3.2 e2e (`e2e/ux-v1/p4.spec.ts`, flag on, dev :3034, Chromium 1234 via UX11_CHROMIUM)

```
PLAYWRIGHT_BASE_URL=http://localhost:3034 UX11_CHROMIUM=... playwright test e2e/ux-v1/p4.spec.ts --project=ux-1440 --project=ux-390
Running 45 tests using 1 worker   (setup + 22 tests x 2 widths; the per-theme tests run light and dark)
  44 passed, 1 skipped (the phone-only sticky Start at 1440), 0 failed   (8.5 min)
```
Final run: commit 23daccb, on a fresh flag-on dev server (`apps/quiz/.next` removed). Earlier full runs on
the same machine (load average 60 to 130: other agents' servers and suites, the production database
shared) failed only on live reads: page renders lost to the 5 s `safeFetch` fallback (HTTP 200 without
the page) and question reads of up to 72 s. The spec now retries a lost render twice (never skips) and
gives live reads 90 s. Also found: a dev server left running for 3 hours had missed one edit in its server
bundle (a hydration warning on the Report button's test marker); a fresh `.next` fixed it (dev only: a
production build compiles both bundles from the same source).
Sheets are A0's `Sheet`, `ShareSheet` and `ConfirmSheet`: X, Escape and backdrop close with focus return
are covered by A0's kit spec; this spec checks Escape and focus return on the share sheet, Escape on the
sign-in sheets, and the first focus of the report sheet (first reason) and of the quit confirm (Keep
playing).

Covered, all with `guardWrites` (every POST is answered locally and its payload asserted):
- quiz page per theme: one H1 = the title; the server HTML carries the Quiz and BreadcrumbList JSON-LD,
  the intro, "Show the N questions" and the group link; crumb, meta line, author link, timer rule, hall of
  fame (at most 5), your-best row, About box, In this quiz (3 + 1), the question list opens in place, text
  cards link to /q/, trivia link; no horizontal scroll; reference styles; basic a11y; axe; zero writes on
  load;
- game per theme: focus shell (nav and footer hidden), one H1, question focused, progress, ring role /
  label, reference styles before and after the answer, answer states and labels, fact, Next focused, live
  region text, axe, Next moves on and refocuses, zero writes before the last question;
- results per theme (guest): exactly one save with the legacy keys in the legacy order and the run's
  values; hidden H1, score, stamp, nav back, "Save your 8/8 ...", stats row, Share primary on a perfect
  run, Keep playing links; reference styles; basic a11y; axe;
- keyboard (1-4 answer, Enter next, key hints per pointer), timer (fake clock: warn, danger, "Time is up",
  wrong mark), quit (no answer: leave at once; after an answer: confirm with Keep playing focused, Leave
  saves to Continue, `?resume=1` resumes at the saved question and cleans the URL), share this quiz (copy,
  Escape closes, focus returns, no write), results share (score line, the four buttons, challenge block;
  challenge POST = { quizId, the run's question texts in played order, score }), low score (Play again
  primary, restarts), like payload { action: 'like' }, comments (guest -> "Sign in to reply", nothing
  sent), report payload { reason, details }, guest Follow and "Sign in to save" sheets, challenge link
  (chip on the page and in the game, "You beat mingi (5/8) ...", attempt { sig, score, perQuestion,
  timeMs }), quiz of the day (?daily=quiz posts { kind: 'quiz' } to /api/daily/complete), level-up overlay
  from the save response, true/false, image, clues (reveal, points line), intruder, relaxed fail-soft,
  sticky Start on phones, read-only API guards (bad signature 404, missing quiz 400, guest standing 200);
- signed in (test user, read only): no Sign in button, your-best row, follow payload, the run's save
  payload, "Saved to your passport", the rank line from get_quiz_rank, comment payload { content }, and no
  other write attempt.

### 3.3 Reference styles (C1)

`compareLandmarks` (A0 helper) against `styles.json`, 0 mismatches at 1440 and 390, light and dark:
`.aboutbox` -> `.p4-about.ux-box` and `.tcard` -> `.ux-tcard` (state quiz), `.sec-h h2`, `.btn-primary`
(quiz, play-answered, end-guest), `.tring` -> `.p4-tring` with width and height (play, play-answered),
`.stats3` -> `.ux-stats3` with height (end-guest). Positions: the side-by-side images line up edge to edge
(crumb, cover, H1, meta, buttons, hall of fame rows, ring, question, answers, fact, Next, photocard, stats
row, action row, Keep playing).

### 3.4 Engine = the EXISTS player ("never fork the scoring or the save calls")

`engine.test.ts` reads `components/quiz/quiz-player.tsx`, transpiles its `getEffectiveOptions`,
`getCorrectIndex`, `isAnswerCorrect` and `quizReducer` and runs them next to the v11 engine: same result
on every answer of 6 question shapes (classic, bare true/false, true/false with options in both orders,
clues) and the same state after every action of 4 runs (classic right / wrong / timeout, untimed, clues
with reveals and a timeout, ignored actions). The save payload keys are read from the legacy
`JSON.stringify({...})` and must equal `playPayload()`'s, in order.

### 3.5 SEO diff, flag on vs flag off (the SEO lock)

`v11/reports/P4/seo-diff.txt` (script: `v11/reports/P4/seo-diff.mjs`): two dev servers of the same commit,
flag off (:3044) and flag on (:3034), fetched back to back (live data, no build-time drift):

```
/q/ultimate-bts-era-quiz-only-real-armys-survive (classic), /q/skz-true-or-false-only-real-stays-pass
(true/false), /q/real-coers-cortis-fans-can-take-this-quiz (image):
  SAME status, title, meta description, og:* (9), twitter:* (4), canonical, hreflang (none on /q)
  SAME H1, all JSON-LD blocks (BreadcrumbList, Organization, Quiz, SiteNavigationElement: byte-equal)
  SAME no FAQ on quiz pages (no FAQPage either side)
  SAME intro paragraph (flag-off .quiz-about-lead text found verbatim in the flag-on HTML)
  SAME question review texts (8/8, 8/8, 5/5)
  SAME internal links of the page (12, 12, 11 in flag off; every one served flag on)
  SAME server rendered (the flag-on HTML carries the content: 80 to 86k characters of text)
/pt/q/<slug> x 3: 404 / 404 (quiz pages have no /pt mirror today; unchanged)
no SEO difference
```
Whole-document links: the only flag-off href missing flag on is `/search` in the legacy top nav (A0's
shell opens the search overlay instead); P4's page content keeps every link.

### 3.6 Flag off = today's site

Full record: `v11/reports/P4/flag-off.txt`.

- Production builds of the base and of this branch, flag off (803 / 803 static pages each), served with
  `next start`: `flag-off-diff.mjs --structure` IDENTICAL on `/q/skz-true-or-false-only-real-stays-pass`,
  `/q/real-coers-cortis-fans-can-take-this-quiz`, the three `/pt/q/<slug>` (404 both) and `/`. The BTS page
  differed only because this branch's build baked the legacy empty hall of fame after 15 hall-of-fame DB
  timeouts (253 "timed out after 5000ms" fallbacks during that build on a saturated machine; 0 in the base
  build); `/quizzes` differed only in live play counts (builds 45 min apart; not a P4 route).
- Live renders (two flag-off dev servers, fetched back to back, dev-only noise removed): the three quiz
  pages and the `/pt` mirror are IDENTICAL, the BTS page with its full hall of fame on both sides.
- JS weight, flag off: `/q` pages list 15 / 15 chunks, 999,289 B vs 1,001,093 B (+1,804 B of `next/dynamic`
  loaders); `/` and `/quizzes` unchanged. Before commit 880f278 the flag-off `/q` pages carried one extra
  ~71 KB chunk of P4 client code; this proof caught it and the islands are now code-split.
- Bytes: only the RSC payload's client module ids differ (as in A0's proof).

### 3.7 Signed-in reads (never /profile or /me)

The only signed-in page P4 loads is /q/[slug] (ISR server render, no session read, no write). Its client
reads, checked in the code before loading: GET /api/auth/me, /api/ux-v1/p4/standing (get_quiz_rank, a
STABLE SQL function), /api/follow?username (select only), /api/quiz/[id]/like (select only),
/api/quiz/[id]/comment (select only), /api/quiz/[id]/questions, and A0's shell reads. The test user's real
standing is quoted in the screenshots ("Your best 1/8 · 3 days ago", "#36 of 41 players").

## 4. Wiring (WIRING-MAP sections 3, 4, 5 + v10 + v11, corrected by the verified map)

| Row | Wired to | Proof |
|---|---|---|
| Breadcrumb | same items; BreadcrumbList JSON-LD byte-equal | SEO diff; e2e hrefs |
| Cover, type, level | `cover_image_url`, else the group photo (public/idols, next/image, sizes); `quiz_type`, `difficulty` | screenshots; e2e meta |
| H1 | `quizzes.title` | SEO diff |
| Author + level title | `profiles.xp` -> `getLevelInfo` | e2e (/u/ link) |
| Follow | GET + POST /api/follow { username, action } (FollowButton's calls); guest: sign-in sheet, resumes after sign-in | e2e guest sheet; signed-in payload |
| Meta: plays, questions, time, average | `play_count`, count, `settings.timer_seconds` x count, average gated like the intro | e2e |
| Start quiz | GET /api/quiz/[id]/questions, same settings | e2e |
| Play without a timer | hidden until `plays.relaxed` exists; then timer off, save with time null, POST /api/ux-v1/p4/relaxed { playId }; hall of fame `relaxed = false` | e2e fail-soft; unit (payload) |
| Challenge a friend | POST /api/ux-v1/p4/challenge -> `battles` (snapshot from stored questions), signed link | e2e payload; unit (snapshot, link) |
| Share | A0 ShareSheet; signed in: POST /api/share/generate { quizId, platform: 'link' } (lib/share.ts), guest: the UTM URL | e2e (guest, no write) |
| About (avg, perfect count) | the page's intro paragraph, verbatim; creator note; counts line | SEO diff |
| In this quiz | 3 prompts + every question in `<details>` (options, clues, fun facts; never the answer) | e2e; SEO diff |
| Did you know + trivia | the page's fact pool; `/{group}-trivia` when the page exists | e2e |
| More group quizzes | the page's related list, text cards; "All N" = published count | e2e; SEO links |
| Stats card | numbers kept: meta line, hall-of-fame header, About counts line | SEO diff (text) |
| Hall of fame | the legacy plays read (+ relaxed filter when live), top 5 | e2e |
| Your best + Beat it | GET /api/ux-v1/p4/standing (get_quiz_rank + date of the best row) | signed-in e2e; screenshot |
| Made by | `profiles.total_quizzes_created`, `total_plays_received` | screenshot |
| Report | POST /api/quiz/[id]/report { reason, details } | e2e payload |
| Owner actions | EXISTS QuizOwnerActions (owner only) | render |
| Quit (+ confirm, Continue) | ConfirmSheet; `ux:continue:v1` (localStorage), `?resume=1` | e2e |
| Group tag, progress, counter, score | run state (title / challenge chip, segmented bar) | e2e |
| Sound toggle | lib/sounds `setSoundEnabled` (same `sound_enabled` key) | e2e (aria) |
| Streak | "N in a row" from 3 | e2e |
| Timer ring | `timer_seconds`, warn 8, danger 5, frozen after the answer | e2e (fake clock) |
| Answers, image / intruder / clues | engine = quiz-player rules | unit parity; e2e |
| Fun fact | `fun_fact`, else "The answer is X." (legacy) | e2e |
| Next / See your result, keyboard | Enter, 1-4 | e2e |
| Per-question times | `per_question_times` in the save payload | e2e payload |
| Challenge chip + win / lose | GET /api/ux-v1/p4/challenge/[id]?s=; POST .../attempt { sig, score, perQuestion, timeMs } -> `battle_results` | e2e (mocked GET, payload); route 404 on a bad signature |
| Save (results numbers) | POST /api/quiz/[id]/play, the legacy payload | e2e + unit |
| Photocard | percentile from the save, `play_count + 1`, `getResultLabel` | e2e |
| XP + streak line | `xp_earned`; `daily_streak` via /api/auth/me after `refreshUxMe()` | signed-in e2e |
| Guest save | sign-in sheet "Save your 8/8"; back signed in: POST /api/claim-runs { anonId } (ClaimRun's call) | e2e (sheet) |
| You / Average / Time | the run's %, the quiz average, the run time | e2e |
| Rank line | standing: signed in get_quiz_rank; guest get_quiz_rank_for_score (pending) | signed-in e2e |
| Primary by score | run % vs the quiz average | e2e (both branches) |
| Like | GET + POST /api/quiz/[id]/like { action } + the `anon_liked_quizzes` key | e2e payload |
| Level up | EXISTS LevelUpOverlay from the save response | e2e |
| Keep playing | the page's related list + `/blindtest/group-<slug>` (playlist rule) | e2e |
| Share numbers, story, more apps | score / beat % / rank of the run; canvas PNG; navigator.share with the file | e2e (sheet, payload) |
| Comments | GET + POST /api/quiz/[id]/comment { content } (200 chars; the endpoint attaches the best score) | e2e payloads |
| Daily quiz | `?daily=quiz`: recordGuestDaily + POST /api/daily/complete { kind: 'quiz' } | e2e payload |
| Streak backup nudge | EXISTS StreakBackup | render |

## 5. Pending migrations (written, never applied)

- `docs/pending-migrations/v11-p4-relaxed-runs.sql`: `plays.relaxed boolean not null default false`
  (metadata-only on PG 11+, no backfill). Until applied: no relaxed control, `/api/ux-v1/p4/relaxed` 503,
  hall of fame as today.
- `docs/pending-migrations/v11-p4-rank-for-score.sql`: `get_quiz_rank_for_score(uuid, int)`, a STABLE
  read-only twin of `get_quiz_rank` for a guest's score. Until applied: no guest rank line.

Both files end with a commented rollback.

## 6. What is NOT verified, and why

- Every write against a real database (the run's save, like, comment, follow, report, relaxed mark,
  challenge create / attempt, claim-runs, daily completion, share link): production only, owner decision
  1. Proven by request shape (payloads) and the unchanged endpoints.
- The relaxed flow and the hall-of-fame filter with the column live, and the guest rank line: need the
  two migrations.
- Sign-in continuations after a real OAuth / magic link (`p4-save`, `p4-comment`, `p4-follow`): no agent
  may sign in another way; the sheet stores the action (A0), the resume code is covered by review only.
- The challenge API against the database (create, read, attempt): the client flow is proven with a mocked
  read and recorded posts; the read route's signature check is proven (404).
- Story image and More apps: the PNG is generated in the browser; the OS share sheet is not reachable
  headless.

## 7. Open items and owner decisions

1. Apply the two pending migrations (after a backup point).
2. Production writes (RUN-STATE decision 1) for the saving flows above.
3. Hall of fame: keep guests ("someone") as today, or signed-in players only.
4. `battles` accepts public inserts (migration 073); P4 only trusts signed links, but tightening that
   policy is an RLS change (owner call, like the ranked_plays one).
5. Challenge secret: set `UX_P4_CHALLENGE_SECRET` in Vercel to decouple challenge links from the service
   role key (optional; without it the key is derived from the service role key, server side only).

## 8. Requests (v11/requests/P4.md)

1. ORCH: make quiz-player.tsx import the engine (one copy of the rules; quiz-player.tsx is unowned).
2. ORCH (optional): `/c/<code>` short challenge route + allowlist.
3. A0: `useUxMe()` should be `null` until mounted (SSR / hydration mismatch trap for page islands).
4. P1: read "Continue playing" from `lib/ux-v1/p4/continue.ts` (`readContinueRuns`, `resumeHref`).
5. A0: ShareSheet on phones (label wrap; focus "Copy link" on open).

