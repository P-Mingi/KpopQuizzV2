# P1 Home report (UX v11.2)

## Progress

- Done: QOTD root cause found (code + read-only queries) and fixed in code behind the server env
  switch `QOTD_ROTATION_FIX` (default OFF: production unchanged); v11.2 home behind
  `NEXT_PUBLIC_UX_V1` (ticker, centred header, quiz of the day, continue, groups, trending, all time
  best | new, blindtest band, community), real cached fail-soft reads, static/ISR kept; `p1.spec.ts`
  37/37 on the dev server (1440 + 390, light + dark, guest + signed in read only), axe 0
  serious/critical; flag-off parity (`/`, `/pt` structure identical) and SEO diff (empty) done on
  production builds.
- Next: on the flag-on production build (:4003) the ticker and signed-in cases time out (page `load`
  never fires in a context with the test-user cookies); investigate, then screenshots, data guard
  after, cleanup, PR into feat/ux-v1-v11.
- Blockers: none.

## 1. What I built

| Area | Where |
|---|---|
| Flag branch on `/` (legacy JSX untouched; the WebSite JSON-LD is one shared object) | `src/app/(site)/page.tsx` |
| Home page body (server, static/ISR, every read `safeFetch` + min-gate) | `src/components/home/ux-v1/ux-home.tsx` |
| Live ticker: the live `ActivityTicker` imported as is, restyled from a wrapper (17.11: 44/40px, radius 14, `--line`, LIVE 11/800 pink-ink, line 14/13), the slot keeps its height (no jump) | `ux-home.tsx`, `src/styles/ux-v1/p1.css` |
| Centred header: guest markup server-rendered with production's H1 / H2 / CTAs (SEO lock), greeting + one streak line for signed-in fans (client island) | `home-header.tsx` |
| Quiz of the day row (17.2 / 17.10 / 17.11): label, truthful note, real title, meta line (type, level, questions, real average, real time), one Play (`/q/{slug}?daily=quiz`); no group tag, no preview | `qotd-row.tsx`, `qotd-note.tsx` |
| Continue playing (signed-in only, device runs, min-gate) + the run store P4 writes | `continue-playing.tsx`, `src/lib/ux-v1/p1/continue-store.ts` |
| Groups rail: 8 most played groups + 2 with a quiz in the last 14 days ("New quiz"), published counts, `/{slug}-quiz`, "All 90 groups" | `groups-rail.tsx` |
| Trending this week (real plays of the last 7 days, one card per group photo), All time best (play_count, numbered, top 3 pink-ink), New quizzes (type glyph, age); no quiz twice | `quiz-rows.tsx`, `ux-home.tsx` |
| Blindtest of the day band (day mode): real "N fans played today", time left, played state (score, rank, See today's board), "Your best N/10" | `blindtest-band.tsx`, `src/app/api/ux-v1/p1/daily-band/route.ts` (GET, read only, 404 flag off) |
| From the community: today's debate (votes), latest thread (replies), latest featured essay (read time) of the opted-in spaces | `community-rows.tsx` |
| Reads (cached, public, cookie-free) and pure helpers | `src/lib/ux-v1/p1/home-data.ts`, `format.ts` |
| Client islands behind next/dynamic, dead code on a flag-off build | `islands.tsx`, `use-client-values.ts` |
| QOTD rotation fix (section 2) | `src/lib/quiz-bank-scheduling.ts`, `src/lib/ux-v1/p1/qotd-rotation.ts`, `src/app/api/cron/ensure-daily-quiz/route.ts`, `src/app/api/qotd/publish/route.ts` |
| Tests | `src/lib/ux-v1/p1/p1.test.ts` (21 unit), `e2e/ux-v1/p1.spec.ts` |

Stylesheet `src/styles/ux-v1/p1.css` is not imported (A0 route serves it flag-on only).

## 2. Quiz of the day: why the rotation stopped, and the fix

### 2.1 Evidence (read-only queries on 2026-09-25, anon key and service role SELECT only)

- `qotd_log`: 82 rows, 2026-04-06 to 2026-06-30, all `selection_method = 'bank'`, two gap days
  (06-18, 06-27). Nothing after 2026-06-30.
- `quiz_bank`: 100 rows. 82 `published` (one per logged day), 18 `scheduled`, 0 `verified`, 0 `draft`.
  The 18 scheduled rows: 2 on missed dates (2026-04-03, 2026-04-05) and 16 between 2026-12-15 and
  2030-01-28. All were dated by one auto-schedule run (`updated_at` 2026-04-01 / 04-02).
- `quizzes.is_quiz_of_the_day = true`: 84 rows (the flag is never cleared); newest date 2026-06-30
  (Stray Kids: District 9 Quiz).

### 2.2 Root cause (code)

1. The cron (`vercel.json` `5 0 * * *` -> `/api/cron/ensure-daily-quiz`) calls `ensure_daily_quiz(today)`,
   which publishes a `quiz_bank` row only if its `scheduled_date` is EXACTLY today.
2. The bank was dated once by `autoSchedule` (`lib/quiz-bank-scheduling.ts`). Its walk compares a
   candidate with the closest scheduled entry at ANY distance and may jump up to 365 days per quiz, so
   the last rows of the bank land years ahead with empty days in between (unit test reproduces it: 6
   same-category quizzes, only 1 placed). From 2026-07-01 no row matches "today".
3. The RPC then returns NULL; the cron ignores the result and answers `{ ok: true }`: a silent stop.
4. The catalog fallback `/api/admin/auto-select-qotd` exists but is not in `vercel.json` (and is
   POST-only while Vercel Cron sends GET), so nothing fills an empty day.
5. The live home masks it: `getQuizOfTheDay` falls back to an unlogged replay of past picks (today
   "K-pop Training System and Idol Life", the pick of 2026-04-20; `/daily` uses it too), while the
   Discord bot reads the latest stored pick (2026-06-30). Three surfaces, two different "quizzes of
   the day".

### 2.3 Fix (code, behind `QOTD_ROTATION_FIX`, default OFF)

- `rotateQotd` (`lib/ux-v1/p1/qotd-rotation.ts`), run by the cron and by the manual
  `/api/qotd/publish` when the switch is on: keep today's pick if it exists; else the existing RPC (a
  bank row dated today); else pull the next open bank row to today (missed dates first, then undated,
  then the earliest future date, avoiding yesterday's group / category) and run the RPC again; else a
  deterministic catalog pick (5+ questions, fewer than 3 reports, played at least once, not featured in
  the last 90 days of `qotd_log`, another group than the last 3 days, ranked like the admin
  auto-select). The catalog pick makes the same three writes as the admin routes (`set-qotd`,
  `auto-select-qotd`): flag + date on `quizzes`, upsert `qotd_log` (`selection_method = 'auto'`). A day
  that stays empty answers 500 so the cron shows as failed.
- `autoSchedule` uses a compact scheduler with the switch on (consecutive days, never a gap).
- Switch OFF: the cron, the publish route and `autoSchedule` run exactly today's code path and return
  today's responses.
- Unit tests (21): the 18 real open rows, gap-free compact schedule, pick order, 90-day no-repeat,
  group diversity, and a 30-day simulation with an in-memory store (18 bank days then catalog days, one
  pick per day, nothing repeated).
- No pending migration: no DDL and no backfill. The job writes through the same RPC and tables as
  today's cron and the admin routes, and only after the owner turns the switch on.

### 2.4 Owner go (to resume the rotation)

Set `QOTD_ROTATION_FIX=1` (server env, Production) and redeploy. The next 00:05 UTC run publishes the
2026-04-03 bank row, then one bank row per day for 18 days, then catalog picks. A manual run:
`GET /api/qotd/publish` as an admin, or the cron URL with `Bearer CRON_SECRET`.

### 2.5 What the v11 home shows while the rotation is stopped

The latest STORED pick (`qotd_log` / `quizzes` flag, on or before today): "Stray Kids: District 9
Quiz", with the note "Picked on June 30" (never a countdown). A pick dated today shows "New quiz in
6h 12m" only when tomorrow's is on its way (switch on, or a bank row dated tomorrow), else "Today's
pick". The legacy home keeps its replay pick (flag off unchanged); both agree again once the switch is
on.

## 3. Screenshots (before = flag off, after = flag on, prototype)

Full pages (production builds, `next build` + `next start`, real data):

| | |
|---|---|
| Guest 1440 light | ![](P1/before-after-prototype-home-guest-1440-light.webp) |
| Guest 1440 dark | ![](P1/before-after-prototype-home-guest-1440-dark.webp) |
| Guest 390 light | ![](P1/before-after-prototype-home-guest-390-light.webp) |
| Guest 390 dark | ![](P1/before-after-prototype-home-guest-390-dark.webp) |
| Signed in 1440 light | ![](P1/before-after-prototype-home-1440-light.webp) |
| Signed in 1440 dark | ![](P1/before-after-prototype-home-1440-dark.webp) |
| Signed in 390 light | ![](P1/before-after-prototype-home-390-light.webp) |
| Signed in 390 dark | ![](P1/before-after-prototype-home-390-dark.webp) |

Element pairs (prototype | implementation): header, ticker, quiz of the day, continue, groups,
trending, best | new, band, community in `P1/*-home-guest-1440-light.webp`,
`P1/*-home-1440-dark.webp`, `P1/*-home-guest-390-light.webp`.

Text and photos differ by design (real data). The signed-in panels use the parity test user and three
Continue runs seeded in the browser's localStorage (device data, nothing written to the database).

## 4. Wiring proof

| Row (WIRING-MAP) | Source | Proof |
|---|---|---|
| Home: Quiz of the day card (v11.1 minimal) | `qotd_log` + `quizzes` flag (latest on or before today), `quiz_time_stats` | p1.spec "quiz of the day": title = the stored pick read from the DB, meta has the real question count and average (`total_score_sum / total_completions / question_count`), note never claims today for an old pick, Play = `/q/{slug}?daily=quiz` (click + Enter) |
| Home: Header (v11.2) | guest server HTML; `/api/auth/me` (display_name, daily_streak, last_daily_date) | spec guest: H1 / H2 / CTA text and hrefs; spec signed in: "Good <time>, <name>", one streak line, no CTAs, zero writes |
| Home: Live ticker | `components/home/activity-ticker.tsx`, `/api/activity/recent`, `/api/stats/live` | spec "live ticker": the line shown is one of the endpoint's lines (or the online count), bar 44/40px, LIVE 11/800 |
| Home: Continue playing | `kq_ux_continue_v1` (device, written by the quiz player, P4) | spec: guest hidden; signed in shows the saved run, "3 of 8 answered", `/q/{slug}` |
| Groups rail (NEW badge) | published quizzes per group (paginated), groups (90 visible) | spec: counts equal a head count of published quizzes, "All 90 groups" = rows minus quarantine, hub hrefs |
| Trending this week | real plays of the last 7 days (`getPopularQuizzes('week')`), browse trending as fallback | spec: at most 4, never the same photo twice |
| All time best | `play_count` desc | spec: numbered 1..5, top 3 pink-ink, plays descending |
| New quizzes | `created_at` desc, published | spec: type glyph, age label, `/quizzes?sort=newest` |
| Daily band played state | `daily_blindtest_scores` (count today; the fan's score, rank, best via `/api/ux-v1/p1/daily-band`), the daily game's `kq_daily_blindtest_played` flag | spec: count equals a head count for today, CTAs, played state; signed-in response shape |
| From the community | `daily_debates` + `debate_questions` + `debate_votes`, `verse_threads` + comments, `verse_essays` (featured), opt-in spaces only | spec: at most 3 rows, typed sub line, real hrefs |
| Blindtest panel links | `/blindtest?daily=true`, `/blindtest`, `/blindtest/leaderboard` | spec: hrefs + click |

No write: every spec wraps the page in `guardWrites` and asserts zero mutating requests (guest and
signed in). Reads of the tables above use the anon key (GET).

## 5. Checks

- e2e `p1.spec.ts` (flag-on production build :4003 and dev :3031), 1440 + 390, light + dark: see
  `P1/proofs/e2e.txt`.
- Landmarks: tops of ticker, header, CTAs, QOTD and the first section within 2px of the guest reference
  at 1440 and 390; computed styles equal to `styles.json` for `.qotd`, `.qlab`, `.qline`,
  `.btn-primary`, `.hhead h1`, `.sec-h h2`, `.qcard`, `.nav` (0 mismatches).
- axe (axe-core 4.11 via eslint-config-next): 0 serious / critical on `.p1-home`, guest and signed in.
- Keyboard: Tab reaches every link of the home in DOM order, 2px pink ring; Enter operates Play.
- Flag off (`P1/proofs/flag-off.txt`): `/` and `/pt` structure IDENTICAL to the base branch
  (DOM, head, meta, canonical, JSON-LD, text, links, stylesheets); JS weight section 5.1.
- SEO flag on vs flag off (`P1/proofs/seo-diff.txt`): title, meta, canonical, hreflang, H1, intro,
  JSON-LD identical on `/` and `/pt`; server-rendered. Internal links change with the redesign (legacy
  rails replaced; every hub stays reachable from `/groups`, linked from the home, and the footer).
- Static/ISR: `/` stays `○` (revalidate 10m) in the flag-on build; no cookie read on the server.
- tsc 0 errors; eslint-config-next core-web-vitals + typescript on P1 files: 0 problems; vitest 21/21;
  em / en dash check on P1 files: none.
- Data guard (read only): `P1/proofs/data-guard-before.txt` and `-after.txt`.

### 5.1 Flag-off JS weight

(filled from `P1/proofs/flag-off.txt`)

## 6. Open items and owner decisions

1. QOTD switch: set `QOTD_ROTATION_FIX=1` to resume the rotation (2.4).
2. Security (existing, found while reading the migrations, not tested): `quiz_bank` has
   `CREATE POLICY quizbank_admin_all ... FOR ALL USING (true) WITH CHECK (true)` (migration 018) and
   `quiz_time_stats` has a write-all policy (032). With the anon key anyone could add a bank row dated
   tomorrow, which today's cron would publish as the quiz of the day under the system account. An RLS
   change is outside this run's rules: owner call.
3. The ticker's random 12 to 27 floor on "fans playing now" is kept (owner decision pending, 17.11).
4. Streak copy "Any quiz or blindtest today keeps your N-day streak" (prototype) is true once P4 / P6
   count any run toward the streak; today only the daily quiz / daily blindtest do.
5. P4 contract: the quiz player writes unfinished runs with `writeRun` / `clearRun` from
   `lib/ux-v1/p1/continue-store.ts` (key `kq_ux_continue_v1`); the home only reads.
6. P2: the home links `/quizzes?sort=most_played` and `/quizzes?sort=newest` (today's sort keys).
7. While the rotation is stopped the legacy home (replay pick) and the v11 home (stored pick) show
   different quizzes; both agree once the switch is on.

## 7. Requests to A0

`v11/requests/P1.md`: focus the page H1 on client navigation (16.5); a dev-only chunk 404 note.
