# P1 Home report (UX v11.2)

## Progress

- Done: QOTD root cause found (read-only queries + code) and fixed in code behind the server env switch
  `QOTD_ROTATION_FIX` (default OFF, production unchanged), 21 unit tests; flag-on home built on A0's
  foundation (ticker, centred header, quiz of the day row, continue, groups rail, trending, all time best
  and new, blindtest band, community rows), real cached fail-soft reads, `p1.css` (not imported); first
  measurements at 1440 match the prototype boxes (header, QOTD, sections, rail).
- Next: trending photo de-duplication (16.8), visual pass at 390 and dark, `e2e/ux-v1/p1.spec.ts`
  (guardWrites, landmarks vs styles.json, axe, keyboard), flag-off diff and SEO diff of `/` and `/pt`,
  screenshots, report, PR into feat/ux-v1-v11.
- Blockers: none. Dev note: in this worktree Turbopack's inferred root is the main checkout; after a stale
  `.next` the page referenced a missing chunk (hydration failed); `rm -rf apps/quiz/.next` fixed it.
