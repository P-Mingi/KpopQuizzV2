# P10 Passport + settings report (UX v11.2)

## Progress

- Done (branch `ux11/p10-passport`, rebased on `feat/ux-v1-v11` dc863a8):
  - Header routes `POST /api/profile/header/upload` (multipart) and `POST /api/profile/header/link` (JSON):
    404 flag off, 401 signed out, 503 before any write while the storage bucket is missing, SSRF-safe link
    fetch (`lib/ux-v1/p10/ssrf.ts`), file checks + sharp re-encode to a 1500 x 300 WebP
    (`lib/ux-v1/p10/header-image.ts`). 129 unit tests green (`vitest run src/lib/ux-v1/p10`).
  - v11 passport for `/me` (personal) and `/u/[username]` (public, cookie-free ISR), flag on only
    (`components/profile/ux-v1/passport.tsx` + islands). `/me` keeps every call and write of today in the same
    order; flag on appends read-only extras.
  - v11 settings (`components/profile/ux-v1/settings.tsx`, lazy chunk, flag on only), save bar posting only
    changed fields to the existing `/api/auth/update-profile` and `/api/notifications/prefs`.
  - Read-only "Download your data" route `GET /api/ux-v1/p10/export`.
  - Stylesheet `styles/ux-v1/p10.css` (not imported). tsc clean.
- Next: pending migration notes (bucket + policies, email prefs), render test of the signed-in `/me` props,
  `e2e/ux-v1/p10.spec.ts` (guardWrites, landmarks vs styles.json, a11y, keyboard, sheets), pixel pass vs the
  references at 1440 / 390 light / dark, flag-off diff + SEO diff on `/u/testtest`, report sections, PR.
- Blockers: none. Owner decisions pending (see the end of this report once written): the header bucket, the
  production-write question (signed-in `/me` is NOT verified until then).
