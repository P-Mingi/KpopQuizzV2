# R155 · Publish

- Section: 6. Create (/create, create-funnel.tsx, 4 steps -> 3 visible + done) (WIRING-MAP.md line 155)
- Owner: P5
- Build: shared flag-on production build on :3021 (feat/ux-v1-v11 dcc3159)
- Verdict: **PASS (payload); DB effect NOT VERIFIED (owner decision 1)**

Map row (verbatim):

| Publish | `POST /api/quiz/create` (`MIN_QUESTIONS = 3`), guest -> claim username (`/api/auth/check-username`, `/api/auth/create-profile`) | EXISTS | Same |

## Control
- Publish. Test user: posts. Guest: 'Sign in to publish ... Your draft is kept.' sheet, no request.

## Call (endpoint, method, status / payload)
- Test user: STUBBED POST /api/quiz/create {group_id:1, title, quiz_type:'multiple_choice', difficulty:'medium', language:'en', questions:[3 complete], settings:{timer:true, timer_seconds:15, shuffle:false, show_answers:true, creator_note}} (the legacy body keys; cover_image_url only with a cover). Guest: 0 writes.

## Database (read-only, service role `select` only)
- -

## Flag off
- Flag off: the legacy funnel posts the same body (P5 unit parity, 17 scenarios).

## Notes
-

## Evidence
- `proofs/create-user-1440.json`
- `proofs/create-guest-1440.json`
- `proofs/unit-p5.txt`
