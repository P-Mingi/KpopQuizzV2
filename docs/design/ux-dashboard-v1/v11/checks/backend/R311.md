# R311 · Auth gates (guest vs signed-in)

- Section: 15. Cross-cutting (WIRING-MAP.md line 311)
- Owner: A0 (sheet) + every page
- Build: shared flag-on production build on :3021 (feat/ux-v1-v11 dcc3159)
- Verdict: **PASS (gates + continue the action); a real OAuth / magic-link round trip NOT VERIFIED (no other sign-in allowed)**

Map row (verbatim):

| Auth gates (guest vs signed-in) | `lib/use-signed-in.ts`, `/login`, `/onboarding`, anon runs claimable | EXISTS |

## Control
- Auth gates: every guest write control opens the A0 sign-in sheet instead of writing (follow, save, comment, vote, cheer, heart, publish, notify).

## Call (endpoint, method, status / payload)
- Sign-in sheet from 'Follow mapple' (guest, /q/skz-true-or-false-only-real-stays-pass): Google -> Supabase /auth/v1/authorize?provider=google&redirect_to=<site>/auth/callback?returnTo=%2Fq%2Fskz-true-or-false-only-real-stays-pass (navigation aborted by C2); Email -> STUBBED POST supabase /auth/v1/otp?redirect_to=<same callback> {email, create_user, PKCE challenge} (no email sent). Pending action stored: {id:'p4-follow', path:'/q/skz...', payload:{username:'mapple'}}. Back on the same path as the test user with that pending action: the page itself sends STUBBED POST /api/follow {"username":"mapple","action":"follow"} and clears the pending action.

## Database (read-only, service role `select` only)
- follows: the test user does not follow mapple (so the resume follows).

## Flag off
- Flag off: /login flow, same /auth/callback.

## Notes
- /auth/callback redirects to returnTo without checking it is a same-site path (new URL(returnTo, request.url): '//host' or 'https://host' leave the site after sign-in). Existing live code (unchanged since 2026-08-31, not a v11 file): reported to ORCH as a live-site note, not filed to a v11 owner.

## Evidence
- `proofs/signin-return-guest.json`
- `proofs/signin-return-user.json`
