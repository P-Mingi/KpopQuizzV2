# R199 · Make a group quiz

- Section: 8. Group hub (/{slug}-quiz, group-quiz-page.tsx) (WIRING-MAP.md line 199)
- Owner: P3 (link) / P5 (prefill)
- Build: shared flag-on production build on :3021 (feat/ux-v1-v11 dcc3159)
- Verdict: **PASS**

Map row (verbatim):

| Make a group quiz | `/create?group={slug}` | EXISTS | prefill group in create-funnel (NEW param) |

## Control
- 'Make your own BLACKPINK quiz -> /create?group=blackpink'; empty hub 'Make the first quiz -> /create?group=chungha'.

## Call (endpoint, method, status / payload)
- /create?group=blackpink = 200; create/page.tsx passes initialGroupSlug to the P5 funnel (prefill).

## Database (read-only, service role `select` only)
- -

## Flag off
- Flag off: the legacy funnel reads the same param.

## Notes
-

## Evidence
- `proofs/hub-guest-1440.json`
- `proofs/community-urls-3021.txt`
