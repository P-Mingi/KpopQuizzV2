# R085 · Stats card (6)

- Section: 3. Quiz page (/q/[slug]) (WIRING-MAP.md line 85)
- Owner: P4
- Build: shared flag-on production build on :3021 (feat/ux-v1-v11 dcc3159)
- Verdict: **PASS**

Map row (verbatim):

| Stats card (6) | `GET /api/quiz/[id]/stats` + `GET /api/quiz/[id]/time-stats` (`quiz-stats-block.tsx`) | EXISTS | Same |

## Control
- Stats card: the numbers are kept in the meta line, the About counts line and the hall of fame header (v11 layout).

## Call (endpoint, method, status / payload)
- Server-rendered.

## Database (read-only, service role `select` only)
- See R077 / R081 (all equal to the DB).

## Flag off
- Flag off: live stats block.

## Notes
-

## Evidence
- `proofs/quiz-guest.json`
