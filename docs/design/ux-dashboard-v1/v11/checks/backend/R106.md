# R106 · Per-question times

- Section: 4. Quiz in-game (quiz-player.tsx, playing phase) (WIRING-MAP.md line 106)
- Owner: P4
- Build: shared flag-on production build on :3021 (feat/ux-v1-v11 dcc3159)
- Verdict: **PASS (payload); DB effect NOT VERIFIED (owner decision 1)**

Map row (verbatim):

| Per-question times | `plays.per_question_times` via `POST /api/quiz/[id]/play` (`record_play` RPC) | EXISTS | Same |

## Control
- Per-question times in the save.

## Call (endpoint, method, status / payload)
- STUBBED POST /api/quiz/4ba8f255-.../play {"score":8,"total_questions":8,"time_taken_seconds":1,"max_score":8,"per_question_times":[0.2,0.1,...],"anon_id":"<uuid>"} (guest); same shape signed in.

## Database (read-only, service role `select` only)
- n/a (record_play would insert a plays row).

## Flag off
- Flag off: legacy payload (same keys, P4 unit parity).

## Notes
-

## Evidence
- `proofs/quiz-guest.json`
- `proofs/quiz-user.json`
- `proofs/unit-p4.txt`
