# R214 · Preferences: sound, email on replies

- Section: 9. Passport (/me, /u/[username]) and Settings (/settings) (WIRING-MAP.md line 214)
- Owner: P10
- Build: shared flag-on production build on :3021 (feat/ux-v1-v11 dcc3159)
- Verdict: **PASS (sound, category prefs); email switches NOT VERIFIED until v11-p10-email-prefs.sql**

Map row (verbatim):

| Preferences: sound, email on replies | sound = `lib/sounds.ts` mute; email = NEW (`notification_prefs` has categories, no email channel) | PARTIAL | add `email_weekly_recap` to `notification_prefs` |

## Control
- Preferences: category switches; 'Streak reminder by email' and 'Weekly recap by email' shown disabled.

## Call (endpoint, method, status / payload)
- GET /api/notifications/prefs 200; toggling Social + Save -> STUBBED POST /api/notifications/prefs {"categories":{"social":false}} (changed key only).

## Database (read-only, service role `select` only)
- notification_prefs has no email columns (pending migration).

## Flag off
- -

## Notes
-

## Evidence
- `proofs/settings-user.json (switches)`
