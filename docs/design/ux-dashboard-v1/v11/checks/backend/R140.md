# R140 · Group search with chip

- Section: 6. Create (/create, create-funnel.tsx, 4 steps -> 3 visible + done) (WIRING-MAP.md line 140)
- Owner: P5
- Build: shared flag-on production build on :3021 (feat/ux-v1-v11 dcc3159)
- Verdict: **PASS**

Map row (verbatim):

| Group search with chip | `components/create/group-picker.tsx` (91 groups, custom group request -> `pending groups` admin) | EXISTS | Same |

## Control
- Group search with chip: 'bts' + Enter -> BTS; hint 'Your quiz appears on the BTS page and counts for ARMY in the fandom war.'

## Call (endpoint, method, status / payload)
- Groups list server side (getAllGroups); payload group_id 1.

## Database (read-only, service role `select` only)
- groups bts: id 1, fandom_name ARMY (= the hint and the payload).

## Flag off
- -

## Notes
-

## Evidence
- `proofs/create-user-1440.json (groupHint)`
