# R370 · Community · Comment like heart

- Section: v11 additions (2026-09-25) (WIRING-MAP.md line 370)
- Owner: P8
- Build: shared flag-on production build on :3021 (feat/ux-v1-v11 dcc3159)
- Verdict: **NOT VERIFIED until v11-p8-community.sql (fail-soft + once-per-user structure proven)**

Map row (verbatim):

| Community | Comment like heart | Toggle like with count | EXISTS likes / quiz_comments; verify endpoint |

## Control
- Comment / thread / debate like hearts: hidden until community_likes exists (no heart on thread and reply rows today).

## Call (endpoint, method, status / payload)
- POST /api/ux-v1/p8/like {target_type:'thread',target_id:'1'} and {target_type:'comment',target_id:'3'}: 503 not_live (guest and test user), before auth and before any write.

## Database (read-only, service role `select` only)
- community_likes does not exist (read-only select). Once per user: the pending table's PRIMARY KEY (target_type, target_id, user_id) (docs/pending-migrations/v11-p8-community.sql:49) and the route toggles: existing row -> delete, else upsert ignoreDuplicates (like/route.ts:83-89), so a fan counts at most once.

## Flag off
- 404 flag off.

## Notes
-

## Evidence
- `proofs/p8-writes-failsoft.txt`
- `proofs/sql-community-verse.json`
