# R090 · Owner actions (edit)

- Section: 3. Quiz page (/q/[slug]) (WIRING-MAP.md line 90)
- Owner: P4
- Build: shared flag-on production build on :3021 (feat/ux-v1-v11 dcc3159)
- Verdict: **NOT VERIFIED (the test user owns no quiz)**

Map row (verbatim):

| Owner actions (edit) | `quiz-owner-actions.tsx`, `/quiz/[id]/edit`, `POST /api/quiz/[id]/edit` | EXISTS | Keep, shown only to the creator |

Corrected by WIRING-MAP.verified.md: (a)4: the save is PUT /api/quiz/[id], not POST .../edit.

## Control
- Owner actions: rendered only for the creator (QuizOwnerActions kept).

## Call (endpoint, method, status / payload)
- -

## Database (read-only, service role `select` only)
- profiles.total_quizzes_created = 0 for the test user: no quiz to open as its owner.

## Flag off
- -

## Notes
-

## Evidence
- `proofs/get-shell-user.json`
