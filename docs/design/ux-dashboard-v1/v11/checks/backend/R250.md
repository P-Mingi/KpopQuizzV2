# R250 · Setup: playlist All / By group (multi) / Girl / Boy / Generation; rounds 5-10-15

- Section: 12. Blindtest hub (/blindtest) (WIRING-MAP.md line 250)
- Owner: P6
- Build: shared flag-on production build on :3021 (feat/ux-v1-v11 dcc3159)
- Verdict: **PASS (payload); the run itself: p6.spec on :3021**

Map row (verbatim):

| Setup: playlist All / By group (multi) / Girl / Boy / Generation; rounds 5-10-15 | `blindtest-game.tsx` setup phase, `POST /api/blind-test/generate` (playlist, groups[], count, mode, difficulty), `lib/blind-test-playlists.ts`, `lib/blind-test-modes.ts` | EXISTS | Same |

## Control
- Setup: playlist (BTS picked in the menu), rounds 5 / 10 / 15, Start (guest and test user).

## Call (endpoint, method, status / payload)
- STUBBED POST /api/blind-test/generate {"playlist":"bts","count":5,"mode":"challenge"} (the live game body). generate is read-only by code (selects songs + refreshes Deezer previews) but it is a POST, so C2 never let it through.

## Database (read-only, service role `select` only)
- Pool = songs (4,120 active), not blind_test_songs; BTS has 18 clean active songs.

## Flag off
- Flag off: the legacy blindtest-game posts the same body.

## Notes
- p6.spec 'playlist menu ... Start sends the live body' and 'results ...' passed on the shared build (light + dark).

## Evidence
- `proofs/blindtest-guest-1440.json`
- `proofs/blindtest-user-1440.json`
- `proofs/sql-playlists.txt`
- `proofs/e2e-p6-subset-1440.txt`
