# R297 · Season score

- Section: 14. Ranked (NEW product surface, /blindtest/ranked) (WIRING-MAP.md line 297)
- Owner: P7
- Build: shared flag-on production build on :3021 (feat/ux-v1-v11 dcc3159)
- Verdict: **NOT VERIFIED live; unit-proven**

Map row (verbatim):

| Season score | SQL: sum of the 5 highest `points` per player per season (`ORDER BY points DESC LIMIT 5`), materialised nightly into a small `ranked_season_scores` view or computed in `GET /api/blind-test/ranked/me` |

## Control
- Season score = sum of the 5 best runs.

## Call (endpoint, method, status / payload)
- GET /api/ranked/me -> 503 not_live.

## Database (read-only, service role `select` only)
- -

## Flag off
- 404.

## Notes
- season.test.ts (17) + ladder.test.ts (6) in proofs/unit-p7-ranked.txt.

## Evidence
- `proofs/p7-ranked-failsoft.txt`
- `proofs/unit-p7-ranked.txt`
