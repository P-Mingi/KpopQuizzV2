# R032 · Night mode toggle

- Section: 0. Shell (every page) (WIRING-MAP.md line 32)
- Owner: A0
- Build: shared flag-on production build on :3021 (feat/ux-v1-v11 dcc3159)
- Verdict: **PASS**

Map row (verbatim):

| Night mode toggle | none | NEW | `data-theme` on `<html>`, tokens in `lib/design-tokens.ts` + globals; persist in localStorage + `profiles` pref later; default = system |

## Control
- Footer theme switch 'Dark mode' (and the same item in the avatar menu).

## Call (endpoint, method, status / payload)
- No API call (client only).

## Database (read-only, service role `select` only)
- n/a: persisted in localStorage 'theme' (the site's existing class-based system); the map's 'profiles pref later' is not built (not required).

## Flag off
- Flag off: the existing theme toggle.

## Notes
- Clicked as guest: html class light -> dark, localStorage theme light -> dark.

## Evidence
- `proofs/shell-guest-1440.json (theme.before / theme.after)`
