# R375 · Passport · Change header (link)

- Section: v11 additions (2026-09-25) (WIRING-MAP.md line 375)
- Owner: P10
- Build: shared flag-on production build on :3021 (feat/ux-v1-v11 dcc3159)
- Verdict: **NOT VERIFIED until v11-p10-header-storage.sql (SSRF block + fail-soft proven)**

Map row (verbatim):

| Passport | Change header (link) | Server fetch, validate, copy to storage | NEW route (no hot-linking) |

## Control
- Change header (link): server fetch, no hot-linking.

## Call (endpoint, method, status / payload)
- POST /api/profile/header/link: http://169.254.169.254/... -> 400 not_https; https://10.0.0.1, https://127.0.0.1, https://[::1] -> 400 private_address (static checks before auth, storage or network); a public https link as guest -> 401; as the test user -> 503 bucket_missing (preflight before any fetch).

## Database (read-only, service role `select` only)
- No row changed.

## Flag off
- 404 flag off.

## Notes
- ssrf.test.ts: special IPv4 / IPv6 ranges, refused link shapes, pinned connection, DNS mixing public and private, redirects re-checked, size and type caps, timeout (proofs/unit-p10.txt).

## Evidence
- `proofs/p10-header-failsoft.txt`
- `proofs/unit-p10.txt`
