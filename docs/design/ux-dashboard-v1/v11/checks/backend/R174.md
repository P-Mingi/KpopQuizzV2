# R174 · Happening now

- Section: 7. Community (/community) - the social layer (posts do not exist yet) (WIRING-MAP.md line 174)
- Owner: P8
- Build: shared flag-on production build on :3021 (feat/ux-v1-v11 dcc3159)
- Verdict: **PASS**

Map row (verbatim):

| Happening now | `happening-now.tsx`, `GET /api/activity/recent` (`activity_events`) | EXISTS | Same |

## Control
- Happening now (rail): 5 real events with links (/u/<name>, /<group>-quiz).

## Call (endpoint, method, status / payload)
- Server-rendered from activity_events (same mapping + spree rule as happening-now.tsx; unit tested).

## Database (read-only, service role `select` only)
- The events shown are the latest activity_events (quiz_completed / perfect_score / quiz_published, ids 4324x); cached 5 min.

## Flag off
- Flag off: legacy HappeningNow on /leaderboard.

## Notes
-

## Evidence
- `proofs/community-guest-1440.json (rail, railLinks)`
- `proofs/sql-community-rail.json (latest events)`
- `proofs/unit-p8.txt`
