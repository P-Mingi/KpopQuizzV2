# R076 · Follow

- Section: 3. Quiz page (/q/[slug]) (WIRING-MAP.md line 76)
- Owner: P4
- Build: shared flag-on production build on :3021 (feat/ux-v1-v11 dcc3159)
- Verdict: **PASS (payload + read state); DB effect NOT VERIFIED (owner decision 1)**

Map row (verbatim):

| Follow | `POST /api/follow` (`follows` table) | EXISTS | Same endpoint |

## Control
- Follow on the author row. Guest: opens 'Follow mina' sign-in sheet (no write). Test user on /q/this-is-only-for-true-gllits: shows 'Following', click -> unfollow.

## Call (endpoint, method, status / payload)
- GET /api/follow?username=peachyblssom2014 200 (state); click -> STUBBED POST /api/follow {"username":"peachyblssom2014","action":"unfollow"} (the existing endpoint and payload).

## Database (read-only, service role `select` only)
- follows: the test user follows 80e5b2b1... = profiles peachyblssom2014 = creator of this quiz -> 'Following' is right.

## Flag off
- Flag off: legacy FollowButton, same endpoint.

## Notes
-

## Evidence
- `proofs/quiz-guest.json (followSheet)`
- `proofs/quiz-user.json`
- `proofs/sql-quiz-user.json`
