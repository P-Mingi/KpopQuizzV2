# R164 · New post composer + 4 modes

- Section: 7. Community (/community) - the social layer (posts do not exist yet) (WIRING-MAP.md line 164)
- Owner: P8
- Build: shared flag-on production build on :3021 (feat/ux-v1-v11 dcc3159)
- Verdict: **PASS (thread payload); fan debate / challenge posts NOT VERIFIED until v11-p8-community.sql (fail-soft proven)**

Map row (verbatim):

| New post composer + 4 modes | none | NEW | Tables from DESIGN-SPEC 12.5: `posts`, `post_replies`, `post_reactions`, `debate_options`, `debate_votes`. Endpoints: `POST /api/posts`, `GET /api/posts?tab=&group=`, `POST /api/posts/[id]/reply`, `POST /api/posts/[id]/react`, `POST /api/posts/[id]/vote`. RLS on all. NOTE: `verse_threads` / `verse_discussions` exist but are Verse; do not reuse. |

## Control
- Composer + editor with 4 modes (Thread, Blog, Debate, Challenge). P8 did not build the map's posts/post_replies tables: threads and blogs are the Verse tables, fan debates and challenges are the pending community_* tables.

## Call (endpoint, method, status / payload)
- Thread (test user, BTS): STUBBED POST /api/verse/threads {"group_id":1,"title":"C2 probe thread title, never saved","body":"..."}.
- Debate mode: Post disabled with 'Fan debates open soon. Vote on the daily debate meanwhile.'; Challenge mode: 'Challenge posts open soon. ...'.
- POST /api/ux-v1/p8/debates and /challenges with valid bodies (guest and test user) -> 503 not_live before any write; bad bodies -> 400.

## Database (read-only, service role `select` only)
- community_debates, community_challenges, community_debate_votes, community_likes, community_replies: 'Could not find the table' (read-only select), so the 503 fires before any write (tableLive probe, then validation order: bad JSON 400, bad target 400, then 503).

## Flag off
- POST /api/ux-v1/p8/* = 404 flag off; /community 301 -> /.

## Notes
-

## Evidence
- `proofs/community-user-1440.json`
- `proofs/p8-writes-failsoft.txt`
- `proofs/sql-community-verse.json`
- `proofs/flag-off-endpoints-dev-4202.txt`
