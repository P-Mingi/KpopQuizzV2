# R031 · Streak pill "12 days"

- Section: 0. Shell (every page) (WIRING-MAP.md line 31)
- Owner: A0
- Build: shared flag-on production build on :3021 (feat/ux-v1-v11 dcc3159)
- Verdict: **PASS (streak 0 case); NOT VERIFIED live for a streak > 0 (needs a daily play = a production write, owner decision 1)**

Map row (verbatim):

| Streak pill "12 days" | `daily-streak.ts`, `GET /api/daily/streak`, guest streak `lib/guest-streak.ts` | EXISTS | Same value |

## Control
- Streak pill in the top nav (signed in only).

## Call (endpoint, method, status / payload)
- Value from GET /api/auth/me profile.daily_streak / last_daily_date (no extra call).

## Database (read-only, service role `select` only)
- Test user daily_streak = 0, last_daily_date = null, so streakView() returns null and the pill is correctly absent (shell-user-1440.json: no .ux-streak). No invented days.

## Flag off
- Flag off: legacy streak UI unchanged.

## Notes
- streakView unit tests (at risk / played today / nothing to show) pass: proofs/unit-a0.txt.
- Streak RULE: see R332.

## Evidence
- `proofs/shell-user-1440.json`
- `proofs/get-shell-user.json`
- `proofs/unit-a0.txt`
