# R100 · Timer ring 15s warn/danger

- Section: 4. Quiz in-game (quiz-player.tsx, playing phase) (WIRING-MAP.md line 100)
- Owner: P4
- Build: shared flag-on production build on :3021 (feat/ux-v1-v11 dcc3159)
- Verdict: **PASS**

Map row (verbatim):

| Timer ring 15s warn/danger | `timer_seconds`, ring in quiz-player (`timer-circle.tsx`) | EXISTS | Same, keep on screen after answer (frozen state = NEW CSS only) |

## Control
- Timer ring from settings.timer_seconds; warn / danger / 'Time is up'.

## Call (endpoint, method, status / payload)
- No request; value from the page data.

## Database (read-only, service role `select` only)
- settings.timer_seconds = 15 on the classic quiz -> '15 seconds per question'.

## Flag off
- Flag off: legacy inline ring.

## Notes
- p4.spec 'timer: warn, danger, then Time is up' passed on :3021 (proofs/e2e-p4-subset-1440.txt).

## Evidence
- `proofs/quiz-guest.json (timerline)`
- `proofs/sql-quiz-classic.json`
- `proofs/e2e-p4-subset-1440.txt`
