# R253 · Blindtest of the day + board

- Section: 12. Blindtest hub (/blindtest) (WIRING-MAP.md line 253)
- Owner: P6
- Build: shared flag-on production build on :3021 (feat/ux-v1-v11 dcc3159)
- Verdict: **PASS (reads real; submit payload via p6.spec); DB effect NOT VERIFIED**

Map row (verbatim):

| Blindtest of the day + board | `GET /api/daily/blindtest`, `POST /api/daily/blindtest/submit`, `GET /api/daily/blindtest/leaderboard` (RPCs `ensure_daily_blindtest`, `submit_daily_bt_score`, `get_daily_bt_leaderboard`) | EXISTS | Same |

## Control
- Blindtest of the day + today's board.

## Call (endpoint, method, status / payload)
- GET /api/daily/blindtest 200 -> date 2026-09-26, 10 questions (read only today: the day's row already exists, ensure_daily_blindtest returns it). GET /api/ux-v1/p6/board 200 -> top jellytaengs 7/10 64.7 s, ajmyoi 6/10 58.5 s, lore_stay_5 5/10 48.3 s, total 3. Submit: POST /api/daily/blindtest/submit {score, time_ms} then completeDaily('blindtest') (p6.spec 'daily: one try, the live saves in order').

## Database (read-only, service role `select` only)
- daily_blindtests 2026-09-26 song_ids = the 10 song ids of the GET in the same order; daily_blindtest_scores 2026-09-26 = (7, 64676), (6, 58517), (5, 48251) = the board.

## Flag off
- Flag off: legacy daily flow, same endpoints; /api/ux-v1/p6/board = 404.

## Notes
-

## Evidence
- `proofs/get-p6-daily-board.txt`
- `proofs/blindtest-guest-1440.json`
- `proofs/e2e-p6-subset-1440.txt`
