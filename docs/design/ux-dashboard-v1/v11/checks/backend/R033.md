# R033 · Bell + unread dot

- Section: 0. Shell (every page) (WIRING-MAP.md line 33)
- Owner: A0 (bell) / P11 (panel)
- Build: shared flag-on production build on :3021 (feat/ux-v1-v11 dcc3159)
- Verdict: **PASS**

Map row (verbatim):

| Bell + unread dot | `/notifications` + `GET /api/notifications` (unreadCount) | EXISTS | Link + count |

## Control
- Bell (signed in): aria-label 'Notifications' (no unread dot), click opens the bell panel (P11) with 'See all notifications -> /notifications'.

## Call (endpoint, method, status / payload)
- GET /api/notifications?limit=1 200 on load ({unreadCount:0}); the bell panel GET /api/notifications?limit=6 200.

## Database (read-only, service role `select` only)
- creator_notifications where user_id = test user: 0 rows, 0 unread (head counts, proofs/sql-shell.json) = unreadCount 0 = no dot.

## Flag off
- Flag off: legacy notification-bell, no v11 call.

## Notes
- Unread > 0 display is NOT verified live: the test user has no notification (creating one is a write).

## Evidence
- `proofs/shell-user-1440.json`
- `proofs/get-shell-user.json`
- `proofs/sql-shell.json`
