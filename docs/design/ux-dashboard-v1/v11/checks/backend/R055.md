# R055 · Quiz card (any)

- Section: 1. Dashboard (/) (WIRING-MAP.md line 55)
- Owner: A0 (card) / P1
- Build: shared flag-on production build on :3021 (feat/ux-v1-v11 dcc3159)
- Verdict: **PASS**

Map row (verbatim):

| Quiz card (any) | `components/quiz/quiz-card.tsx` -> `/q/{slug}` | EXISTS | Re-skin `quiz-card.tsx`; cover = `quizzes.cover_image_url` else group photo `public/idols/{Group}.jpg` (`GroupLogo` fallback logic in `components/group`) |

## Control
- Quiz cards link /q/{slug}; photo = cover or group photo.

## Call (endpoint, method, status / payload)
- Links only.

## Database (read-only, service role `select` only)
- Slugs are real published quizzes (the lists above).

## Flag off
- Flag off: live cards.

## Notes
-

## Evidence
- `proofs/home-guest-1440.json`
