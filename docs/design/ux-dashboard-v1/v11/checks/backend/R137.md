# R137 · Title (5+) + counter

- Section: 6. Create (/create, create-funnel.tsx, 4 steps -> 3 visible + done) (WIRING-MAP.md line 137)
- Owner: P5
- Build: shared flag-on production build on :3021 (feat/ux-v1-v11 dcc3159)
- Verdict: **PASS**

Map row (verbatim):

| Title (5+) + counter | `MIN_TITLE`, `POST /api/quiz/title-check` | EXISTS | Same |

Corrected by WIRING-MAP.verified.md: (a)9: title-check is GET, not POST.

## Control
- Title (5+) + duplicate check.

## Call (endpoint, method, status / payload)
- GET /api/quiz/title-check?title=<typed> 200 while typing (debounced). Existing title 'Ultimate BTS era quiz - only real ARMYs survive' -> {exists:true}; the probe title -> {exists:false}.

## Database (read-only, service role `select` only)
- quizzes with title 'BTS era quiz': 0 (no hint shown, right).

## Flag off
- Flag off: the same GET from the legacy funnel.

## Notes
-

## Evidence
- `proofs/create-guest-1440.json`
- `proofs/get-title-check.json`
