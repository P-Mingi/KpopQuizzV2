# R048 · Keep playing stats (streak, best BT, rank)

- Section: 1. Dashboard (/) (WIRING-MAP.md line 48)
- Owner: P1
- Build: shared flag-on production build on :3021 (feat/ux-v1-v11 dcc3159)
- Verdict: **N/A (not in the v11 home)**

Map row (verbatim):

| Keep playing stats (streak, best BT, rank) | streak EXISTS; best BT = `daily_blindtest_scores` best for user (EXISTS in `/api/daily/blindtest/leaderboard` shape) ; rank = weekly players rank (`lib/weekly-leaderboard-padding.ts`, `/leaderboard`) | PARTIAL | One small `GET /api/passport/summary` returning the 3 numbers |

## Control
- Keep playing stats (streak, best BT, rank): the v11.2 home has no stats strip (prototype); the streak is in the header line and the pill, the best blindtest in the band (signed in: /api/ux-v1/p1/daily-band).

## Call (endpoint, method, status / payload)
- GET /api/ux-v1/p1/daily-band 200 (guest {signedIn:false,today:null,best:null}; test user {signedIn:true,today:null,best:null}).

## Database (read-only, service role `select` only)
- daily_blindtest_scores for the test user: none (the test user never played the daily blindtest) -> today/best null.

## Flag off
- Flag off: /api/ux-v1/p1/daily-band = 404.

## Notes
-

## Evidence
- `proofs/get-daily-band-user.json`
- `proofs/get-daily-band-guest.json`
- `proofs/flag-off-endpoints-dev-4202.txt`
