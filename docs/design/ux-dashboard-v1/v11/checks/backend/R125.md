# R125 · Comments (200 chars, score chip)

- Section: 5. Quiz results (quiz-player.tsx, result phase) (WIRING-MAP.md line 125)
- Owner: P4
- Build: shared flag-on production build on :3021 (feat/ux-v1-v11 dcc3159)
- Verdict: **PASS (payload + list); DB effect NOT VERIFIED**

Map row (verbatim):

| Comments (200 chars, score chip) | `quiz-comments.tsx`, `POST /api/quiz/[id]/comment` (`quiz_comments`) | EXISTS | Same |

## Control
- Comments: field (200 chars), score chip 'Your score 8/8 is shown', Send.

## Call (endpoint, method, status / payload)
- GET /api/quiz/<id>/comment?limit=20 200 (the 1 existing comment). Guest Send -> 'Sign in to reply' sheet, no write. Test user Send -> STUBBED POST /api/quiz/62e332ec-.../comment {"content":"Great quiz"} (the endpoint attaches the best score).

## Database (read-only, service role `select` only)
- quiz_comments on 4ba8f255: 1 row (= the list).

## Flag off
- Flag off: legacy quiz-comments.

## Notes
-

## Evidence
- `proofs/quiz-guest.json`
- `proofs/quiz-user.json`
- `proofs/sql-quiz-classic.json`
