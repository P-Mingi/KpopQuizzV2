# R171 · Daily debate (vote, results)

- Section: 7. Community (/community) - the social layer (posts do not exist yet) (WIRING-MAP.md line 171)
- Owner: P8
- Build: shared flag-on production build on :3021 (feat/ux-v1-v11 dcc3159)
- Verdict: **PASS (payload + state); DB effect NOT VERIFIED (owner decision 1)**

Map row (verbatim):

| Daily debate (vote, results) | `components/community/daily-debate.tsx`, `POST /api/debate/vote`, `GET /api/debate/me`, `daily_debates`, `debate_votes` | EXISTS | Same |

## Control
- Daily debate in the rail and on /community/debate/<today>: guest -> 'Sign in to vote' sheet; test user votes Agree -> results shown ('you voted Agree').

## Call (endpoint, method, status / payload)
- GET /api/debate/me 200 {voted:false} for the test user today; vote -> STUBBED POST /api/debate/vote {"side":"a"} (the legacy island's endpoint and payload).

## Database (read-only, service role `select` only)
- debate_votes: unique (date, user_id) (migration 108): one vote per fan per day; the test user has no vote for 2026-09-26 (voted:false is right); today's question 'Remixes are almost never better than the original', 0 votes today.

## Flag off
- Flag off: the legacy DailyDebate on /leaderboard (ensure_daily_debate on view).

## Notes
- Owner decision (RUN-STATE): with the flag on nothing calls ensure_daily_debate (a write on view), so the daily debate stops rotating unless a cron is added.

## Evidence
- `proofs/community-user-1440.json`
- `proofs/community-posts-user.json`
- `proofs/get-p8-user.json`
- `proofs/sql-home-community.json`
