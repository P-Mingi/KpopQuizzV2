# R213 · Settings form (all fields)

- Section: 9. Passport (/me, /u/[username]) and Settings (/settings) (WIRING-MAP.md line 213)
- Owner: P10
- Build: shared flag-on production build on :3021 (feat/ux-v1-v11 dcc3159)
- Verdict: **PASS (payload, changed fields only); DB effect NOT VERIFIED (owner decision 1)**

Map row (verbatim):

| Settings form (all fields) | `/settings`, `POST /api/auth/update-profile` | EXISTS | Same |

## Control
- Settings form (test user): no save bar at rest; Your look changes -> 'Unsaved changes' bar -> Save.

## Call (endpoint, method, status / payload)
- STUBBED POST /api/auth/update-profile {"bias":"C2BIAS","profile_theme":"teal","name_accent":"purple","name_font":"serif"} (the live endpoint, only the 4 changed fields). The page GET reads profiles, user_badges, groups, notification prefs (no write).

## Database (read-only, service role `select` only)
- profiles of the test user unchanged (header_url null, updated_at 2026-09-22) after the run.

## Flag off
- Flag off: the live /settings posts the same endpoint.

## Notes
-

## Evidence
- `proofs/settings-user.json`
