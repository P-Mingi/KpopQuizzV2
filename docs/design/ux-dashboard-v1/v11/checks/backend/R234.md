# R234 · List grouped by day, unread state

- Section: 11. Notifications (/notifications) (WIRING-MAP.md line 234)
- Owner: P11
- Build: shared flag-on production build on :3021 (feat/ux-v1-v11 dcc3159)
- Verdict: **PASS**

Map row (verbatim):

| List grouped by day, unread state | `notifications-center.tsx`, `GET /api/notifications?limit=&offset=` (`NotificationRow`) | EXISTS | Same, group by day client-side |

## Control
- /notifications signed in: list grouped by day, unread state.

## Call (endpoint, method, status / payload)
- GET /api/notifications?limit=50 200 (the live center's call). Real: 0 rows -> 'All caught up ... Nothing here yet ... Read notifications older than 60 days are cleared.' Sample rows (GET answered locally, UI only): 2 unread + 1 read rendered with unread marks.

## Database (read-only, service role `select` only)
- creator_notifications for the test user: 0 rows (head count), so the empty state is right. The page GET writes nothing (no mark-read on view: markAllRead only in the button handler).

## Flag off
- Flag off: the live notifications-center, same GET.

## Notes
-

## Evidence
- `proofs/notifications-user-real.json`
- `proofs/notifications-user-sample.json`
- `proofs/sql-shell.json`
