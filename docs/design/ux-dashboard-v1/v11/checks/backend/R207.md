# R207 · Header (theme, avatar, name font/colour, level title, meta)

- Section: 9. Passport (/me, /u/[username]) and Settings (/settings) (WIRING-MAP.md line 207)
- Owner: P10
- Build: shared flag-on production build on :3021 (feat/ux-v1-v11 dcc3159)
- Verdict: **PASS (public passports, guest); signed-in /me NOT VERIFIED (writes on view, owner decision 1/4)**

Map row (verbatim):

| Header (theme, avatar, name font/colour, level title, meta) | `lib/passport-themes.ts`, `lib/passport-flair.ts`, `profiles`, `lib/level-titles.ts` | EXISTS | Same |

## Control
- Passport header on /u/mingi and /u/testtest (guest): name with accent + font, bias chip, level title, meta.

## Call (endpoint, method, status / payload)
- Server-rendered (ISR); GET /api/follow?username=... and /api/auth/me on the client.

## Database (read-only, service role `select` only)
- mingi: name_accent pink (h1 class ux-acc-pink), name_font mono (inline font-family, passport.tsx:195), bias 'Felix', stan_since 2016 ('Stan since 2016'), follower_count 2 ('2 followers'), xp 1,377 -> 'Lv 7 · Stan'. testtest: xp 32 -> 'Lv 2 · New Fan', 0 followers, joined September 2026 (created 2026-09-21).

## Flag off
- Flag off: the live /u page (same ISR reads).

## Notes
-

## Evidence
- `proofs/passport-guest.json`
