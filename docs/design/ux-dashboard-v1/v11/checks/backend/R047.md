# R047 · Keep playing (resume rows, progress %)

- Section: 1. Dashboard (/) (WIRING-MAP.md line 47)
- Owner: P1 (home) / P4 (store)
- Build: shared flag-on production build on :3021 (feat/ux-v1-v11 dcc3159)
- Verdict: **PASS (see R334)**

Map row (verbatim):

| Keep playing (resume rows, progress %) | none: plays are recorded at the end only (`POST /api/quiz/[id]/play`); no in-progress state server-side. `lib/create-draft.ts` pattern exists for drafts | NEW | localStorage "in-progress" per quiz (questionIndex, answers) written by quiz-player; rows read client-side. Server sync later. |

## Control
- Continue playing: device runs (localStorage 'ux:continue:v1', written by the quiz game, lib/ux-v1/p4/continue.ts), signed in only; the map's NEW localStorage plan is what was built.

## Call (endpoint, method, status / payload)
- No server call (client store). Guest: hidden. Test user with no saved run: hidden (continueRows = []).

## Database (read-only, service role `select` only)
- n/a: no server-side in-progress state (verified map (c)9).

## Flag off
- Flag off: no Continue section.

## Notes
- End-to-end (quit a quiz mid-run -> home row -> resume) is in R334 / R338.

## Evidence
- `proofs/home-user-1440.json (continueRows)`
- `proofs/quiz-continue-user.json`
