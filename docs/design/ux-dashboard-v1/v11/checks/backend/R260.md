# R260 · How scoring works

- Section: 12. Blindtest hub (/blindtest) (WIRING-MAP.md line 260)
- Owner: P6
- Build: shared flag-on production build on :3021 (feat/ux-v1-v11 dcc3159)
- Verdict: **PASS (display only; server scoring is ranked only, P7)**

Map row (verbatim):

| How scoring works | today: 1 point per correct. Speed + combo = NEW | NEW | server-side scoring in `/api/blind-test/play` (needs `time_ms` per answer: `challenge_attempts.song_results` shape already has it) |

## Control
- How scoring works.

## Call (endpoint, method, status / payload)
- Points / combo shown client side (lib/ux-v1/p6/points.ts mirrors 17.6); the daily stores score + time only.

## Database (read-only, service role `select` only)
- daily_blindtest_scores has no points column (score, time_ms).

## Flag off
- -

## Notes
-

## Evidence
- `proofs/unit-p6.txt`
