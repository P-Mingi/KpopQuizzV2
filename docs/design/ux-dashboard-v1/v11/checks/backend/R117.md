# R117 · Discord line / Brag

- Section: 5. Quiz results (quiz-player.tsx, result phase) (WIRING-MAP.md line 117)
- Owner: P4
- Build: shared flag-on production build on :3021 (feat/ux-v1-v11 dcc3159)
- Verdict: **FAIL (C2-003)**

Map row (verbatim):

| Discord line / Brag | `DiscordResultsLine`, `BragButton` (`/api/discord/flex`) | EXISTS | Same |

## Control
- Discord line / Brag on the results.

## Call (endpoint, method, status / payload)
- The v11 results have no DiscordResultsLine (the invite link) and no BragButton (POST /api/discord/flex, the server-posted brag). The share sheet's 'Discord' button only copies a message (A0 ShareSheet).

## Database (read-only, service role `select` only)
- -

## Flag off
- Flag off: quiz-player.tsx:1132-1137 renders both.

## Notes
- Map: 'EXISTS, Same'. Filed to P4 (keep the Discord line + Brag on strong results, or record an owner decision to drop them).

## Evidence
- `proofs/quiz-guest.json (results)`
