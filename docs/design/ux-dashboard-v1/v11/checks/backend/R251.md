# R251 · Your stats (rank title, best, combo, streak)

- Section: 12. Blindtest hub (/blindtest) (WIRING-MAP.md line 251)
- Owner: P6
- Build: shared flag-on production build on :3021 (feat/ux-v1-v11 dcc3159)
- Verdict: **NOT VERIFIED (bt_players unwired: production writes need the owner, P6 owner item 5)**

Map row (verbatim):

| Your stats (rank title, best, combo, streak) | `bt_players` (rank_title, rank_level, best_score, best_combo, current_streak) + RPCs `update_player_rank`, `award_bt_xp` (mig 052/059) | DEAD | Tables and RPCs exist, nothing writes them: `POST /api/blind-test/play` writes `blind_test_plays` and calls `award_xp` (profile XP). Wire: on play submit, upsert `bt_players` + insert `bt_plays`, call `award_bt_xp` then `update_player_rank`. Rank titles: trainee, rookie, debut, idol, star, superstar, legend (XP 0/500/1500/3000/6000/12000/25000). |

## Control
- Your stats: best daily score (daily_blindtest_scores), rank title (bt_players), streak = the site-wide pill.

## Call (endpoint, method, status / payload)
- Board GET /api/ux-v1/p6/board: test user 'You not played today Play' (me null).

## Database (read-only, service role `select` only)
- bt_players: 0 rows (still DEAD, as the map says); the test user has no daily_blindtest_scores row.

## Flag off
- -

## Notes
-

## Evidence
- `proofs/blindtest-user-1440.json`
- `proofs/get-p6-daily-board.txt`
