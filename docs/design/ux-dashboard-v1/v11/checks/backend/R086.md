# R086 · Hall of fame top 5 + times

- Section: 3. Quiz page (/q/[slug]) (WIRING-MAP.md line 86)
- Owner: P4
- Build: shared flag-on production build on :3021 (feat/ux-v1-v11 dcc3159)
- Verdict: **PASS**

Map row (verbatim):

| Hall of fame top 5 + times | `quiz-hall-of-fame.tsx` (`plays` + `quiz_time_stats`) | EXISTS | Same |

## Control
- Hall of fame top 5.

## Call (endpoint, method, status / payload)
- Server-rendered (getP4HallOfFame: score desc, fastest first, best row per signed-in player; relaxed filter off while the column is missing).

## Database (read-only, service role `select` only)
- Classic quiz: plays top rows 8/8 in 17, 18, 19, 19, 19 s, all guests -> '1 someone 8/8 0:17' ... '5 someone 8/8 0:19'. Gllits quiz: 9/10 44 s (peachyblssom2014, bias 'Wonhee Illit' shown), 6/10 53 s, 5/10 12 s (testtest), 5/10 67 s, 4/10 64 s = the 5 rows shown in that order. plays.relaxed does not exist (select error), so no filter (fail soft).

## Flag off
- Flag off: legacy hall of fame.

## Notes
-

## Evidence
- `proofs/quiz-guest.json (hof)`
- `proofs/quiz-user.json (page.hof)`
- `proofs/sql-quiz-classic.json`
- `proofs/sql-quiz-user.json`
