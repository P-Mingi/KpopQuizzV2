# R374 · Passport · Change header (upload)

- Section: v11 additions (2026-09-25) (WIRING-MAP.md line 374)
- Owner: P10
- Build: shared flag-on production build on :3021 (feat/ux-v1-v11 dcc3159)
- Verdict: **NOT VERIFIED until v11-p10-header-storage.sql (fail-soft + file checks proven)**

Map row (verbatim):

| Passport | Change header (upload) | Upload to storage, crop 1500x300, save header_url | PARTIAL: header_url EXISTS; verify bucket + upload route |

## Control
- Change header (upload).

## Call (endpoint, method, status / payload)
- POST /api/profile/header/upload: guest 401; test user 503 bucket_missing (headerPreflight: user, then getBucket, before reading the body; nothing stored, header_url still null).

## Database (read-only, service role `select` only)
- profiles.header_url of the test user = null after the probes.

## Flag off
- 404 flag off.

## Notes
- Type / size / magic bytes / re-encode 1500x300 WebP / EXIF drop / decompression bomb: header-image.test.ts (in proofs/unit-p10.txt, all pass).

## Evidence
- `proofs/p10-header-failsoft.txt`
- `proofs/unit-p10.txt`
- `proofs/flag-off-endpoints-dev-4202.txt`
