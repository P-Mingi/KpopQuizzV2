# R029 · Sidebar user row (name, streak)

- Section: 0. Shell (every page) (WIRING-MAP.md line 29)
- Owner: A0
- Build: shared flag-on production build on :3021 (feat/ux-v1-v11 dcc3159)
- Verdict: **PASS**

Map row (verbatim):

| Sidebar user row (name, streak) | `/me`, streak from `lib/streak.ts` + `/api/daily/streak` | EXISTS | Link + streak value |

## Control
- v11 has no sidebar user row; the account is the avatar menu (signed in): Passport -> /profile, My quizzes -> /u/testtest, Settings -> /settings, Dark mode, Sign out. Guest: 'Sign in' button (opens the sign-in sheet).

## Call (endpoint, method, status / payload)
- GET /api/auth/me 200 once per page (shared useUxMe). Test user: profile.username 'testtest', daily_streak 0.

## Database (read-only, service role `select` only)
- profiles row of the test user: username testtest, daily_streak 0, last_daily_date null (read-only select).

## Flag off
- Flag off: legacy nav account menu; no v11 call.

## Notes
-

## Evidence
- `proofs/shell-user-1440.json (avatarLinks)`
- `proofs/get-shell-user.json`
