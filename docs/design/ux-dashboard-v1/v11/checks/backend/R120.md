# R120 · Like

- Section: 5. Quiz results (quiz-player.tsx, result phase) (WIRING-MAP.md line 120)
- Owner: P4
- Build: shared flag-on production build on :3021 (feat/ux-v1-v11 dcc3159)
- Verdict: **PASS (payload + read state); DB effect NOT VERIFIED**

Map row (verbatim):

| Like | `POST /api/quiz/[id]/like` (`like-quiz-button`) | EXISTS | Same |

## Control
- Like on results. Guest classic quiz: not liked, 43; test user gllits quiz: pressed (liked), count 2.

## Call (endpoint, method, status / payload)
- GET /api/quiz/<id>/like 200 ({liked:false, like_count:43} guest). Clicks: STUBBED POST /api/quiz/4ba8f255-.../like {"action":"like"} (guest), STUBBED POST /api/quiz/62e332ec-.../like {"action":"unlike"} (test user).

## Database (read-only, service role `select` only)
- likes: the test user has 1 like, on 62e332ec (-> pressed); quizzes.like_count 62e332ec = 2, 4ba8f255 = 43.

## Flag off
- Flag off: legacy like-quiz-button, same endpoint.

## Notes
-

## Evidence
- `proofs/quiz-guest.json`
- `proofs/quiz-user.json`
- `proofs/sql-quiz-user.json`
