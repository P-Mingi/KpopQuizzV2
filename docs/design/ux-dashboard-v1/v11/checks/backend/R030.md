# R030 · Top search

- Section: 0. Shell (every page) (WIRING-MAP.md line 30)
- Owner: A0 (overlay) / P11 (results)
- Build: shared flag-on production build on :3021 (feat/ux-v1-v11 dcc3159)
- Verdict: **PASS**

Map row (verbatim):

| Top search | `/search` page + `GET /api/search`, `GET /api/quizzes/search` (`components/home/search-bar.tsx`) | EXISTS | Same component, new skin, results dropdown = NEW UI |

## Control
- Top search: a real <a href=/search>; a plain click opens the overlay. Typed 'blackpink'.

## Call (endpoint, method, status / payload)
- GET /api/ux-v1/p11/search (empty query, popular) 200, then GET /api/ux-v1/p11/search?q=blackpink 200. Results: group BLACKPINK -> /blackpink-quiz, 5 quizzes -> /q/<slug>, songs -> /blindtest/group-blackpink. Enter opened the first result (/blackpink-quiz). No results state for 'zzqxjvkw' ('No results for ...').

## Database (read-only, service role `select` only)
- Read-only endpoint over groups, quizzes (published), blind_test songs; BLACKPINK hub shows 24 quizzes = published count (P3 R351 SQL).

## Flag off
- Flag off: /api/ux-v1/p11/search?q=bts = 404 JSON; /search is the live page with no v11 call.

## Notes
-

## Evidence
- `proofs/shell-guest-1440.json (results, afterEnter, noResults)`
- `proofs/flag-off-endpoints-dev-4202.txt`
