# R255 · Challenge a friend

- Section: 12. Blindtest hub (/blindtest) (WIRING-MAP.md line 255)
- Owner: P6
- Build: shared flag-on production build on :3021 (feat/ux-v1-v11 dcc3159)
- Verdict: **PASS (payloads + route resolution); DB write NOT VERIFIED (owner decision 1)**

Map row (verbatim):

| Challenge a friend | `challenges` + `challenge_attempts` tables exist (short_code, frozen questions, expires_at), no code | NEW | `POST /api/blind-test/challenge` (after a run: store questions payload + creator score, return short_code), `/bt/c/[code]` page (plays the frozen set), `POST /api/blind-test/challenge/[code]/attempt`; "challenges waiting" = attempts where I am invited (needs an `invitee` or is public by link) |

## Control
- Challenge a friend (after a run) and the challenge link /blindtest?c=<code>.

## Call (endpoint, method, status / payload)
- Create: POST /api/ux-v1/p6/challenge (payload asserted by p6.spec 'results ... Copy link payload': the run without preview URLs). Invalid bodies -> 400 invalid_body before any write. Attempt: POST /api/ux-v1/p6/challenge/<code>/attempt resolves to the route on the production build and on the flag-on dev server (JSON 404 + no-store for an unknown code; Next's HTML 404 only for a path no route owns).

## Database (read-only, service role `select` only)
- challenges 0 rows, challenge_attempts 0 rows (tables exist; nothing written by C2).

## Flag off
- 404 flag off.

## Notes
- 'Challenges waiting' not shown (no invitee column: P6 owner item 6).

## Evidence
- `proofs/p6-challenge-invalid.txt`
- `proofs/nested-routes-prod-3021.txt`
- `proofs/nested-routes-dev-on-4202.txt`
- `proofs/e2e-p6-subset-1440.txt`
