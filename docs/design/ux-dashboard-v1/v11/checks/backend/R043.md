# R043 · Greeting "Good evening, Mingi"

- Section: 1. Dashboard (/) (WIRING-MAP.md line 43)
- Owner: P1
- Build: shared flag-on production build on :3021 (feat/ux-v1-v11 dcc3159)
- Verdict: **PASS**

Map row (verbatim):

| Greeting "Good evening, Mingi" | `profiles.username` via `GET /api/auth/me`; guest = "Good evening" | EXISTS | server render |

## Control
- Home header: guest = live hero H1 'Are you a real fan?' + H2 + 'Browse K-pop quizzes -> /quizzes' + 'Create a quiz -> /create'; signed in = 'Good evening, testtest' + one streak line (v11.2, see R363).

## Call (endpoint, method, status / payload)
- Guest: server HTML, no call. Signed in: GET /api/auth/me 200 (username testtest, display_name null -> the username is shown).

## Database (read-only, service role `select` only)
- profiles (test user): username testtest, display_name null.

## Flag off
- Flag off: the live home, no v11 request (proofs/flag-off-pages-*.json '/').

## Notes
- The streak line copy is wrong: C2-002 (R363).

## Evidence
- `proofs/home-guest-1440.json`
- `proofs/home-user-1440.json`
- `proofs/get-shell-user.json`
