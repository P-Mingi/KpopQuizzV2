# R079 · Challenge a friend (battle link)

- Section: 3. Quiz page (/q/[slug]) (WIRING-MAP.md line 79)
- Owner: P4
- Build: shared flag-on production build on :3021 (feat/ux-v1-v11 dcc3159)
- Verdict: **PASS (payload + read route against a real row); DB write NOT VERIFIED (owner decision 1)**

Map row (verbatim):

| Challenge a friend (battle link) | `battles` table exists; written today only by `POST /api/game/[id]/play` (games). Quiz battles: `docs/pending-migrations/154_battle_challenge_notification.sql` pending, `battle_beaten` notification type declared | PARTIAL | `POST /api/quiz/[id]/battle` (creates battles row with question_ids + challenger score) + `/b/{code}` route that opens the quiz with the frozen questions; needs mig 154 applied by owner |

Corrected by WIRING-MAP.verified.md: (a)5 / (b): the map's 'battles written by /api/game/[id]/play' was false; P4 built POST /api/ux-v1/p4/challenge on the existing battles table (no migration 154 needed).

## Control
- Challenge a friend: the results Share sheet mints a challenge link from the run; the receiver opens /q/<slug>?c=<id>.<sig>.

## Call (endpoint, method, status / payload)
- Guest results -> STUBBED POST /api/ux-v1/p4/challenge {quizId, questions:[the 8 question texts in the run order], score:8}.
- Read route against a REAL battles row (fe2ea6d6..., quiz c15ae418..., challenger_score 5, created 2026-09-20): GET /api/ux-v1/p4/challenge/<id>?s=<valid signature> -> 200 {score:5,total:10,maxScore:10,expired:true,questions:[]} (48 h window passed, so no questions leak); same id with a wrong signature -> 404. Attempt route: see R339 and the nested route check.

## Database (read-only, service role `select` only)
- battles: 1363 rows with quiz_id (legacy challenges), read-only; the signed id resolves to its row.

## Flag off
- All /api/ux-v1/p4/challenge* = 404 flag off.

## Notes
- The signature was computed locally with the server key from the env (never printed or committed); the proof file shows the status and body only.

## Evidence
- `proofs/quiz-guest.json (results: share + challenge)`
- `proofs/p4-challenge-read.txt`
- `proofs/nested-routes-prod-3021.txt`
- `proofs/flag-off-endpoints-dev-4202.txt`
