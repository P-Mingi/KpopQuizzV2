# R024 · Sidebar Quizzes + count 419

- Section: 0. Shell (every page) (WIRING-MAP.md line 24)
- Owner: A0
- Build: shared flag-on production build on :3021 (feat/ux-v1-v11 dcc3159)
- Verdict: **FAIL (flag off only: C2-001); the control itself passes (link; the count is dropped by design)**

Map row (verbatim):

| Sidebar Quizzes + count 419 | `/quizzes` (`components/quiz/browse-quizzes.tsx`), count = published quizzes (`lib/db/queries/quizzes.ts`) | EXISTS | Link + count query |

Corrected by WIRING-MAP.verified.md: (a)1: there was no published-total count; a new head count was needed.

## Control
- Top nav 'Quizzes -> /quizzes'. The v11 top nav has no count badge (prototype v11 nav: label + icon only), so the count is not shown.

## Call (endpoint, method, status / payload)
- Link only. /api/quizzes/count (phase 1 badge endpoint, head count of status='published') still exists but has NO caller in the app (grep apps/quiz/src, apps/quiz/e2e: 0 hits).

## Database (read-only, service role `select` only)
- quizzes status='published' head count = 425 (proofs/sql-shell.json); /api/quizzes/count answers {count:425} (flag off, proofs/flag-off-endpoints-dev-4202.txt).

## Flag off
- FINDING: /api/quizzes/count is the one endpoint added since main (450f053) that answers with the flag OFF (200 {count:425}, not 404). Read only, public, unused. Filed C2-001 to A0 (gate it behind UX_V1 or delete it).

## Notes
-

## Evidence
- `proofs/shell-guest-1440.json`
- `proofs/flag-off-endpoints-dev-4202.txt`
- `proofs/sql-shell.json`
