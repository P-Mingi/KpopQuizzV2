# R239 · Per-row read on click

- Section: 11. Notifications (/notifications) (WIRING-MAP.md line 239)
- Owner: P11
- Build: shared flag-on production build on :3021 (feat/ux-v1-v11 dcc3159)
- Verdict: **PASS (payload); DB effect NOT VERIFIED**

Map row (verbatim):

| Per-row read on click | same endpoint with id | EXISTS | Same |

## Control
- Per-row read on click.

## Call (endpoint, method, status / payload)
- Opening the unread row: STUBBED POST /api/notifications/mark-read {"ids":["c2-n1"]} then navigation to its /q/ link. Row menu: Mark as read / Mute this quiz / Dismiss: Dismiss -> STUBBED DELETE /api/notifications {"id":"c2-n2"}; Mute -> STUBBED POST /api/notifications/prefs {"muteQuiz":"4ba8f255-..."}.

## Database (read-only, service role `select` only)
- -

## Flag off
- Flag off: same endpoints.

## Notes
-

## Evidence
- `proofs/notifications-user-sample.json`
