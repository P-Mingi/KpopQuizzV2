# R352 · Group hub · Empty group (0 quizzes)

- Section: v10 additions and changes (2026-09-25) (WIRING-MAP.md line 352)
- Owner: P3
- Build: shared flag-on production build on :3021 (feat/ux-v1-v11 dcc3159)
- Verdict: **NOT VERIFIED until v11-p3-group-quiz-alerts.sql (fail-soft proven)**

Map row (verbatim):

| Group hub | Empty group (0 quizzes) | Make the first quiz + Notify me; noindex until 3 quizzes | NEW notify subscription (pending migration) |

## Control
- Empty group /chungha-quiz: 'Make the first quiz' + 'Notify me' (data-state off while the table is missing: a click shows the not-live toast, no request).

## Call (endpoint, method, status / payload)
- GET /api/ux-v1/p3/notify?group=57 -> {live:false} (guest and test user). POST {groupId:57,on:true}: guest 401 before any read; test user 503 not_live (read-only probe of the missing table, before any write); bad body 400.

## Database (read-only, service role `select` only)
- group_quiz_alerts: 'Could not find the table' (read-only select).

## Flag off
- GET/POST /api/ux-v1/p3/notify = 404 flag off.

## Notes
- noindex for thin hubs not shipped (owner decision 8).

## Evidence
- `proofs/hub-guest-1440.json (chungha)`
- `proofs/hub-user-1440.json`
- `proofs/p3-notify-failsoft.txt`
