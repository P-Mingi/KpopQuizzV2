# R293 · Play a ranked run

- Section: 14. Ranked (NEW product surface, /blindtest/ranked) (WIRING-MAP.md line 293)
- Owner: P7
- Build: shared flag-on production build on :3021 (feat/ux-v1-v11 dcc3159)
- Verdict: **NOT VERIFIED until v11-p7-ranked.sql (fail-soft + unit proven)**

Map row (verbatim):

| Play a ranked run | `POST /api/blind-test/generate` with `mode: 'ranked'`: server draws 10 songs from the whole pool, 4/4/2 by community accuracy (`blind_test_songs.times_correct / times_played`), 6 song + 4 artist rounds, ignores playlist and groups, stores the drawn set with a run id (in `ranked_plays` as a pending row or in a signed payload) |

## Control
- Play a ranked run: /blindtest/ranked shows 'Not live yet ... The first season has not started' (guest and test user); no play button.

## Call (endpoint, method, status / payload)
- POST /api/ranked/run (guest and test user) -> 503 {ranked:not_live, reason:migration_missing}: liveSeason() reads ranked_seasons first and throws before auth and before any write. GET /api/ranked/run -> 405.

## Database (read-only, service role `select` only)
- ranked_seasons and ranked_runs do not exist ('Could not find the table'); ranked_plays 0 rows.

## Flag off
- All /api/ranked/* = 404 flag off; /blindtest/ranked = 404.

## Notes
- Draw 4 easy / 4 medium / 2 hard, 6 song + 4 artist rounds: select.test.ts (25 tests) in proofs/unit-p7-ranked.txt. The draw uses songs.tier until ranked answers exist (DECISIONS: songs has no accuracy stats).

## Evidence
- `proofs/p7-ranked-failsoft.txt`
- `proofs/ranked-guest.json`
- `proofs/ranked-user.json`
- `proofs/unit-p7-ranked.txt`
- `proofs/flag-off-endpoints-dev-4202.txt`
