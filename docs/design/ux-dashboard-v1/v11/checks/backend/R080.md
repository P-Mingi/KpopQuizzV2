# R080 · Share Reddit / Discord / X / copy

- Section: 3. Quiz page (/q/[slug]) (WIRING-MAP.md line 80)
- Owner: P4 (A0 ShareSheet)
- Build: shared flag-on production build on :3021 (feat/ux-v1-v11 dcc3159)
- Verdict: **PASS (payload); DB effect NOT VERIFIED**

Map row (verbatim):

| Share Reddit / Discord / X / copy | `components/share/*`, `POST /api/share/generate`, `/api/share/reddit-image`, `/s/[code]` redirect + `/api/share/click/[code]` | EXISTS | Same |

## Control
- Share this quiz sheet: Copy link, More apps, Discord.

## Call (endpoint, method, status / payload)
- Guest: sheet opens, no write (UTM URL). Test user: STUBBED POST /api/share/generate {"quizId":"6f3eef40-...","platform":"link"} (lib/share.ts payload).

## Database (read-only, service role `select` only)
- n/a (would insert a share row: owner decision 1).

## Flag off
- Flag off: legacy share components.

## Notes
-

## Evidence
- `proofs/quiz-guest.json (shareSheet)`
- `proofs/quiz-user-share.json`
