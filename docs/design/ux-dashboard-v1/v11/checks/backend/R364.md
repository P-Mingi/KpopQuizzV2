# R364 · Home · Live ticker

- Section: v11 additions (2026-09-25) (WIRING-MAP.md line 364)
- Owner: P1
- Build: shared flag-on production build on :3021 (feat/ux-v1-v11 dcc3159)
- Verdict: **PASS**

Map row (verbatim):

| Home | Live ticker | Cycles recent activity, falls back to fans playing now, hides when neither | EXISTS components/home/activity-ticker.tsx, /api/activity/recent, /api/stats/live (owner to decide on the random floor) |

## Control
- Live ticker, sampled 14 times over 14 s.

## Call (endpoint, method, status / payload)
- GET /api/activity/recent 200 + GET /api/stats/live 200. Every line shown is one of the endpoint's lines (e.g. 'lore_stay_5 scored 4/7 on the BLACKPINK quiz' = event 43236).

## Database (read-only, service role `select` only)
- The 14 event ids the endpoint returned all exist in activity_events (head count with id in [...] = 14). The online-count fallback (random floor 12-27, owner decision 7) was not shown: real events were available.

## Flag off
- Flag off: the live activity ticker (same endpoints).

## Notes
-

## Evidence
- `proofs/home-guest-1440.json (ticker)`
- `proofs/sql-home-qotd.json (activity ids found = 14)`
