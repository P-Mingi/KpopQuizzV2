# R135 · Autosave chip

- Section: 6. Create (/create, create-funnel.tsx, 4 steps -> 3 visible + done) (WIRING-MAP.md line 135)
- Owner: P5
- Build: shared flag-on production build on :3021 (feat/ux-v1-v11 dcc3159)
- Verdict: **PASS**

Map row (verbatim):

| Autosave chip | `lib/create-draft.ts` (localStorage draft + step) | EXISTS | Show the saved-at time |

## Control
- Autosave (draft saved on this device).

## Call (endpoint, method, status / payload)
- No request: localStorage kq_create_draft_v1 + kq_create_step_v1 (lib/create-draft keys, same as the legacy funnel) written while typing (guest and test user).

## Database (read-only, service role `select` only)
- n/a (client draft).

## Flag off
- Flag off: legacy create-funnel, same keys.

## Notes
-

## Evidence
- `proofs/create-guest-1440.json (draftKeys)`
- `proofs/create-user-1440.json`
