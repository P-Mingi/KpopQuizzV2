# R115 · Share this card

- Section: 5. Quiz results (quiz-player.tsx, result phase) (WIRING-MAP.md line 115)
- Owner: P4
- Build: shared flag-on production build on :3021 (feat/ux-v1-v11 dcc3159)
- Verdict: **PASS (payload); DB effect NOT VERIFIED**

Map row (verbatim):

| Share this card | `handleShare` + `POST /api/quiz/[id]/share-card` | EXISTS | Same |

## Control
- Share your score sheet: Copy link, Story image, More apps, Discord.

## Call (endpoint, method, status / payload)
- Guest: STUBBED POST /api/ux-v1/p4/challenge (the challenge link in the sheet, R079). Signed in: the share link goes through STUBBED POST /api/share/generate {quizId, platform:'link'} (R080). The legacy POST /api/quiz/[id]/share-card is not used (the story PNG is drawn in the browser).

## Database (read-only, service role `select` only)
- -

## Flag off
- Flag off: legacy handleShare.

## Notes
-

## Evidence
- `proofs/quiz-guest.json (results.shareSheet)`
- `proofs/quiz-user-share.json`
