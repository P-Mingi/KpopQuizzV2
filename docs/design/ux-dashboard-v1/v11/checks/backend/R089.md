# R089 · Report

- Section: 3. Quiz page (/q/[slug]) (WIRING-MAP.md line 89)
- Owner: P4
- Build: shared flag-on production build on :3021 (feat/ux-v1-v11 dcc3159)
- Verdict: **PASS (payload); DB effect NOT VERIFIED (owner decision 1)**

Map row (verbatim):

| Report | `POST /api/quiz/[id]/report` (`report-form.tsx`) | EXISTS | Same |

## Control
- Report sheet: 'Wrong answers' + Send report.

## Call (endpoint, method, status / payload)
- STUBBED POST /api/quiz/4ba8f255-.../report {"reason":"wrong_answers","details":""} (the existing endpoint).

## Database (read-only, service role `select` only)
- n/a (would insert a report).

## Flag off
- Flag off: legacy report-form, same endpoint.

## Notes
-

## Evidence
- `proofs/quiz-guest.json (results: report)`
