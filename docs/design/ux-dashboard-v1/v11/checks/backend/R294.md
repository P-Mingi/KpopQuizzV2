# R294 · Submit run

- Section: 14. Ranked (NEW product surface, /blindtest/ranked) (WIRING-MAP.md line 294)
- Owner: P7
- Build: shared flag-on production build on :3021 (feat/ux-v1-v11 dcc3159)
- Verdict: **NOT VERIFIED until v11-p7-ranked.sql (server scoring unit-proven)**

Map row (verbatim):

| Submit run | `POST /api/blind-test/ranked/submit`: answers with `time_ms`; server recomputes points (100 + speed bonus 100*(1 - max(0,t-2)/8) capped at 0, x combo 1.0 + 0.1 per consecutive correct, cap 2.0); inserts `ranked_plays` (player_id, score, correct_count, total_rounds, best_combo, avg_speed_ms, song_ids, points, season) |

## Control
- Submit a run: server recomputes points.

## Call (endpoint, method, status / payload)
- POST /api/ranked/run/start, /answer, /submit -> 503 not_live (guest and test user) before any write.

## Database (read-only, service role `select` only)
- -

## Flag off
- 404.

## Notes
- Ranked math: scoring.test.ts 63 table tests (speed bonus, combo multiplier cap, rounding, quit = 0), timing.test.ts 20 (server clip start, client time clamps). Token replay: run.test.ts 'the token is single use: a full run submits once; every later call on the token is refused', 'an answer is locked once (replaying it is refused)'; service.test.ts 'token replay is rejected: a second submit, and answering after submit', 'another player's token is not found'. 233/233 ranked tests pass.

## Evidence
- `proofs/p7-ranked-failsoft.txt`
- `proofs/unit-p7-ranked.txt`
